# DataPulse 第 01 次测试计划：核心闭环与容器构建回归

## 本次目标

本次按仓库当前真实能力验证以下 P0/P1 流程：

- 首次初始化、管理员登录、会话恢复、退出和密码/CSRF/Origin 保护。
- SQLite 数据源创建、连接测试、Schema 浏览、只读参数化 SQL 查询和 SQL 数据集创建/预览。
- 文件数据集的上传、Sheet/样例预览、维护和引用删除限制。
- 大屏空白/模板创建、九类内置组件编辑、撤销/重做、800 ms 自动保存、刷新恢复和 revision 冲突处理。
- 草稿预览、明确确认发布、发布快照与草稿隔离、复制作为人工备份。
- 独立播放密钥交换/轮换、访问策略、单组件查询失败隔离和 16:9 留白。
- Embed API Key、短期 ticket、Origin/参数限制、Bearer-only API 和 `@datapulse/embed-sdk` 通信。
- 针对 HEAD 提交 `a0e973e` 的 Docker web stage 回归：容器构建能够解析并打包 `packages/embed-sdk` 工作区包。

仓库没有历史版本恢复或 rollback API；本计划中的“失败恢复”只验证自动保存后的重新加载、revision 冲突后的重新加载、发布失败时旧快照保持不变，以及复制大屏作为人工备份。没有提供具体目标占位符的进一步业务范围时，以上范围以 README、P0 冒烟表、当前路由和最近提交为准。

## 当前代码和版本依据

- 项目：DataPulse；代码路径：`/Users/dundundebaba/sync/code/open/DataPulse`。
- 分支：`main`；HEAD：`a0e973e0475f01dd735dcae6448fd56fcc562ae`（`fix: include embed SDK in container build`）。
- 工作区（生成本文件前）：无已跟踪文件改动；存在未跟踪的 `.codegraph/`、`.idea/` 和 `docs/TESTING_SESSION.md`，本计划不清理、不覆盖。生成后的本测试计划文件是预期新增产物。
- 维护文档目录：`/Users/dundundebaba/sync/code/open/DataPulse/docs`，包括 `known-issues.md`、`TESTING_SESSION.md`、`docs/superpowers/plans/` 和 `docs/superpowers/specs/`。
- 后端为 FastAPI，认证接口为 `/api/auth`，管理接口集中在 `/api/admin/datasources`、`/api/admin/datasets`、`/api/admin/screens`；播放接口为 `/api/player`，嵌入接口为 `/api/embed`。
- 前端路由包括 `/studio/setup`、`/studio/login`、`/studio/datasources`、`/studio/datasets`、`/studio/screens`、`/studio/screens/:id/edit`、`/studio/screens/:id/preview`、`/play/:screenId` 和 `/embed/:screenId`。
- 相关实现：`apps/server/src/datapulse/auth/`、`datasource/`、`dataset/`、`filedata/`、`screen/`、`display/`、`embedding/`、`query/`、`contracts/`，以及 `apps/web/src/features/auth/`、`datasources/`、`datasets/`、`screens/`、`player/`、`runtime/` 和 `apps/web/src/e2eEmbedHost.ts`。
- 最近代码改动只涉及 `Dockerfile`：web 构建阶段现在复制并安装 `packages/embed-sdk/package.json`，并复制 `packages/embed-sdk` 源码后执行 `pnpm build:web`。
- 现有测试目录：`apps/server/tests/`、`apps/web/src/**/*.test.ts`、`apps/web/e2e/`、`packages/embed-sdk/src/index.test.ts` 和 `packages/schema/src/index.test-d.ts`。

## 测试环境与前置条件

- Node.js 22、pnpm 10、Python `>=3.13,<3.15`、uv、Docker/Compose；当前工具链命令已确认可被调用。依赖准备使用：
  `uv sync --all-packages --group dev`、`pnpm install`。
- 本地 Studio 默认前端 `http://127.0.0.1:5173`，Vite 默认代理后端 `http://127.0.0.1:8000`；如端口占用，使用未占用端口并设置 `VITE_API_PROXY_TARGET`，在证据中记录实际端口。
- 启动本地服务前执行 `uv run --package datapulse-server alembic -c apps/server/alembic.ini upgrade head`；后端使用 `uv run --package datapulse-server uvicorn datapulse.app:app --host 127.0.0.1 --port <port>`，前端使用 `pnpm dev:web`。
- E2E 通过 `tools/run_e2e.py` 动态预留端口、创建临时 SQLite/静态目录和 fake AI 服务；Playwright 配置当前只有 Chromium 项目，运行结束由 `global-teardown.ts` 仅清理本次临时目录。
- 浏览器至少安装 Chromium：`pnpm --filter @datapulse/web exec playwright install chromium`。Firefox/WebKit 不在当前 Playwright 配置中，需人工另行确认是否安装。
- 测试管理员使用 `admin` 和不少于 10 位的专用密码；初始化码只使用测试环境生成或 `DATAPULSE_BOOTSTRAP_CODE_OVERRIDE`，不使用生产初始化码。
- SQLite 数据使用临时目录中的副本；E2E 由 `apps/web/e2e/fixtures/sales.sql` 生成 `sales.db`。文件数据使用仓库 fixtures：`file-dataset.csv`、`file-dataset-zh.csv`、`file-dataset.xlsx`、`file-dataset.json`、`file-dataset.parquet`。
- 外部数据库集成依赖 `compose.test.yaml` 中 PostgreSQL `127.0.0.1:55432` 和 MariaDB `127.0.0.1:53306`；只使用文档中的测试账号和数据库，不连接生产或个人数据库。
- AI 自动化只使用 `tools/run_e2e.py` 的本地 fake OpenAI-compatible 服务；默认关闭 AI 或人工联调时不得把真实客户数据、数据库密码、API Key、签名密钥和绝对文件路径发送给第三方模型。
- 每次测试使用独立临时数据目录、测试账号、测试密钥、Docker 容器/卷和端口；不得使用真实凭据、生产 URL、真实客户文件或可识别个人信息。

## 自动化测试

自动化用例的正常、边界、异常、权限/安全、失败恢复和数据一致性均有覆盖。不适用及原因：历史版本恢复/rollback 不适用，当前代码和协议明确只维护 `draft_document` 与 `published_document`，没有版本历史接口；以自动保存、revision 冲突、发布失败旧快照和复制备份替代验证。

### 单元测试

- AUTO-UNIT-001：认证初始化、限流、Session 和密码边界
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/auth/`；初始化码单次使用/过期、密码长度、统一登录错误、限流、CSRF、会话撤销。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/auth/test_bootstrap.py apps/server/tests/auth/test_api.py apps/server/tests/auth/test_limiter.py apps/server/tests/auth/test_password.py apps/server/tests/auth/test_session.py -q`
  - 前置条件：已安装 uv 依赖；pytest 使用临时目录 fixture；不需要外部数据库。
  - 预期结果：覆盖正常初始化/登录和会话恢复；错误初始化码、短密码、过期/重复码、错误凭据和第六次失败登录返回稳定错误；密码变更撤销其他会话，CSRF/Origin 缺失被拒绝；测试进程退出码为 0。
  - 失败后证据：pytest 完整 stdout/stderr、失败 traceback 和 `X-Request-ID`（如有）保存为 `test-evidence/AUTO-UNIT-001.log`。
  - 实际结果：退出码 `0`；21 passed。
  - 问题编号：无

- AUTO-UNIT-002：数据源模型、密钥封装与 SQL 安全/限制
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/datasource/models.py`、`secrets.py`、`apps/server/src/datapulse/query/`；路径、端口、秘密不回显、只读 AST、参数绑定、行数/超时/并发边界。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/datasource/test_models.py apps/server/tests/datasource/test_secrets.py apps/server/tests/query/test_safety.py apps/server/tests/query/test_parameters.py apps/server/tests/query/test_limits.py -q`
  - 前置条件：使用测试 master key fixture 或临时随机密钥；不连接生产数据源。
  - 预期结果：合法配置可序列化；路径穿越、非法端口、错误密钥、写 SQL、多语句、缺失/错误参数、超时/并发超限均被稳定拒绝；密码/API 凭据不进入响应或错误字符串；边界值符合配置上限；退出码为 0。
  - 失败后证据：pytest 日志、失败用例输入摘要（脱敏）和错误响应快照，保存为 `test-evidence/AUTO-UNIT-002.log`。
  - 实际结果：退出码 `0`；85 passed。
  - 问题编号：无

- AUTO-UNIT-003：Dashboard/Chart 契约、发布校验与屏幕仓储一致性
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/contracts/dashboard.py`、`contracts/chart.py`、`screen/publishing.py`、`screen/repository.py`、`screen/service.py`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/contracts/test_dashboard.py apps/server/tests/contracts/test_chart.py apps/server/tests/screen/test_publishing.py apps/server/tests/screen/test_repository.py apps/server/tests/screen/test_service.py apps/server/tests/screen/test_runtime.py apps/server/tests/screen/test_chart_query.py -q`
  - 前置条件：使用测试仓储和临时元数据库 fixture；准备合法与非法组件、字段、数据集和资源引用。
  - 预期结果：合法文档/图表可往返；重复 ID、重复参数、越界画布、未知组件、视觉类型不匹配、缺失数据集/资产和 revision 不一致阻止发布；成功发布只覆盖当前快照，失败不破坏原快照；复制文档的组件 ID 独立；退出码为 0。
  - 失败后证据：pytest 日志、发布前后脱敏文档 JSON 差异和 `test-evidence/AUTO-UNIT-003.log`。
  - 实际结果：退出码 `0`；54 passed。
  - 问题编号：无

- AUTO-UNIT-004：文件解析、存储隔离与 DuckDB 查询限制
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/src/datapulse/filedata/parsers.py`、`storage.py`、`duckdb_executor.py`、`query.py`；CSV/Excel/JSON/Parquet、Sheet、文件大小/行数/超时和路径隔离。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/filedata/test_parsers.py apps/server/tests/filedata/test_storage.py apps/server/tests/filedata/test_duckdb_executor.py apps/server/tests/contracts/test_filedata.py -q`
  - 前置条件：使用临时文件和仓库固定 fixtures；不上传真实文件。
  - 预期结果：支持格式和多 Sheet 样例可解析；空/坏/未知格式、超限、越界路径和查询失败返回稳定错误且不遗留不可访问文件；服务端不泄露绝对路径；退出码为 0。
  - 失败后证据：pytest 日志、格式/Sheet 失败样例摘要、临时文件清单和 `test-evidence/AUTO-UNIT-004.log`。
  - 实际结果：退出码 `0`；39 passed。
  - 问题编号：无

- AUTO-UNIT-005：认证、数据源、数据集和 SQL 调试前端状态
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web/src/features/auth/auth.test.ts`、`datasources/datasource-form.test.ts`、`datasources/schema-browser.test.ts`、`datasets/datasets.test.ts`、`query/sql-debug.test.ts`。
  - 执行命令：`pnpm --filter @datapulse/web exec vitest run src/features/auth/auth.test.ts src/features/datasources/datasource-form.test.ts src/features/datasources/schema-browser.test.ts src/features/datasets/datasets.test.ts src/features/query/sql-debug.test.ts`
  - 前置条件：pnpm workspace 依赖已安装；Vitest 使用 happy-dom 和测试 fetch mock。
  - 预期结果：路由守卫、初始化/登录/退出、表单校验、凭据不回显、Schema 加载/错误态、参数化查询、空/错误结果和删除确认符合组件契约；退出码为 0。
  - 失败后证据：Vitest stdout/stderr、失败组件 DOM 快照和 `test-evidence/AUTO-UNIT-005.log`、`test-evidence/AUTO-UNIT-005-targeted.log`。
  - 实际结果：原计划命令因参数传递实际运行全量 Web 套件（28 files/122 tests passed）；随后使用直接 Vitest 定向命令复核，5 files/30 tests passed，退出码均为 `0`。
  - 问题编号：ISS-002（已修复）

- AUTO-UNIT-006：大屏创建、编辑器几何、命令历史、自动保存、运行时和播放器
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web/src/features/screens/{screen-list,templates}.test.ts`、`apps/web/src/features/screens/editor/{geometry,commands,canvas,store,inspector}.test.ts`、`apps/web/src/features/runtime/runtime.test.ts`、`apps/web/src/features/player/{standalone-player,embed-player,player}.test.ts`。
  - 执行命令：`pnpm --filter @datapulse/web exec vitest run src/features/screens/screen-list.test.ts src/features/screens/templates.test.ts src/features/screens/editor/geometry.test.ts src/features/screens/editor/commands.test.ts src/features/screens/editor/canvas.test.ts src/features/screens/editor/store.test.ts src/features/screens/editor/inspector.test.ts src/features/runtime/runtime.test.ts src/features/player/standalone-player.test.ts src/features/player/embed-player.test.ts src/features/player/player.test.ts`
  - 前置条件：前端依赖已安装；使用测试文档和 fetch/window mock。
  - 预期结果：0.5/1/1.5 缩放下坐标、边界、复制粘贴、多选、锁定/隐藏、撤销/重做和 800 ms 自动保存稳定；revision 冲突停止自动重试；播放/嵌入 scrubbing、Bearer、Origin、参数和错误隔离符合预期；退出码为 0。
  - 失败后证据：Vitest 日志、组件 DOM/消息快照和 `test-evidence/AUTO-UNIT-006.log`、`test-evidence/AUTO-UNIT-006-targeted.log`。
  - 实际结果：原计划命令因参数传递实际运行全量 Web 套件（28 files/122 tests passed）；随后使用直接 Vitest 定向命令复核，11 files/59 tests passed，退出码均为 `0`。
  - 问题编号：ISS-002（已修复）

- AUTO-UNIT-007：Embed SDK 协议和多实例生命周期
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`packages/embed-sdk/src/index.ts`、`protocol.ts` 及 `packages/embed-sdk/src/index.test.ts`。
  - 执行命令：`pnpm --filter @datapulse/embed-sdk test`
  - 前置条件：pnpm workspace 依赖已安装；在 happy-dom 中运行，不访问真实嵌入服务。
  - 预期结果：ticket 只作为 bootstrap URL 参数并指定明确 Origin；错误来源/窗口/实例被忽略；请求 ID 正确关联；`destroy` 移除监听；多个实例互不串扰；退出码为 0。
  - 失败后证据：Vitest 日志、`postMessage` 调用快照和 `test-evidence/AUTO-UNIT-007.log`。
  - 实际结果：退出码 `0`；5 passed。happy-dom iframe abort stderr 不影响退出码。
  - 问题编号：无

### 集成/API 测试

集成/API 用例覆盖管理接口的正常、参数边界、异常映射、未登录/无 CSRF/错误 Origin、revision/发布一致性、失败恢复和外部连接器；历史版本恢复不适用，原因同上。

- AUTO-INT-001：认证、数据源和数据集 API 主流程及权限
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/auth/api.py`、`datasource/api.py`、`dataset/api.py`；`/api/auth/*`、`/api/admin/datasources/*`、`/api/admin/datasets/*`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/auth/test_api.py apps/server/tests/datasource/test_api.py apps/server/tests/datasource/test_repository.py apps/server/tests/dataset/test_api.py apps/server/tests/dataset/test_repository.py -q`
  - 前置条件：使用各测试模块的临时元数据库和 SQLite fixture；不需要 PostgreSQL/MySQL。
  - 预期结果：未登录返回认证错误；写请求缺 CSRF/Origin 被拒绝；合法 CRUD、连接测试、Schema、只读查询、参数和数据集预览返回契约格式；重名、不存在、数据源被引用、危险 SQL、超时和并发错误保持稳定状态码/错误码；重复提交不产生重复资源；退出码为 0。
  - 失败后证据：pytest 日志、HTTP 请求/响应（脱敏）、错误 envelope 和 `test-evidence/AUTO-INT-001.log`。
  - 实际结果：退出码 `0`；22 passed。
  - 问题编号：无

- AUTO-INT-002：大屏草稿、预览、发布、复制和访问策略 API
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/screen/api.py`、`runtime_api.py`、`publishing.py`、`access.py`；`/api/admin/screens/*`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/screen/test_api.py apps/server/tests/screen/test_access_policy_api.py apps/server/tests/screen/test_runtime_api.py apps/server/tests/screen/test_publishing.py apps/server/tests/screen/test_assets.py -q`
  - 前置条件：使用临时管理员、数据集、资产和 SQLite；准备合法文档与非法引用。
  - 预期结果：创建/更新/复制/删除和草稿组件查询要求管理员及 CSRF；`expected_revision` 正确递增且冲突返回 409；预览读取草稿；发布前校验所有引用，失败时已发布快照保持不变；策略接受空值、Origin/IP/CIDR 合法值并拒绝非法值；退出码为 0。
  - 失败后证据：pytest 日志、发布前后 `draft_document`/`published_document` 脱敏 JSON、状态码和 `test-evidence/AUTO-INT-002.log`。
  - 实际结果：退出码 `0`；25 passed。
  - 问题编号：无

- AUTO-INT-003：独立播放密钥、播放会话和运行时访问
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/display/api.py`、`display/service.py`、`display/tokens.py`；`/api/admin/screens/{id}/display-key`、`/api/player/screens/{id}/*`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/display/test_api.py apps/server/tests/display/test_service.py apps/server/tests/display/test_tokens.py -q`
  - 前置条件：提供独立随机的测试 signing key；使用已发布、未发布和不存在的大屏。
  - 预期结果：合法 key 只能换取绑定大屏的 HttpOnly 播放会话；无效/过期/撤销 key、未发布和错误 screen ID 返回稳定错误；轮换 key 使旧会话不能续期；运行时仅读取已发布文档，响应不泄露内部错误；退出码为 0。
  - 失败后证据：pytest 日志、Cookie 属性、响应头/体和 `test-evidence/AUTO-INT-003.log`，禁止保存明文 key。
  - 实际结果：退出码 `0`；7 passed。
  - 问题编号：无

- AUTO-INT-004：Embed API Key、ticket、Origin/参数和响应安全头
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/embedding/api.py`、`service.py`、`tokens.py`；`/api/admin/embed/api-key`、`/api/embed/tickets`、`/api/embed/screens/{id}/*`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/embedding/test_api.py apps/server/tests/embedding/test_service.py apps/server/tests/embedding/test_tokens.py apps/server/tests/contracts/test_embed.py -q`
  - 前置条件：提供独立随机测试 signing key；使用非生产 Origin、已发布大屏和 mutable/immutable 参数。
  - 预期结果：旧 API Key 轮换后失效；ticket 生命周期不超过 8 小时且绑定 screen、完整 Origin 和参数范围；错误 API Key/ticket、错误 Origin、越权参数和错误 screen 被拒绝；嵌入读取使用 Bearer 且不带 Cookie，响应含 `Cache-Control: no-store`、精确 `frame-ancestors`、`no-referrer`、`nosniff`；退出码为 0。
  - 失败后证据：pytest 日志、请求头/响应头、脱敏 claims 和 `test-evidence/AUTO-INT-004.log`。
  - 实际结果：退出码 `0`；41 passed。
  - 问题编号：无

- AUTO-INT-005：文件资产 API、文件数据集生命周期和引用清理
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/src/datapulse/filedata/api.py`、`dataset/api.py`、`filedata/service.py`；`/api/admin/files*` 和 `/api/admin/datasets/files`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/filedata/test_api.py apps/server/tests/dataset/test_api.py -q`
  - 前置条件：使用临时 CSV/Excel 和管理员会话；所有上传内容均为非敏感测试数据。
  - 预期结果：上传、预览、Sheet 选择、文件数据集创建/更新/删除契约一致；缺失/坏文件返回稳定错误；被数据集引用的资产不能删除，删除数据集后资产才可清理；未登录和无 CSRF 请求被拒绝；退出码为 0。
  - 失败后证据：pytest 日志、资产列表/数据集定义脱敏快照、临时文件清单和 `test-evidence/AUTO-INT-005.log`。
  - 实际结果：退出码 `0`；6 passed。
  - 问题编号：无

- AUTO-INT-006：PostgreSQL/MariaDB 连接器契约和超时/只读防护
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/src/datapulse/datasource/postgresql.py`、`mysql.py`、`_sqlalchemy.py` 及连接器契约测试。
  - 执行命令：`docker compose -f compose.test.yaml up -d --wait`；随后执行 `DATAPULSE_TEST_POSTGRES_URL='postgresql+asyncpg://datapulse:datapulse@127.0.0.1:55432/datapulse' DATAPULSE_TEST_MYSQL_URL='mysql+asyncmy://datapulse:datapulse@127.0.0.1:53306/datapulse' pnpm test:integration`。
  - 前置条件：Docker 可用且 55432/53306 未占用；仅使用 `compose.test.yaml` 的测试数据库。执行结束后必须执行 `docker compose -f compose.test.yaml down -v`。
  - 预期结果：两种外部连接器的连接、catalog、参数化查询、重复列/空结果、数据库侧超时和绕过上层校验时的写操作拒绝符合同一契约；测试服务或 DSN 不可用时记录为阻塞，不得误报为功能通过；退出码为 0 才满足该用例。
  - 失败后证据：pytest/Compose 日志、数据库服务健康状态、脱敏 DSN 和 `test-evidence/AUTO-INT-006-rerun.log`、`test-evidence/AUTO-INT-006-compose-up-rerun.log`。
  - 实际结果：OrbStack 启动后 Compose 两个服务均为 Healthy；集成套件退出码 `0`，13 passed、12 deselected。
  - 问题编号：无（首次执行因 Docker daemon 未启动的阻塞记录见 ISS-001）

- AUTO-INT-007：迁移头、应用生命周期、健康检查和错误 envelope
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/src/datapulse/lifespan.py`、`app.py`、`errors.py`、Alembic migration；`/api/health`。
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/metadata/test_migrations.py apps/server/tests/test_lifespan.py apps/server/tests/test_app.py apps/server/tests/test_errors.py -q`
  - 前置条件：使用临时数据目录和测试 Settings；不复用开发或生产元数据库。
  - 预期结果：迁移头不匹配时启动失败并给出可定位信息；正常生命周期创建并关闭服务；健康检查版本来自 `Settings`；错误响应含稳定 code/message/request_id 且不泄露堆栈/秘密；退出码为 0。
  - 失败后证据：pytest 日志、临时数据库版本、健康响应和 `test-evidence/AUTO-INT-007.log`。
  - 实际结果：退出码 `0`；11 passed。
  - 问题编号：无

### E2E、构建、静态检查和视觉回归

这些用例覆盖跨页面正常/边界/异常流程、浏览器权限边界、失败恢复、草稿/发布数据一致性、静态契约、类型、生产构建、视觉快照和本次 Dockerfile 改动。浏览器视觉回归只使用配置中实际存在的 Chromium 项目。

- AUTO-E2E-001：核心流程全量 Playwright 回归
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web/e2e/*.spec.ts`、Playwright webServer、`tools/run_e2e.py`；初始化/登录、SQLite、文件、AI、编辑、发布、播放、访问策略和嵌入。
  - 执行命令：`pnpm --filter @datapulse/web exec playwright install chromium`（首次环境准备）；`pnpm test:e2e`
  - 前置条件：uv/pnpm 依赖已安装，Chromium 可启动；运行器会创建 fake AI 和临时 SQLite，不使用已存在开发数据。
  - 预期结果：所有现有 E2E 场景完成；P0 流程能从初始化到发布/播放/嵌入，非法 AI/参数/Origin 不创建非法草稿或绕过权限，刷新/自动保存后的文档与 API 读取一致；退出码为 0。
  - 失败后证据：Playwright HTML 报告、失败截图、trace、浏览器 console、Network 请求和 `test-evidence/AUTO-E2E-001/`。
  - 实际结果：退出码 `0`；14 passed（49.4s）。
  - 问题编号：无

- AUTO-E2E-002：播放比例、主题、组件错误隔离视觉快照
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/web/e2e/screen-playback.spec.ts`、`ScreenRuntime.vue`、`PlayerView.vue`；`apps/web/e2e/snapshots/{darwin,linux}/*player*.png`。
  - 执行命令：`pnpm test:e2e -- --grep "published screens retain stable dark, light, isolated-error, and letterbox visuals"`
  - 前置条件：Chromium、E2E fake SQLite 和固定 fixtures；运行器会使用平台快照目录，执行前确认平台快照已存在。
  - 预期结果：深色/浅色主题渲染稳定；一个组件查询失败不阻断其他组件；1200x900 容器保持 16:9 contain、留边、不拉伸/裁切；快照差异必须由执行者分类记录，退出码为 0 才满足用例。
  - 失败后证据：Playwright diff 图片、基准/实际快照、trace、控制台错误和 `test-evidence/AUTO-E2E-002/`。
  - 实际结果：退出码 `0`；1 passed（12.3s），未产生未分类视觉差异。
  - 问题编号：无

- AUTO-E2E-003：契约生成、前端/SDK/schema 类型检查和生产构建门禁
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`tools/export_schemas.py`、`tools/generate_types.mjs`、`tools/check_generated.py`、web/schema/embed-sdk TypeScript 配置和 Vite 构建。
  - 执行命令：`pnpm verify`
  - 前置条件：依赖已安装；执行前检查不要把 `.env`、临时数据库、E2E 目录或 `dist` 产物当作业务变更提交。
  - 预期结果：契约导出/生成结果与仓库生成文件一致；Embed SDK、schema、Vue/Playwright 类型检查无错误；单元测试和 Web 生产构建返回退出码 0；仅允许记录已配置的大 chunk 警告，不得把警告当作功能结果。
  - 失败后证据：完整命令日志、生成文件 diff、TypeScript/Vite 报告和 `test-evidence/AUTO-E2E-003.log`。
  - 实际结果：`pnpm verify` 退出码 `0`；Embed SDK 5 passed、Web 122 passed、后端 437 passed、13 deselected，类型检查和 Web 构建通过。存在 Rollup 注释及大 chunk warning，不影响退出码。
  - 问题编号：无（warning 作为风险记录）

- AUTO-E2E-004：Docker web stage 解析 Embed SDK 工作区包
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`Dockerfile` 的 web stage、根 `package.json`、`pnpm-workspace.yaml`、`apps/web/package.json`、`packages/schema/package.json`、`packages/embed-sdk/package.json`。
  - 执行命令：`docker build --tag datapulse-test:20260829 .`
  - 前置条件：Docker daemon 可用；构建上下文为仓库根目录；不把真实 `.env` 或敏感文件复制进上下文。
  - 预期结果：依赖安装使用 frozen lockfile；web stage 能解析 `@datapulse/schema` 和 `@datapulse/embed-sdk` 工作区包并完成 `pnpm build:web`；不出现 workspace package missing、lockfile 不一致或 TypeScript/Vite 构建错误；退出码为 0。
  - 失败后证据：`docker build --progress=plain` 完整日志、镜像 inspect 信息和 `test-evidence/AUTO-E2E-004-final.log`、`test-evidence/AUTO-E2E-004-image-inspect.log`。
  - 实际结果：使用镜像源预拉取 `python:3.13-slim` 后原始 Dockerfile 构建退出码 `0`；Node/web stage、Embed SDK 工作区复制、`pnpm build:web` 和 runtime 依赖安装均完成，生成镜像 ID `sha256:109992e744434c8ff53a526fff53d289d6e1a75ce86220dd9225ae18167c8195`。
  - 问题编号：ISS-005（已修复）

- AUTO-E2E-005：容器启动、迁移和健康检查烟囱
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/docker-entrypoint.sh`、runtime stage、`compose.yaml`、`/api/health` 和静态 SPA fallback。
  - 执行命令：先准备临时 `DATAPULSE_PLAN_MASTER_KEY`、`DATAPULSE_PLAN_SIGNING_KEY`（均为随机 32 字节 Base64，不复用生产密钥），再执行：
    `docker run --name datapulse-plan -d -p 18000:8000 -e DATAPULSE_MASTER_KEY="$DATAPULSE_PLAN_MASTER_KEY" -e DATAPULSE_SIGNING_KEY="$DATAPULSE_PLAN_SIGNING_KEY" -v datapulse-plan-data:/data datapulse-test:20260829`；随后执行 `curl --fail http://127.0.0.1:18000/api/health` 和 `curl --fail http://127.0.0.1:18000/studio`。
  - 前置条件：AUTO-E2E-004 镜像已生成；18000 未占用；仅使用临时 Docker volume。完成后执行 `docker rm -f datapulse-plan` 和 `docker volume rm datapulse-plan-data`。
  - 预期结果：entrypoint 先完成 Alembic head 迁移再启动 Uvicorn；健康检查返回 `status: ok` 和版本；`/studio` 返回静态入口；容器日志不包含 master key、signing key 或数据库密码；退出码为 0。
  - 失败后证据：容器日志、`curl -i` 响应头/体、`docker inspect` 和 `test-evidence/AUTO-E2E-005-final.log`，密钥字段已脱敏。
  - 实际结果：容器启动并完成 8 个 Alembic migration；`GET /api/health` 返回 200 和 `{"status":"ok","version":"0.1.0"}`；`GET /studio` 返回 200 静态入口；容器运行状态为 running，退出码 `0`。
  - 问题编号：无（ISS-005 已修复）

- AUTO-E2E-006：AI 面板高对比度样式与模板编译回归
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/web/src/features/ai/AiAnalysisPanel.vue`、`AiEditPanel.vue`、`AiScreenGeneratorDialog.vue` 的背景、辅助文字、错误、命令和结果徽章样式。
  - 执行命令：`pnpm test:e2e -- --grep "AI analysis uses one request|blank creation supports hand editing"`；`pnpm --filter @datapulse/web exec vitest run src/features/ai/ai-panel.test.ts src/features/ai/AiEditPanel.test.ts`；`pnpm --filter @datapulse/web build`
  - 前置条件：前端依赖已安装；使用 happy-dom/fake fetch，不连接真实 AI 服务；构建过程不使用真实 `.env` 或敏感数据。
  - 预期结果：AI 分析、AI 修改和 AI 生成模板可编译；浏览器计算样式确认面板为白色实底、说明文字为深色，状态提示、表单标签、占位、错误、命令和结果信息在浅色面板上可读；测试和构建退出码为 0。
  - 失败后证据：Vitest stdout/stderr、Vite 构建日志、视觉截图或 DOM 快照，保存到 `test-evidence/automation-20260829/AUTO-E2E-006.log`。
  - 实际结果：AI 分析/编辑 E2E 颜色断言通过；2 个 AI 单元测试文件、3 个测试通过；Web 构建退出码 `0`，仅有既有 Rollup 注释和大 chunk warning。
  - 问题编号：ISS-017（已修复）

## 人工黑盒测试

人工用例覆盖正常、边界、异常、权限/安全、失败恢复和数据一致性。不适用及原因：历史版本恢复不适用，产品明确没有版本历史/回滚；用发布前复制大屏进行人工备份，并验证发布失败/冲突时旧快照不变。

### BB-001：首次初始化、登录、会话恢复和退出

- 待执行
- 优先级：P0
- 前置条件：使用独立临时 `DATAPULSE_DATA_DIR` 和未初始化服务；准备一次性初始化码、专用管理员名 `admin`、不少于 10 位测试密码；确认前端和后端实际 URL。
- 操作步骤：
  1. 打开 `/api/health`，再访问 `/studio`；记录未初始化时的跳转。
  2. 分别提交空/错误/重复初始化码、短密码和两次不一致密码，记录页面错误和网络响应。
  3. 使用有效初始化码创建管理员，刷新页面，退出后再用正确密码登录。
  4. 登录失败数次，观察统一错误和限流；成功登录后再验证限流状态恢复。
  5. 点击退出并使用浏览器后退/刷新访问数据源页面。
- 预期结果：只有有效输入创建管理员并建立会话；未初始化进入 setup，已初始化匿名进入 login；错误凭据不区分用户名/密码，达到阈值限流；刷新保持当前会话，退出后不能通过后退恢复管理页数据。
- 需要记录的实际结果：每一步实际 URL、状态码、页面文本、Cookie 属性、限流次数和 `X-Request-ID`；不得记录密码/初始化码原文。
- 证据要求：步骤截图、Network HAR（敏感字段脱敏）、浏览器 console 和 `test-evidence/BB-001/` 录屏/日志。
- 清理动作：退出登录；停止本地服务；删除本次专用临时数据目录和浏览器上下文。

### BB-002：管理员权限、Origin 和 CSRF 保护

- 待执行
- 优先级：P0
- 前置条件：已初始化并登录；准备浏览器 DevTools 的 Network replay 或等价 HTTP 客户端；保留合法 CSRF cookie 仅用于对照。
- 操作步骤：
  1. 匿名访问 `/api/admin/datasources`、`/api/admin/datasets`、`/api/admin/screens` 和 `/studio/*`。
  2. 对一个 POST/PATCH/DELETE 请求分别移除 `Origin`、改成错误 Origin、移除 `X-CSRF-Token`、伪造 token 后重放。
  3. 对同一资源执行 GET，确认只读请求不被错误要求 CSRF；再以合法 Origin+CSRF 执行一次测试创建和删除。
- 预期结果：匿名管理接口返回 401 并不泄露数据；写请求的缺失/错误 Origin 或 CSRF 返回 403；合法请求可完成；错误请求不产生副作用。
- 需要记录的实际结果：请求方法/URL、状态码、错误 code、响应体是否含秘密、资源列表前后差异和 request ID。
- 证据要求：Network 请求/响应截图或 HAR、页面截图、数据库/资源数量对照，保存到 `test-evidence/BB-002/`。
- 清理动作：删除合法测试资源并退出登录；不修改其他用户或共享数据。

### BB-003：SQLite 数据源、Schema、只读查询和 SQL 数据集闭环

- 待执行
- 优先级：P0
- 前置条件：已登录；在专用 sources 目录放置从 `apps/web/e2e/fixtures/sales.sql` 导入的 `sales.db`；不要使用真实数据库。
- 操作步骤：
  1. 创建 SQLite 数据源，填写相对路径 `sales.db`，测试连接并打开 Schema。
  2. 展开 `sales` 关系，检查字段类型，再执行 `SELECT region, amount FROM sales WHERE amount >= :minimum` 并绑定 `minimum`。
  3. 保存为数据集，打开详情运行预览，比较 SQL 调试与数据集预览的列、行和参数结果。
  4. 修改数据集名称/最大行数/超时后保存并刷新详情。
- 预期结果：数据源可用，Schema 按需加载；参数绑定不改变 SQL 语义；数据集定义和最新预览一致；凭据和绝对路径不出现在页面、响应、URL 或浏览器存储。
- 需要记录的实际结果：实际行列、查询耗时/截断状态、保存 revision/更新时间、页面和 Network 中的敏感字段检查结果。
- 证据要求：连接状态、Schema、结果表、数据集详情截图，相关请求 HAR 和 console 日志存入 `test-evidence/BB-003/`。
- 清理动作：删除数据集和 SQLite 数据源；删除临时 `sales.db` 和浏览器下载/存储。

### BB-004：数据源凭据自动填充回归（DP-001）

- 待执行
- 优先级：P1
- 前置条件：Chromium 中仅保存测试管理员登录凭据；不要保存任何真实数据库凭据；已登录 Studio。
- 操作步骤：
  1. 打开“新建数据源”，切换 PostgreSQL、MySQL/MariaDB 和 HTTP API（若页面提供）。
  2. 观察数据库用户名、密码、token 字段的初始值和 `autocomplete` 语义；分别打开编辑已有数据源页面。
  3. 手动输入专用测试凭据并执行连接/保存，再重新打开编辑页。
- 预期结果：新建表单不会自动填入管理员凭据；编辑用户名按产品设计显示，密码不回显且留空保留原值；手动凭据仍可连接/保存；登录页仍可使用密码管理器。
- 需要记录的实际结果：各字段初始值、浏览器 autofill 提示、DOM `autocomplete` 值、请求 payload/响应中是否出现明文密码，以及 DP-001 是否仍可复现。
- 证据要求：字段截图、DevTools DOM/Network 截图（遮盖秘密）和复现录屏，保存到 `test-evidence/BB-004/`。
- 清理动作：删除测试数据源；清除浏览器中专用测试凭据和站点数据。

### BB-005：查询边界、危险 SQL、错误恢复和结果一致性

- 待执行
- 优先级：P0
- 前置条件：复用 BB-003 的专用 SQLite 数据源/数据集；打开 SQL 调试。
- 操作步骤：
  1. 运行合法 SELECT、空 SQL、语法错误、多语句、INSERT/UPDATE/DELETE/DDL 和 `load_extension`。
  2. 分别使用缺失参数、错误类型、重复参数、包含 SQL 注入片段的字符串。
  3. 将 `max_rows`、超时和并发操作设置到允许边界及越界值；在查询进行时再次运行或离开页面。
  4. 错误后重新运行原合法查询并与第一次结果比较。
- 预期结果：只有单条只读查询执行；越界、写操作、危险函数、参数错误和资源限制得到稳定错误；新请求不会被旧请求结果覆盖；错误后可恢复并得到相同数据，数据库无写入副作用。
- 需要记录的实际结果：每次输入、状态码/code、行数/截断、按钮 loading、最终结果、数据库行数前后对照和 request ID。
- 证据要求：关键错误与恢复截图、Network/console 日志、查询结果快照到 `test-evidence/BB-005/`；禁止保存真实 SQL 凭据。
- 清理动作：关闭调试页并删除测试数据集/数据源。

### BB-006：文件数据集格式、Sheet、坏文件和引用清理

- 待执行
- 优先级：P1
- 前置条件：已登录；使用 `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/e2e/fixtures/` 下 CSV、中文 CSV、Excel、JSON、Parquet；另备一个临时坏 JSON/超限文件。
- 操作步骤：
  1. 上传中文 CSV，确认中文预览，创建数据集并修改名称、最大行数、超时。
  2. 上传 Excel，切换两个 Sheet；再依次预览 JSON 和 Parquet。
  3. 上传坏 JSON、空文件、未知扩展名或超过配置大小限制的文件。
  4. 在数据集引用文件时尝试删除文件；删除数据集后再次删除文件。
- 预期结果：支持格式、中文和 Sheet 选择正确；坏/空/超限文件显示稳定错误且不生成半成品；被引用文件拒绝删除，解除引用后可删除；列表和预览不返回绝对文件路径。
- 需要记录的实际结果：每个文件的格式、Sheet、行列、错误 code、资源/数据集列表变化和清理结果。
- 证据要求：预览/错误/删除冲突截图、Network 响应和临时目录清单存入 `test-evidence/BB-006/`。
- 清理动作：删除所有测试数据集、文件资产和临时坏文件；清空浏览器上传缓存。

### BB-007：大屏创建、组件编辑、缩放、撤销重做和自动保存

- 待执行
- 优先级：P0
- 前置条件：已登录；已有可复用测试数据集和可选图片/GeoJSON 资产；桌面浏览器视口先设为 1920x1080，再准备 1200x900。
- 操作步骤：
  1. 在 `/studio/screens` 创建空白大屏或模板大屏，添加文本、图片、指标、表格、进度、折线、柱状、饼图和地图组件。
  2. 修改文本/主题/图表字段，拖拽和缩放组件到边界附近，切换网格/吸附，测试 0.5、1、1.5 缩放和多选对齐/分布。
  3. 锁定、隐藏、复制粘贴组件，检查图层 ID、数据绑定和层级；执行撤销/重做。
  4. 等待保存状态从未保存/保存中变为已保存，刷新并重新选择组件；在窄视口检查面板与画布是否遮挡。
- 预期结果：组件坐标始终在 1920x1080 逻辑画布内；锁定/隐藏生效；复制 ID 唯一且绑定不串联；每次编辑可正确撤销/重做；刷新恢复最后一次成功草稿，面板无核心内容重叠。
- 需要记录的实际结果：组件数量/ID、frame 坐标尺寸、保存状态和 PATCH revision、刷新前后文档差异、布局溢出/遮挡情况。
- 证据要求：各关键状态截图、操作录屏、Network 保存请求和 console 日志到 `test-evidence/BB-007/`。
- 清理动作：删除测试大屏和上传资产；关闭浏览器上下文。

### BB-008：草稿预览、明确发布、快照隔离和人工备份

- 待执行
- 优先级：P0
- 前置条件：已有一个可编辑大屏和数据集；准备两个明显不同的标题/图表内容。
- 操作步骤：
  1. 修改草稿为版本 A，打开草稿预览并记录标题/数据；确认预览不生成 display key。
  2. 点击发布后先取消确认，检查是否发送发布请求；再次打开确认并发布版本 A。
  3. 复制已发布大屏作为人工备份；将原大屏草稿改为版本 B，检查编辑器/预览与独立播放分别显示什么。
  4. 使用非法数据集/资产引用尝试发布，随后再次打开播放页。
- 预期结果：取消确认不发布；确认后 `published_document` 为 A；草稿改为 B 不影响线上 A；复制大屏是独立未发布备份；非法发布被阻止且已有 A 继续可播放。仓库没有直接 rollback，恢复只能通过人工备份复制后的大屏。
- 需要记录的实际结果：发布/复制请求次数、草稿与发布文档摘要、预览/播放标题、错误 code/request ID 和备份大屏 ID。
- 证据要求：确认弹窗、预览、播放、错误和备份列表截图，Network HAR/console 保存到 `test-evidence/BB-008/`。
- 清理动作：删除原屏、备份屏及资产；退出登录。

### BB-009：独立播放密钥交换、地址清理和轮换

- 待执行
- 优先级：P0
- 前置条件：已有已发布大屏；管理员会话和专用 signing key 可用；使用独立浏览器上下文作为播放端。
- 操作步骤：
  1. 在编辑器生成 display key，访问 `/play/{screenId}?key=...`，观察地址栏、Cookie、页面内容和查询请求。
  2. 在无 key、错误 key、错误 screen ID、未发布屏下访问播放页。
  3. 保留旧播放上下文，重新生成 key，再分别刷新旧会话和新 key 页面。
  4. 检查播放响应的 CSP、缓存、Cookie path/HttpOnly/SameSite/Secure（按环境）。
- 预期结果：首次交换后地址栏移除 key，后续只用受限 HttpOnly 播放会话；无效/未发布访问显示中性错误；轮换后旧 key/会话不能续期，新 key 可用；播放只读取已发布内容且不泄露敏感信息。
- 需要记录的实际结果：实际 URL、状态码、Cookie 属性、响应头、页面错误文案、旧/新 key 行为和 request ID（不记录 key 值）。
- 证据要求：地址栏与 Network/Cookie/响应头截图、失败录屏和 `test-evidence/BB-009/`。
- 清理动作：撤销/轮换测试 key；关闭播放上下文并删除测试大屏。

### BB-010：播放访问策略 Origin、IP 和 CIDR

- 待执行
- 优先级：P1
- 前置条件：已有已发布大屏；可通过编辑器访问策略面板或管理 API 设置策略；准备本机允许/不允许 Origin 和 `127.0.0.1/32`。
- 操作步骤：
  1. 使用空策略播放，确认不限制；再只配置允许 Origin、只配置允许 IP、同时配置两者。
  2. 从命中 Origin、命中 IP、两者都不命中和伪造 `X-Forwarded-For` 的请求访问播放交换接口。
  3. 输入非法 Origin、非法 IP/CIDR、带路径的 Origin，观察保存校验。
- 预期结果：Origin 或 IP 任一命中即可访问；都未命中拒绝；服务端不信任未明确配置的代理头；非法策略不保存；错误返回稳定 403/422 和 request ID。
- 需要记录的实际结果：策略保存响应、请求来源头、实际对端地址、状态码/code、播放是否建立和页面文案。
- 证据要求：策略表单/API、请求头、响应头和失败截图到 `test-evidence/BB-010/`。
- 清理动作：恢复空策略；撤销播放 key；删除测试屏。

### BB-011：安全嵌入 ticket、SDK 控制和多实例隔离

- 待执行
- 优先级：P0
- 前置条件：已有已发布大屏；准备宿主 Origin、Embed API Key、允许修改的 `region` 参数和不可修改的参数；使用仓库 `apps/web/e2e/embed-host.html` 或等价专用宿主页。
- 操作步骤：
  1. 轮换 Embed API Key，用正确 key 为指定 Origin/参数签发短期 ticket，在宿主页加载 `/embed/{screenId}`。
  2. 调用 `ready`、`setParameters`、`getParameters`、`refresh`、`fullscreen(false)` 和 `destroy`；再尝试修改 immutable 参数。
  3. 使用错误 API Key、错误 screen、错误 Origin、过期 ticket 和另一实例的消息/窗口重放。
  4. 检查 iframe URL 在启动后是否移除 ticket，Network 是否使用 Bearer 且 `credentials: omit`，Storage/日志是否含 ticket。
- 预期结果：合法嵌入 ready；只允许 mutable 参数；请求/回调按 instance_id/request_id 关联；destroy 后不再响应；错误访问被拒绝；ticket 不落 Cookie/Local Storage/Session Storage/后续 URL；响应有精确 CSP、no-store、no-referrer、nosniff。
- 需要记录的实际结果：宿主状态文本、各消息 payload（脱敏）、请求头/响应头、URL/Storage 检查、错误 code 和轮换前后 key 行为。
- 证据要求：宿主截图/录屏、Network HAR、Application 面板和 console 日志存入 `test-evidence/BB-011/`，不得保存 ticket/API key 原文。
- 清理动作：轮换并作废测试 API Key；关闭 iframe/宿主页；删除测试屏和资产。

### BB-012：保存失败、revision 冲突和网络恢复

- 待执行
- 优先级：P0
- 前置条件：同一已登录管理员在两个浏览器上下文打开同一大屏；准备 DevTools 网络阻断/恢复能力；保留专用测试文档。
- 操作步骤：
  1. 上下文 A 和 B 分别编辑不同文本；让 A 保存成功，再让 B 用旧 revision 自动保存。
  2. 在编辑器修改时短暂阻断后端或让 PATCH 返回 5xx，观察保存状态和是否自动重复覆盖。
  3. 恢复网络后重新加载最新草稿，确认用户能继续编辑并显式保存；检查重复点击不会生成多次资源/发布。
  4. 再打开预览和播放，比较草稿、发布快照与最终 API 数据。
- 预期结果：冲突显示明确提示并停止自动重试，不覆盖 A；网络恢复后通过重新加载回到最新成功版本；失败请求不产生半写入文档；草稿与发布快照边界保持一致。
- 需要记录的实际结果：两个上下文的 revision、保存状态、PATCH 次数/响应、恢复后的文档字段、预览/播放差异和 request ID。
- 证据要求：冲突/离线/恢复截图或录屏、HAR、console 和 API 文档快照到 `test-evidence/BB-012/`。
- 清理动作：关闭两个上下文；删除测试屏/数据集/资产；恢复网络设置。

### BB-013：容器部署启动和静态入口人工核验

- 待执行
- 优先级：P1
- 前置条件：已完成 AUTO-E2E-004；准备临时随机 master/signing key、18000 端口和独立 Docker volume；不使用生产 `.env`。
- 操作步骤：
  1. 用测试密钥启动 `datapulse-test:20260829`，等待 entrypoint 日志显示迁移后服务监听。
  2. 访问 `/api/health`、`/studio`，检查健康状态、版本和 SPA 入口。
  3. 停止并重新启动同一临时 volume，确认迁移不会破坏元数据；检查日志和环境变量展示。
- 预期结果：容器按迁移后再启动服务；健康检查和 Studio 可访问；重启保留仅测试数据；日志不出现 master/signing key、数据库密码或 API token。
- 需要记录的实际结果：容器 ID、启动耗时、日志关键行、健康响应、HTTP 响应头、重启前后资源摘要。
- 证据要求：脱敏容器日志、`curl -i` 输出、浏览器截图和 `test-evidence/BB-013/`。
- 清理动作：`docker rm -f` 测试容器并删除专用 volume、临时密钥文件和浏览器数据。

### BB-014：跨视口、键盘和播放器视觉可用性

- 待执行
- 优先级：P2
- 前置条件：已有已发布九组件大屏；Chromium 可用，并尽可能准备 Firefox/WebKit 进行人工补充；准备 1920x1080、1200x900 和窄窗口。
- 操作步骤：
  1. 在各视口打开编辑器、草稿预览和独立播放，检查画布比例、留白、面板边界和文本溢出。
  2. 使用 Tab/Enter/Escape 完成登录、弹窗确认、工具栏和删除确认；检查焦点和禁用/加载状态。
  3. 在播放页观察组件查询中、成功、单组件失败和手动刷新状态。
- 预期结果：播放器保持 16:9 contain；编辑器窄窗口不遮挡核心画布；主要流程可键盘操作，焦点可见；单组件错误不破坏其他组件；无新增明显溢出、空白或遮挡。
- 需要记录的实际结果：浏览器/版本/视口、键盘路径、焦点顺序、截图差异、console 错误和布局问题。
- 证据要求：各视口截图/录屏、可访问性检查清单和 console 日志到 `test-evidence/BB-014/`。
- 清理动作：关闭所有浏览器上下文；删除测试屏/数据和临时截图中的敏感信息。

### BB-015：AI 未配置时的降级提示和数据集入口

- 待执行
- 优先级：P1
- 前置条件：独立临时数据目录；仅使用专用测试管理员、临时 SQLite 数据源和数据集；服务设置 `DATAPULSE_AI_ENABLED=false`，不连接真实 AI provider。
- 操作步骤：
  1. 登录后打开大屏列表，打开“AI 生成大屏”，选择临时数据集并提交生成请求。
  2. 打开临时数据集详情的“AI 分析”面板，提交一个普通分析问题。
  3. 对照调用 `/api/admin/ai/status` 的状态与页面错误文案，记录请求 ID、状态码和是否提供人工 SQL 入口。
- 预期结果：AI 未配置时不向外部 provider 发请求；接口返回稳定的未配置错误；页面明确显示配置状态、可恢复提示和人工 SQL 路径，不创建草稿或发布请求。
- 需要记录的实际结果：AI status、分析/生成响应状态及错误 code、页面提示、草稿/发布请求数量和资源列表前后差异。
- 证据要求：页面截图、脱敏 Network 响应和 console 日志保存到 `test-evidence/blackbox-20260829/BB-015/`。
- 清理动作：删除临时数据集/数据源，退出登录，停止隔离服务并删除临时数据目录。

### BB-016：管理员修改密码、旧会话撤销和恢复登录

- 待执行
- 优先级：P1
- 前置条件：独立临时服务；两个浏览器上下文使用专用管理员账号登录；新旧密码均为一次性测试值，不写入证据。
- 操作步骤：
  1. 在上下文 A 使用错误当前密码、短新密码分别调用修改密码流程，记录校验错误。
  2. 在上下文 A 使用正确当前密码修改为新密码；上下文 B 保留旧会话并请求管理接口。
  3. 检查 A 是否保持会话、B 是否被撤销；分别使用旧密码和新密码重新登录。
- 预期结果：短密码/错误当前密码被稳定拒绝；修改成功后当前会话保持有效，其他会话失效；旧密码不能登录，新密码可以登录；无密码明文进入响应或日志。
- 需要记录的实际结果：各状态码、错误 code/request ID、两个会话的 `/api/auth/session` 状态和新旧密码登录状态。
- 证据要求：脱敏响应 JSON、会话状态和页面截图保存到 `test-evidence/blackbox-20260829/BB-016/`。
- 清理动作：退出所有上下文，销毁隔离服务和临时数据目录；不影响主测试服务账号。

### BB-017：查询行数/超时边界和参数错误恢复

- 待执行
- 优先级：P0
- 前置条件：独立临时 SQLite `sales.db`、数据源和 SQL 数据集；已登录并取得合法 CSRF；不使用真实数据。
- 操作步骤：
  1. 创建数据集和直接查询时分别提交 `max_rows`、`timeout_seconds` 的最小值、最大值及越界值。
  2. 提交同时包含 SQL 和 REST query 的数据集定义，提交缺失/额外参数的查询。
  3. 运行 `max_rows=2` 的合法查询，随后运行合法参数查询，比较行数、`truncated` 和结果内容。
  4. 对空更新请求执行 PATCH，观察是否产生部分修改。
- 预期结果：越界输入返回稳定 422 和字段错误；只接受一种查询定义；缺失/额外参数被拒绝；行数上限和截断标志一致；错误后合法查询可恢复且数据无副作用。
- 需要记录的实际结果：请求/响应状态、错误 code、request ID、返回行数/截断、恢复查询结果和资源列表差异。
- 证据要求：脱敏响应 JSON、数据库/资源摘要保存到 `test-evidence/blackbox-20260829/BB-017/`。
- 清理动作：删除测试数据集和数据源，删除临时 SQLite 文件并退出登录。

### BB-018：真实 Chromium 数据源凭据字段与 DP-001 补充观察

- 待执行
- 优先级：P1
- 前置条件：Chromium 可用；只使用专用测试管理员；不保存真实数据库/API 凭据，不使用真实密码管理器账号。
- 操作步骤：
  1. 打开新建数据源，依次切换 PostgreSQL、MySQL/MariaDB 和 HTTP API Bearer 认证。
  2. 在字段首次出现后读取用户名、密码/token 的实际值和 `autocomplete` 属性并截图。
  3. 打开编辑已有数据源，确认已保存密码不回显；记录无法模拟真实密码管理器 autofill 的原因。
- 预期结果：新建凭据字段为空且不会带入管理员密码；密码字段使用新凭据语义；编辑密码为空并保留原值；任何明文凭据不进入 URL、Storage 或响应。
- 需要记录的实际结果：浏览器版本、字段初始值、DOM 属性、页面/Network/Storage 检查和 autofill 是否实际触发。
- 证据要求：字段快照、脱敏 DOM/Network 记录保存到 `test-evidence/blackbox-20260829/BB-018/`。
- 清理动作：关闭浏览器上下文，清除专用站点数据和测试凭据；不保存真实秘密。

### BB-019：后端中断时登录错误显示和服务恢复

- 待执行
- 优先级：P1
- 前置条件：前端 `5173` 保持运行；后端 `8001` 可在测试窗口内停止/恢复；仅使用临时测试账号，不记录密码。
- 操作步骤：
  1. 在后端不可达时打开登录页，提交页面已填入的测试账号，记录 Network、console 和页面提示。
  2. 恢复后端，检查 `/api/health`、`/api/auth/status` 和前端页面是否恢复；再次提交登录。
  3. 对照后端访问日志，确认中断期间是否产生登录 API 请求以及恢复后的状态。
- 预期结果：后端不可达时页面明确提示网络不可用并提供重试方向，不把连接失败误报为账号密码错误；恢复后健康检查和登录流程可继续；中断期间不产生伪造的登录失败 request ID。
- 需要记录的实际结果：前后端进程状态、健康响应、页面文案、Network/console 错误、后端是否收到请求和恢复后的登录状态。
- 证据要求：用户截图、脱敏 Network/console 摘要、服务启动日志和健康检查响应保存到 `test-evidence/blackbox-20260829/BB-019/`。
- 清理动作：保持主测试服务运行；关闭临时浏览器上下文；不删除主测试数据或用户浏览器凭据。

### BB-020：无效凭据登录错误文案本地化观察

- 待执行
- 优先级：P2
- 前置条件：前端 `5173` 和后端 `8001` 正常运行；仅使用一次性无效测试账号/密码，不记录任何真实凭据。
- 操作步骤：
  1. 打开 `/studio/login`，提交明确无效的测试凭据。
  2. 记录 Network 响应的状态码、错误 code、页面实际文案和 request ID。
  3. 对照登录页整体语言、设计文档和错误提示约定，判断是否存在未本地化文本。
- 预期结果：无效凭据返回 401 和统一错误 code；登录页以当前界面语言显示可理解的错误，不泄露用户名/密码哪一项错误；request ID 可用于排查。
- 需要记录的实际结果：状态码、错误 code、页面文案、语言一致性和截图；密码字段只记录“已填充/已遮罩”，不记录值。
- 证据要求：用户截图、脱敏 Network 响应和页面截图保存到 `test-evidence/blackbox-20260829/BB-020/`。
- 清理动作：清空输入框、关闭浏览器上下文；不保存测试凭据。

### BB-021：重启隔离实例后管理员初始化和登录

- 待执行
- 优先级：P0
- 前置条件：使用新的临时 `DATAPULSE_DATA_DIR`；先执行仓库迁移命令；后端监听 `8001`，前端监听 `5173` 并通过 `VITE_API_PROXY_TARGET` 指向后端；只使用本轮输出的合成测试账号，不使用真实凭据。
- 操作步骤：
  1. 启动后端和前端，确认启动输出包含测试账号信息但不包含生产密钥、Cookie 或 token。
  2. 在新的 Chromium 上下文打开 `/studio/login`，先提交明确无效密码，记录状态码、错误 code 和页面文案。
  3. 使用启动输出的合成测试密码重新提交，确认进入 `/studio/datasources`，并检查会话接口。
- 预期结果：迁移完成后服务可启动；首次 setup 只创建一个管理员；无效凭据返回 401 且不泄露具体字段；正确测试凭据登录返回 204，建立会话并进入数据源页面；测试数据与会话仅存在临时目录。
- 需要记录的实际结果：迁移命令退出码、服务端口、`/api/auth/status`、setup、login、session 状态码、最终 URL、页面标题/关键 heading、Network/console 摘要；密码只记录为“已输入”，不记录值。
- 证据要求：启动日志（密钥脱敏）、API 状态摘要、无效/成功登录截图和页面级观察保存到 `test-evidence/blackbox-20260829/BB-021/`。
- 清理动作：关闭 Chromium 上下文；停止本轮新实例；删除或保留带明确前缀的临时目录由测试收尾决定，不触碰用户数据。

## 测试数据与清理

- 管理员：只使用测试环境专用 `admin` 和随机不少于 10 位密码；测试结束删除浏览器密码管理器条目。
- 数据库：SQLite 只使用 `apps/web/e2e/fixtures/sales.sql` 生成的临时 `sales.db`；PostgreSQL/MariaDB 只使用 `compose.test.yaml` 的 `datapulse` 测试账号和端口。
- 文件：使用仓库固定 CSV/中文 CSV/Excel/JSON/Parquet fixtures；坏文件、超限文件和资产均生成在临时目录，禁止真实客户文件。
- 屏幕/资产：每个执行者使用唯一名称或独立数据目录，组件绑定只指向测试数据；删除前记录 ID 以便确认没有误删其他数据。
- 密钥：master key、signing key、display key、Embed API Key、ticket 均为一次性测试值；证据、日志、URL、HAR、截图和 Storage 导出中必须脱敏或删除。
- AI：自动化使用 `tools/run_e2e.py` 启动的本地 fake 服务；不配置真实 provider，不发送敏感上下文。
- E2E 清理：由 `.e2e` 配置和 `global-teardown.ts` 清理本次临时目录；若进程中断，先核对目录名带 `datapulse-e2e-` 和配置文件位于 `.e2e/` 后再清理。
- 外部数据库清理：测试后执行 `docker compose -f compose.test.yaml down -v`；确认不删除非 `datapulse-test` 项目资源。
- Docker 清理：删除 `datapulse-plan` 容器、`datapulse-plan-data` 卷和 `datapulse-test:20260829` 测试镜像（如不再需要）；不得删除其他镜像、卷或用户目录。

## 通过标准

- 计划执行范围内的 P0 自动化命令和 P0 人工用例均已实际执行并留存证据；未执行项不得标记为完成或通过。
- 自动化命令退出码为 0；外部数据库若不可用必须标记阻塞并保留 Compose/DSN 证据，不得以跳过替代通过。
- P0 主链路数据一致：数据源/数据集定义、草稿、预览、发布快照、播放和嵌入读取的文档/查询结果符合各自边界；发布失败或 revision 冲突不得破坏已有发布快照。
- 安全标准满足：管理写接口要求有效 Session、Origin 和 CSRF；播放/嵌入凭证绑定 screen/Origin/参数且轮换可撤销；密码、token、绝对路径和堆栈不出现在不应出现的页面、响应、URL、Cookie、Storage、日志或模型上下文。
- E2E、视觉快照、契约、类型、Web 构建和 Docker 构建无未分类失败；视觉差异必须有人工判定和截图记录。
- 仓库没有配置覆盖率阈值，本计划不虚构覆盖率百分比；如 CI 另有门禁，以 CI 配置为准并记录差异。

## 风险、阻塞和文档不一致

- 风险：用户请求中的项目名称、路径、分支和目标仍是占位符；本计划按当前 DataPulse 仓库、HEAD 提交和 README P0 流程推定，执行前需确认是否需要缩小/替换目标。
- 风险：Playwright 配置只有 Chromium；`docs/TESTING_SESSION.md` 要求 WebKit/Firefox 回归，但仓库没有对应 project，跨浏览器只能人工补充，不能假设自动覆盖。
- 阻塞：PostgreSQL/MariaDB 集成测试依赖 Docker daemon、端口和镜像；AI 人工测试依赖本地 fake/兼容服务，默认 AI 关闭；任一缺失都要标记未执行/阻塞并记录原因。
- 阻塞：Docker 构建需要网络拉取 Node/Python 基础镜像和依赖；失败时区分网络/资源环境问题与构建错误。
- 产品边界：代码没有历史版本、rollback 或直接 restore API；不可把复制大屏或草稿刷新恢复描述成版本回滚。
- 文档不一致（已处理）：生成计划时 `docs/known-issues.md` 将 DP-001 列为“待统一修复”，而 `docs/TESTING_SESSION.md` 写成“暂无”；本轮已将两份文档更新为 DP-001 代码已解决、真实 autofill 仍是残余覆盖缺口。
- 文档不一致：`docs/TESTING_SESSION.md` 的测试数据列出 `sales.db`，仓库实际提交的是 `apps/web/e2e/fixtures/sales.sql`，`tools/prepare_e2e.py` 运行时才在临时目录生成 `sales.db`；人工测试不得直接假设仓库根目录存在该数据库。
- 文档不一致：`docs/TESTING_SESSION.md` 记录上次后端使用 8001 端口且服务保持运行，但当前检查未确认有 8000/8001/5173 监听；该记录是历史会话，执行本计划需重新启动并记录实际端口。
- 文档不一致：README 要求 Docker `.env` 同时填写 `DATAPULSE_MASTER_KEY` 与 `DATAPULSE_SIGNING_KEY`，但当前 `compose.yaml` 只把 `DATAPULSE_MASTER_KEY` 传入容器；生产嵌入签名配置需在执行容器测试前人工确认，不能假设 compose 已传递 signing key。
- 文档不一致：多个 `docs/superpowers/plans/*.md` 仍保留未勾选实施步骤，而对应源代码和测试文件已存在；这些 checkbox 不能作为当前实现状态或测试结果依据。
- 工作区风险：`.codegraph/`、`.idea/`、`docs/TESTING_SESSION.md` 为未跟踪文件；执行者不得用清理命令删除或覆盖它们。

## 测试执行结果

- 执行日期：2026-08-29；分支/提交：`main` / `a0e973e0475f01dd735dcae6448fd56fcc562ae`。
- 2026-08-30 登录恢复跟进：仅在隔离测试数据库中重置合成 `admin` 凭据并撤销旧会话；通过前端页面提交后进入 `/studio/datasources`，未修改业务代码、测试代码或项目配置。
- 2026-08-31 黑盒续测：新建隔离实例并完成迁移、setup、无效密码和正确密码登录验证；BB-021 结果已写入执行记录和证据目录。
- 2026-08-31 黑盒续测：复核“数据集 → 从数据源创建 → SQL 调试 → 保存为数据集”闭环；BB-022 在正确入口完成查询、创建和详情跳转，未复现产品故障；用户确认此前是找错入口。
- 自动化：20 个用例状态为“通过”，0 个用例状态为“阻塞”；没有自动化断言失败。
- 单元测试：AUTO-UNIT-001 至 AUTO-UNIT-007 均通过；前端定向复核分别为 30 和 59 个测试通过。
- 集成/API：AUTO-INT-001 至 AUTO-INT-007 均通过；AUTO-INT-006 在 OrbStack 恢复后为 13 passed、12 deselected。
- E2E/构建：AUTO-E2E-001 至 AUTO-E2E-006 均通过；AUTO-E2E-004 使用镜像源预拉取基础镜像后构建成功，AUTO-E2E-005 容器迁移、健康检查和静态入口均成功，AUTO-E2E-006 验证 AI 面板高对比度样式。
- 人工黑盒：BB-001 至 BB-022 已进行本轮实际操作；BB-004/BB-018 仍缺少真实密码管理器 autofill 验证，BB-005/007/011 的部分高成本边界仍需后续人工补充；BB-019 的服务中断、代理和隔离测试账号恢复已完成环境核对；BB-020 的无效凭据响应和中文界面英文错误文案已实际观察；BB-022 的数据源到数据集闭环已实际观察。
- 本轮总体结论：自动化原始用例和修复后回归均未失败；ISS-006～ISS-015 已在用户授权后完成代码修复和自动化复测，ISS-017（AI 面板文字对比度）已完成样式修复和 Web 回归。仍保留 ISS-004 的真实密码管理器覆盖缺口、Firefox/WebKit 人工覆盖、部分高成本边界，以及外部测试数据质量风险 ISS-016；不据此给出最终发布结论。
- 2026-08-31 修复复测：播放 401 隔离、窄视口编辑器、发布 Escape、坏 JSON 错误/清理、AI 未配置降级、登录错误映射、数据源空状态入口和测试连接功能对应测试均通过；详细命令和证据见 `test-evidence/automation-20260829/ISSUES.md`。
- 2026-08-31 视觉修复复测：AI 分析/修改面板和 AI 生成弹窗辅助文字改为高对比度浅色主题；Chromium AI 分析/编辑 E2E 计算样式断言 2/2 通过，AI 定向 Vitest 2 files/3 tests passed，Web 构建退出码 0。
- 证据目录：`test-evidence/automation-20260829/`、`test-evidence/blackbox-20260829/`；问题明细：`test-evidence/automation-20260829/ISSUES.md`。

## 黑盒人工测试执行记录

以下记录只描述本轮已经进行的操作和观察；未覆盖的真实浏览器/外部服务条件仍保持待执行，不将观察写成通过结论。
原始 BB 用例条目中的“待执行”状态按计划模板保留；本节是本轮实际操作记录，问题状态以“发现的问题”和问题索引为准。

| 编号 | 状态 | 本轮实际观察 | 证据 | 问题/缺口 |
| --- | --- | --- | --- | --- |
| BB-001 | 已执行 | 未初始化进入 setup；短密码被浏览器原生校验拦截；不一致密码显示错误；有效初始化后刷新保持会话，退出后回到登录；错误登录第 6 次返回 429，重启后正确登录恢复。 | `test-evidence/blackbox-20260829/BB-001/` | 无；真实密码管理器未纳入本条 |
| BB-002 | 已执行 | 匿名 datasources/datasets/screens 均 401；缺失/错误 Origin 或 CSRF 的写请求均 403；合法 GET 200 且无副作用。 | `BB-002-anon-studio.png` 及本轮 Network 摘要 | 无 |
| BB-003 | 已执行 | SQLite 创建和连接测试显示可用；Schema 显示 sales；只读查询返回 4 行并可保存/预览数据集；DELETE 被 `QUERY_NOT_READ_ONLY` 拒绝。 | `test-evidence/blackbox-20260829/BB-003/` | 参数最小/最大边界未在本轮逐项操作 |
| BB-004 | 部分执行 | PostgreSQL、MySQL/MariaDB 新建表单用户名/密码为空，均为 `autocomplete=new-password`；HTTP API token 字段按页面真实控件检查；SQLite 编辑密码不回显。 | `test-evidence/blackbox-20260829/BB-004/` | ISS-004：没有安全可用的真实密码管理器 autofill 环境 |
| BB-005 | 已执行（核心边界） | 空 SQL、语法错误、多语句、INSERT 均 422 并返回稳定错误码；合法查询错误后可恢复并返回 4 行。 | `test-evidence/blackbox-20260829/BB-005/` | 并发/超时/最大行数和参数类型矩阵未逐项人工操作 |
| BB-006 | 已执行 | 中文 CSV、Excel 两个 Sheet、JSON、Parquet 均可预览；引用文件删除返回 409，解除引用后可删除；坏 JSON 返回 500 且遗留未登记文件，空/未知格式返回 422。 | `test-evidence/blackbox-20260829/BB-006/` | ISS-009、ISS-010 |
| BB-007 | 已执行（核心编辑） | 创建大屏并添加 8 类组件；编辑文本后显示已保存，刷新恢复组件和文本；发布前无效引用被阻止。 | `test-evidence/blackbox-20260829/BB-007/` | 多选几何/锁定隐藏的逐项人工操作未完整执行；窄视口问题见 BB-014/ISS-007 |
| BB-008 | 已执行 | 发布确认取消未发送请求；合法文本屏确认发布成功；草稿 B 预览显示 B，独立播放仍显示已发布 A；复制生成独立副本。 | `test-evidence/blackbox-20260829/BB-008/` | 无；无效引用发布阻止同时作为失败恢复证据 |
| BB-009 | 已执行 | 有效 display key 播放后 URL 去除 key 并显示已发布内容；无 key/错误 key 的播放器请求 401 后跳转 `/studio/login`；错误 screen 显示中性错误。 | `test-evidence/blackbox-20260829/BB-009/` | ISS-006；播放 Cookie/响应头已记录，未保存明文 key |
| BB-010 | 已执行 | Origin 命中 204、未命中 403；CIDR 命中 IP 可访问；伪造 `X-Forwarded-For` 未绕过；非法带路径 Origin 返回 422；策略已恢复为空。 | `test-evidence/blackbox-20260829/BB-010/` | 无 |
| BB-011 | 已执行（开发环境） | Embed host ready、可变参数、刷新、全屏和 immutable 参数拒绝均观察到；过期 ticket 中性错误，旧 API key 轮换后 401；响应含 no-store/CSP/no-referrer/nosniff。 | `test-evidence/blackbox-20260829/BB-011/` | Vite 开发外壳的跨 Origin/CSP 与生产静态入口差异仍需部署环境确认 |
| BB-012 | 已执行 | 双上下文旧 revision 保存返回 409 并显示草稿冲突；强制 500 显示保存失败；恢复网络刷新回到 A 版本，无覆盖。 | `test-evidence/blackbox-20260829/BB-012/` | 无 |
| BB-013 | 已执行 | 重新构建镜像；容器完成 8 个迁移，health/studio GET 200；同一 volume 重启后仍正常，退出码 0；临时容器/volume/镜像已清理。 | `test-evidence/blackbox-20260829/BB-013/` | 无 |
| BB-014 | 已执行 | 播放页 1920x1080、1200x900、390x844 均保持 16:9、无滚动；编辑器 390px 宽出现 `scrollWidth=1200`；发布弹窗 Escape 不关闭。 | `test-evidence/blackbox-20260829/BB-014/` | ISS-007、ISS-008；Firefox/WebKit 未配置/未执行 |
| BB-015 | 已执行观察 | 在独立 AI 关闭实例中，`GET /api/admin/ai/status` 返回 `unconfigured`；大屏生成和数据集分析均返回 503/`AI_NOT_CONFIGURED`，页面仅显示通用错误和 request ID，没有配置状态或人工 SQL 入口。 | `test-evidence/blackbox-20260829/BB-015/` | ISS-011 |
| BB-016 | 已执行观察 | 短新密码返回 422；错误当前密码返回 401；修改密码返回 204；当前会话仍为 200，另一会话为 401；旧密码登录 401，新密码登录 204。 | `test-evidence/blackbox-20260829/BB-016/` | 无 |
| BB-017 | 已执行观察 | `max_rows` 0/5001、`timeout_seconds` 0/301、SQL/query 同时存在和空更新均返回 422；合法行数上限返回 2 行且 `truncated=true`；缺失/额外参数返回稳定 422；合法参数查询返回 2 行且不截断。 | `test-evidence/blackbox-20260829/BB-017/` | 无 |
| BB-018 | 已执行观察 | 真实 Chromium 新建 PostgreSQL、MySQL/MariaDB、HTTP API Bearer 表单的凭据字段均为空；用户名/密码/token 字段均使用 `autocomplete=new-password`；本隔离上下文未配置密码管理器，未能触发真实 autofill。 | `test-evidence/blackbox-20260829/BB-018/` | ISS-004 残余覆盖缺口 |
| BB-019 | 已执行（恢复跟进） | 用户截图对应的初始核对时前端仍在 `5173`，后端 `8001` 不可达；后端恢复后，原 Vite 进程仍将 `/api` 代理到默认的未监听 `8000`，导致 `/api/auth/status` 返回 500，因此没有产生登录 API 请求，页面显示通用“登录失败，请稍后重试”。随后重启 Vite 并显式代理到 `8001`，前端初始化请求恢复为 200；为解除隔离测试账号的浏览器自动填充密码不一致，又备份数据库、重置合成 `admin` 密码并撤销旧会话，通过前端代理登录返回 204，会话查询返回 200；独立 Chromium 页面最终进入 `/studio/datasources`。 | `test-evidence/blackbox-20260829/BB-019/BB-019-login-after-reset.txt`、`BB-019-login-after-reset.png`、用户提供截图 | ISS-012；修复后的网络错误映射已纳入代码回归，默认部署代理仍需确认 |
| BB-020 | 已执行观察 | 2026-08-30 用户截图中后端请求已返回 401 `AUTH_INVALID_CREDENTIALS`；页面显示英文 `The username or password is invalid.`，与中文登录界面语言不一致。该响应只表明提交凭据无效，不能证明正确密码是什么。 | `test-evidence/blackbox-20260829/BB-020/BB-020-user-login-401.png`、`BB-019-after-recovery-invalid-credentials.json` | ISS-013 |
| BB-021 | 已执行观察 | 新隔离目录先执行 0001 至 0008 迁移后启动成功；setup 返回 201、无效密码登录返回 401（页面显示已知英文错误文案）、正确测试密码登录返回 204，最终进入 `/studio/datasources` 且“数据源”标题存在，会话返回 200。未发现新的业务缺陷；ISS-013 在无效凭据场景继续复现。 | `test-evidence/blackbox-20260829/BB-021/BB-021-auth-restart-observation.txt`、`BB-021-login-invalid.png`、`BB-021-login-success.png` | ISS-013；无新增问题 |
| BB-022 | 已执行观察 | 从“数据集”页点击“从数据源创建”后进入数据源详情；详情、Schema 接口均返回 200；SQL 调试执行 `SELECT 1 AS health_check` 和 ERP `products` 查询均返回 200，保存数据集请求均返回 201 并跳转详情页。两个临时数据集已删除，列表无 `BB-022-*` 残留。用户确认此前是找错入口，未复现“无法从数据源创建数据集”；ERP 返回的中文字段存在双重编码形态，待数据源所有者确认。 | `test-evidence/blackbox-20260829/BB-022/BB-022-observation.txt`、`BB-022-detail-loaded.png`、`BB-022-save-result.png`、`BB-022-products-save.png` | ISS-016（外部测试数据质量待确认）；一次 CSS 选择器脚本错误属于测试脚本定位问题 |

## 发现的问题

### ISS-001：首次执行时 Docker daemon 不可用

- 类型：环境阻塞；优先级：P0；状态：已解除并完成重试。
- 现象：首次执行时 Docker CLI 无法连接 OrbStack socket，阻塞 Compose、Docker build 和容器测试。
- 处理结果：启动 OrbStack 后 Compose 服务恢复，AUTO-INT-006 重试通过；相关首次失败证据保留在 `AUTO-INT-006-compose-up.log` 和 `AUTO-INT-006.log`。

### ISS-002：前端定向 Vitest 命令参数传递导致全量执行

- 类型：测试基础设施；优先级：P1；状态：已修复，修正命令已复核通过。
- 现象：计划命令 `pnpm --filter @datapulse/web test -- <file list>` 实际运行 28 files/122 tests，而非指定文件集合。
- 处理结果：使用 `pnpm --filter @datapulse/web exec vitest run <file list>` 复核通过；原始和修正日志分别为 `AUTO-UNIT-005.log`、`AUTO-UNIT-006.log`、`AUTO-UNIT-005-targeted.log`、`AUTO-UNIT-006-targeted.log`。

### ISS-003：计划生成说明在执行后过时

- 类型：文档追踪；优先级：P1；状态：已修复。
- 现象：原生成说明写着“没有执行本计划中的测试套件”，与本节实际执行结果不一致。
- 处理结果：当前文件已补充本节执行记录；用例状态和证据以本节及日志为准。

### ISS-004：DP-001 未完成真实密码管理器验证

- 类型：测试覆盖缺口；优先级：P1；状态：代码已修复并完成自动化验证，真实密码管理器行为待人工抽样确认。
- 原因：自动化 happy-dom 只能检查 `autocomplete` 属性和字段初始值，不能模拟真实浏览器密码管理器 autofill。
- 处理结果：`DatasourceFormView.vue` 已使用 `autocomplete="off"` 和 `autocomplete="new-password"`；AUTO-UNIT-005 定向集合 30 tests passed；`docs/known-issues.md` 已移至“已解决”。
- 后续动作：如需完全关闭覆盖缺口，执行 BB-004，使用专用 Chromium 测试凭据完成新建/编辑数据源表单验证；不得使用真实凭据。

### ISS-005：Docker 构建 runtime 基础镜像下载阻塞

- 类型：环境阻塞；优先级：P0；状态：已修复并完成重试。
- 现象：首次构建时 `python:3.13-slim` 层下载长时间低速，构建进程中止并返回退出码 `130`。
- 影响：首次阻塞 AUTO-E2E-004，并连带阻塞 AUTO-E2E-005。
- 证据：`AUTO-E2E-004-rerun.log`（首次阻塞）；`AUTO-E2E-004-final.log`、`AUTO-E2E-004-image-inspect.log`、`AUTO-E2E-005-final.log`（修复后）。
- 处理结果：从 `docker.m.daocloud.io/library/python:3.13-slim` 预拉取并复用同 digest 基础镜像后，原始 Dockerfile 构建和容器烟囱测试均退出码 `0`。

### ISS-006：无 key 或错误 key 的独立播放跳转管理员登录

- 类型：产品行为/安全 UX；优先级：P1；状态：已修复并完成自动化回归。
- 现象：在隔离的未登录播放上下文访问 `/play/{screen_id}` 或使用错误 display key，播放器 API 返回 401 后页面被导航到 `/studio/login`，没有显示播放器模板中的中性“无法播放此大屏”错误。
- 复现：使用 BB-009 的已发布文本屏；分别打开无 key、`key=invalid`，观察最终 URL 和页面文案。
- 影响：公开播放链接失效时暴露管理员登录入口，用户无法得到预期的播放错误提示；有效 key 播放不受影响。
- 证据：`test-evidence/blackbox-20260829/BB-009/BB-009-player-no-key.png`、`BB-009-player-wrong-key.png`；对应 401 request ID 见本轮 Network 摘要。
- 处理结果：独立播放请求设置 `suppressAuthExpiredEvent`，401 不再触发 Studio 全局登录跳转；播放器保留中性错误。AUTO-UNIT-006 定向集合和 AUTO-E2E-001 均已复测。

### ISS-007：编辑器窄视口产生水平溢出

- 类型：产品可用性/布局；优先级：P2；状态：已修复并完成自动化回归。
- 现象：390×844 视口打开编辑器时 `document.documentElement.scrollWidth=1200`、`clientWidth=390`，组件与图层、画布、属性面板横向排列在屏外，核心画布不能直接操作。
- 复现：登录后打开 BB-014 的大屏编辑器，将 Chromium viewport 设为 390×844，检查页面滚动宽度和面板位置。
- 影响：窄窗口或移动尺寸的编辑工作流不可用；播放页本身在同一视口无溢出。
- 证据：`test-evidence/blackbox-20260829/BB-014/BB-014-editor-narrow.png`，记录的面板 x 坐标和 scrollWidth 见本轮执行输出。
- 处理结果：编辑器路由增加窄视口布局标记；600px 以下隐藏侧栏、纵向排列面板并取消固定 1200px 最小宽度。窄视口 E2E 已复测通过。

### ISS-008：发布确认弹窗不响应 Escape

- 类型：产品可用性/键盘交互；优先级：P2；状态：已修复并完成自动化回归。
- 现象：打开“确认发布大屏”弹窗后按 Escape，弹窗仍存在（前后 heading count 均为 1）。
- 复现：已登录编辑器点击“发布”，按 Escape，观察弹窗和焦点。
- 影响：键盘用户无法按预期取消高风险发布确认；仍可点击“取消”。
- 证据：`test-evidence/blackbox-20260829/BB-014/BB-014-publish-dialog-escape.png`。
- 处理结果：编辑器注册并清理 Escape 监听，发布进行中禁止关闭，其他状态可关闭弹窗；Escape E2E 已复测通过，焦点返回仍需人工补充。

### ISS-009：坏 JSON 上传未映射稳定错误

- 类型：后端异常处理/错误契约；优先级：P1；状态：已修复并完成自动化回归。
- 现象：上传内容为 `{bad json` 的 `.json` 文件时 `POST /api/admin/files` 返回 500，响应没有 `X-Request-ID`；页面只显示“请求失败，请稍后重试”，后端日志出现未处理 `JSONDecodeError`。
- 复现：登录后打开文件数据集导入，上传 BB-006 的坏 JSON fixture，记录 Network 和页面错误。
- 影响：用户无法定位输入错误；500 堆栈污染服务日志，违反稳定错误 envelope/request ID 约定。
- 证据：`test-evidence/blackbox-20260829/BB-006/BB-006-bad-json.png`、`bad-json-response.txt`；后端启动会话日志中的脱敏 traceback。
- 处理结果：JSONDecodeError/UnicodeDecodeError 映射为 `FILE_PARSE_INVALID`，上传返回 422 并保留 request ID；后端 API 回归已通过。

### ISS-010：坏 JSON 上传失败后遗留未登记存储文件

- 类型：数据一致性/失败清理；优先级：P1；状态：已修复并完成自动化回归。
- 现象：ISS-009 的 500 响应后，临时 `files/<orphan-id>/source.json` 存在，但 `file_asset` 表没有对应 id；文件列表不显示该资产。
- 复现：同 ISS-009 上传坏 JSON，检查临时 files 目录和 `file_asset` 元数据表。
- 影响：磁盘产生不可管理的孤儿文件，重复失败上传可能持续占用存储并破坏资产计数一致性。
- 证据：`test-evidence/blackbox-20260829/BB-006/orphan-check.txt`、`orphan-source.json`。
- 处理结果：解析或元数据创建失败时删除已保存文件；回归断言 files 目录和资产列表均为空，避免孤儿文件。

### ISS-011：AI 未配置时页面未显示配置状态或人工 SQL 入口

- 类型：产品可用性/错误降级；优先级：P1；状态：已修复并完成自动化回归。
- 现象：`GET /api/admin/ai/status` 在 `DATAPULSE_AI_ENABLED=false` 时返回 `{"status":"unconfigured","model":null}`；大屏生成和数据集分析请求返回 503、错误 code `AI_NOT_CONFIGURED`。两个页面只显示通用英文错误“The AI request could not be completed.”和 request ID，没有显示未配置状态、重试/降级说明或设计文档要求的人工 SQL 入口。
- 影响：管理员无法区分 AI 未启用、provider 故障和网络错误；默认关闭 AI 的部署无法得到可操作的降级路径，也不利于排查配置。
- 复现：使用 BB-015 的独立实例，访问 `test-evidence/blackbox-20260829/BB-015/BB-015-dataset-ai-error.png` 或打开大屏 AI 生成对话框，提交已选临时数据集后观察 Network 与页面文案。
- 证据：`test-evidence/blackbox-20260829/BB-015/BB-015-status-response.txt`、`BB-015-screen-response.txt`、`BB-015-dataset-ai-error.png`、`BB-015-ai-not-configured.png`。
- 处理结果：三个 AI 入口读取状态并显示未配置中文提示；数据集详情提供 `#dataset-sql-editor` 人工 SQL 入口；后端 AI 错误 code 已中文化。AI 单元/E2E 已复测。

### ISS-012：前端代理端口错误时登录页显示为账号登录失败

- 类型：环境中断下的错误映射/可观测性；优先级：P1；状态：已修复并完成自动化回归。
- 现象：用户截图显示登录页已经填入 `admin` 和密码并提示“登录失败，请稍后重试”。核对发现前端 `5173` 进程未设置 `VITE_API_PROXY_TARGET`，按 `apps/web/vite.config.ts` 默认代理到未监听的 `8000`；即使后端在 `8001` 恢复，`/api/auth/status` 仍返回 500。后端日志没有对应的 `POST /api/auth/login`，页面进入 `LoginView.vue` 的非 `ApiError` 通用分支。
- 影响：开发服务端口配置错误被用户误解为凭据错误，无法区分代理故障和登录失败；截图中的密码是否正确无法通过该次请求判断。
- 复现：使用 `pnpm --filter @datapulse/web dev --host 127.0.0.1 --port 5173` 且不设置 `VITE_API_PROXY_TARGET`，后端仅监听 `8001`，打开 `/studio/login` 并观察 `/api/auth/status` 500 与通用错误；设置 `VITE_API_PROXY_TARGET=http://127.0.0.1:8001` 后重启 Vite 可恢复。
- 证据：用户提供的登录失败截图；`test-evidence/blackbox-20260829/BB-019/BB-019-service-health.txt`、`BB-019-server-recovery.log`。
- 当前恢复核对：Vite 已使用 `VITE_API_PROXY_TARGET=http://127.0.0.1:8001` 重启；隔离数据库的合成 `admin` 凭据已重置并撤销旧会话，`POST /api/auth/login` 返回 204、`GET /api/auth/session` 返回 200。该动作仅恢复本地测试环境，不代表默认端口配置或错误文案已完成产品修复；证据见 `test-evidence/blackbox-20260829/BB-019/BB-019-login-after-reset.txt`。
- 处理结果：登录页区分凭据错误、限流和服务器/代理不可用；认证状态探测遇到 5xx 保持匿名，避免误判凭据。恢复后的登录 E2E 已通过；默认端口仍需部署确认，不记录密码原文。

### ISS-013：中文登录界面直接显示英文凭据错误

- 类型：产品可用性/本地化；优先级：P2；状态：已修复并完成自动化回归。
- 影响范围：BB-020；管理员登录页无效凭据错误展示。
- 现象：中文登录页在收到 401 `AUTH_INVALID_CREDENTIALS` 后直接显示后端英文文案 `The username or password is invalid.`；页面其他主要文案为中文。后端错误 code 仍统一区分用户名和密码，语义符合安全要求，但前端没有转换为中文用户提示。
- 影响：中文用户看到语言不一致的错误信息，降低可理解性和界面一致性；不影响认证安全边界。
- 复现：前端代理指向 `8001` 时，在 `/studio/login` 提交明确无效的测试账号/密码，观察 401 响应和页面文案。
- 证据：`test-evidence/blackbox-20260829/BB-020/BB-020-user-login-401.png`、`test-evidence/blackbox-20260829/BB-019/BB-019-after-recovery-invalid-credentials.json`。
- 处理结果：登录页按 `AUTH_INVALID_CREDENTIALS`、`AUTH_RATE_LIMITED` 和网络错误显示中文提示，同时保留 request ID 和字段错误；自动化回归已通过。

### ISS-014：数据源空状态重复提供创建入口

- 类型：产品可用性/UI 冗余；优先级：P2；状态：已修复并完成自动化回归。
- 影响范围：`/studio/datasources` 无数据源空状态。
- 现象：空状态同时显示可进入创建流程的加号和“新建数据源”文字按钮；两者都指向 `/studio/datasources/new`，操作能力重复。
- 复现：使用无数据源的隔离测试实例打开 `/studio/datasources`，观察空状态中的两个创建入口。
- 证据：用户提供的 `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-92454e94-784b-4985-86b0-f90a7973e9c8.png`；代码位置为 `apps/web/src/features/datasources/DatasourceListView.vue` 的空状态模板。
- 用户期望：只保留加号作为新建数据源入口，并保证加号仍可进入 `/studio/datasources/new`。
- 处理结果：空状态仅保留加号入口；有数据源时保留顶部创建按钮；加号继续进入 `/studio/datasources/new`。列表单元回归已通过。

### ISS-015：数据源新建/编辑表单缺少测试连接入口

- 类型：产品需求/功能缺口；优先级：P1；状态：已修复并完成自动化回归。
- 影响范围：`/studio/datasources/new` 和 `/studio/datasources/:id/edit` 表单。
- 现象：当前表单底部只有“取消”和“创建数据源/保存更改”，用户希望增加“测试连接”功能。现有前端 `apps/web/src/features/datasources/api.ts` 的 `testDatasource(datasourceId)` 和后端 `POST /api/admin/datasources/{datasource_id}/test` 只面向已保存数据源，不能直接验证尚未保存的表单配置。
- 影响：管理员在保存前无法确认新填写的主机、端口、数据库、凭据或 API 地址是否可连接；已保存数据源详情页也没有手动检查按钮，创建后会保持“连接状态：未检查、上次检查：尚未检查”，需要返回列表点击“测试”才会更新；若直接新增按钮，需要定义未保存配置的请求契约、凭据处理、错误展示和重复点击行为。
- 证据：用户提供的 `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-7dac98f3-b705-420d-b6ea-4241f9b28ab1.png` 和 `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-f0864a1b-7b24-4b54-a7e1-90dd0e78af95.png`；代码位置为 `apps/web/src/features/datasources/DatasourceFormView.vue`、`apps/web/src/features/datasources/DatasourceDetailView.vue`、`apps/web/src/features/datasources/DatasourceListView.vue`、`apps/web/src/features/datasources/api.ts`、`apps/server/src/datapulse/datasource/api.py`。
- 处理结果：新增 `POST /api/admin/datasources/test` 临时配置测试接口；新建/编辑表单增加“测试连接”按钮，编辑时可复用已保存密文，测试请求不持久化配置或回显密码。前后端单元/API 回归已通过。

### ISS-017：AI 面板辅助文字与背景对比度不足

- 类型：产品可用性/无障碍视觉；优先级：P1；状态：已修复并完成自动化回归。
- 影响范围：大屏编辑器右侧“AI 修改”“AI 分析”面板，以及 AI 生成大屏对话框。
- 现象：修复前面板使用半透明深色背景和浅灰/浅蓝辅助文字，说明、标签和命令列表与背景接近，截图中难以阅读。
- 证据：用户截图 `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-b4fd1d99-c492-4be5-bcea-54f81e426899.png`；代码位置为 `apps/web/src/features/ai/AiAnalysisPanel.vue`、`AiEditPanel.vue`、`AiScreenGeneratorDialog.vue`。
- 处理结果：两个面板改为不透明白底和深色边框；说明、占位、错误、命令和结果徽章改用高对比度文字；生成大屏对话框同步统一辅助文字、数据集区域和结果徽章配色。
- 验证证据：`test-evidence/automation-20260829/AUTO-E2E-006.log`；Chromium AI 分析/编辑 E2E 颜色断言 2/2 通过，AI 定向 Vitest 2 files/3 tests passed，`pnpm --filter @datapulse/web build` 退出码 0。

### ISS-016：ERP 测试数据中文字段存在双重编码（外部数据质量待确认）

- 类型：测试数据质量/数据一致性风险；优先级：P1；状态：待数据源所有者确认，未认定为 DataPulse 产品缺陷。
- 影响范围：BB-022 中 ERP `products.product_name` 查询结果及由该查询生成的数据集预览。
- 现象：SQL 查询返回的中文名称显示为 UTF-8 乱码；只读核对 `HEX(product_name)` 发现数据库存储字节本身已是乱码字符串的 UTF-8 编码，连接字符集 `client/connection/results` 均为 `utf8mb4`。因此当前证据更支持测试数据在写入源库时发生双重编码，不能仅凭页面结果归因于 DataPulse。
- 复现：在 BB-022 的 ERP 数据源执行 `SELECT product_name, HEX(product_name), @@character_set_client, @@character_set_connection, @@character_set_results FROM products LIMIT 1`，保存前后均观察到相同编码结果。
- 证据：`test-evidence/blackbox-20260829/BB-022/BB-022-observation.txt`、`BB-022-products-save.png`。
- 后续动作：由数据源所有者用数据库客户端核对原始值和导入脚本编码；确认源数据正确后，再复测 DataPulse 查询和数据集预览。当前不修改业务代码或测试数据源。

## 不在本次范围

- 计划生成阶段不修改业务代码、测试代码或项目配置；用户后续明确授权修复后，代码和测试变更及验证结果已在本文件和问题索引中记录。
- 不测试或承诺多用户协作、RBAC、模板/组件市场、多页面、设备管理、3D、复杂动画和响应式自由布局。
- 不测试 SQL Server、Oracle、ClickHouse 等未注册连接器；不连接生产 PostgreSQL/MySQL/MariaDB 或真实 AI provider。
- 不实现或验收发布版本历史、审批流、自动 rollback、直接 restore；只验证复制备份和当前草稿/发布快照边界。
- 不把历史 `docs/TESTING_SESSION.md` 的执行结果复述为本次结果；人工黑盒本轮结果以“黑盒人工测试执行记录”章节和证据目录为准。
- 不删除用户已有数据、镜像、卷、浏览器配置或未跟踪文件；Docker 临时容器/volume/测试镜像已按 BB-013 清理，应用测试资源在收尾阶段按 ID 清理。

## 生成说明

本次实际读取/核对的关键文件包括：

- `/Users/dundundebaba/sync/code/open/DataPulse/README.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/package.json`
- `/Users/dundundebaba/sync/code/open/DataPulse/pnpm-workspace.yaml`
- `/Users/dundundebaba/sync/code/open/DataPulse/pnpm-lock.yaml`
- `/Users/dundundebaba/sync/code/open/DataPulse/pyproject.toml`
- `/Users/dundundebaba/sync/code/open/DataPulse/uv.lock`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/pyproject.toml`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/package.json`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/vite.config.ts`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/playwright.config.ts`
- `/Users/dundundebaba/sync/code/open/DataPulse/Dockerfile`
- `/Users/dundundebaba/sync/code/open/DataPulse/compose.yaml`
- `/Users/dundundebaba/sync/code/open/DataPulse/compose.test.yaml`
- `/Users/dundundebaba/sync/code/open/DataPulse/.env.example`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/app.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/auth/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/auth/dependencies.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/datasource/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/dataset/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/screen/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/screen/runtime_api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/display/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/server/src/datapulse/embedding/api.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/src/router/index.ts`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/src/features/screens/ScreenEditorView.vue`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/src/features/player/PlayerView.vue`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/src/e2eEmbedHost.ts`
- `/Users/dundundebaba/sync/code/open/DataPulse/tools/prepare_e2e.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/tools/run_e2e.py`
- `/Users/dundundebaba/sync/code/open/DataPulse/apps/web/e2e/global-teardown.ts`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/known-issues.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/TESTING_SESSION.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/superpowers/specs/2026-07-29-datasource-studio-design.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/superpowers/specs/2026-07-30-screen-editor-player-embedding-design.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/superpowers/specs/2026-08-09-datav-editor-workbench-design.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/superpowers/plans/2026-08-08-editor-datasource-api-remediation.md`
- `/Users/dundundebaba/sync/code/open/DataPulse/docs/superpowers/plans/2026-08-09-datav-editor-workbench.md`
- `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-9621be25-ff9d-43ab-ac3a-eaa818b67952.png`（用户于 2026-08-30 提供的登录页面截图；仅复制脱敏可见内容，未记录密码）
- `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-92454e94-784b-4985-86b0-f90a7973e9c8.png`（用户于 2026-08-31 提供的数据源空状态截图）
- `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-7dac98f3-b705-420d-b6ea-4241f9b28ab1.png`（用户于 2026-08-31 提供的数据源表单截图）
- `/var/folders/vn/mfggccds6x1dy3d4pn48_2680000gn/T/codex-clipboard-f0864a1b-7b24-4b54-a7e1-90dd0e78af95.png`（用户于 2026-08-31 提供的数据源详情“尚未检查”截图）

已确认的命令来源是根 `package.json`、`README.md`、`pyproject.toml`、Vitest/Vite/Playwright 配置、Compose 文件和 E2E runner；本轮实际执行命令及结果已写入“测试执行结果”和“黑盒人工测试执行记录”章节，并保留在 `test-evidence/automation-20260829/`、`test-evidence/blackbox-20260829/`。本轮追加 BB-015～BB-018，使用端口 8002/5174 的独立临时实例完成 AI 未配置、密码会话、查询边界和真实 Chromium 字段观察；相关临时实例已停止，临时数据移入 `/tmp` 下带明确前缀的可恢复隔离目录，未触碰主测试数据。仓库未发现 `AGENTS.md` 或 `CONTRIBUTING*` 文件。`NN=01` 是因为当前仓库没有既有 `*测试计划.md` 文件；仍需人工确认的假设包括 DP-001 的真实浏览器 autofill、Firefox/WebKit 兼容性、生产静态 `/embed` 入口的 CSP，以及未逐项执行的并发/超时/最大行数矩阵。BB-013 的 Docker runtime 构建和容器烟囱测试已完成。用户后续明确授权统一修复，ISS-006～ISS-015 和本轮新增 ISS-017 的代码/样式修复及回归结果已追加到本文和问题索引；ISS-004 残余人工覆盖和 ISS-016 外部数据质量仍保持未关闭。以上记录不等同于最终发布结论。
截至 2026-08-30，主测试服务仍在运行并已核对 `GET http://127.0.0.1:8001/api/health` 和 `GET http://127.0.0.1:5173/api/auth/status` 均返回 HTTP 200；前端 Vite 进程通过 `VITE_API_PROXY_TARGET=http://127.0.0.1:8001` 启动。针对用户截图后发现的隔离测试凭据不一致，已将数据库备份到 `/tmp/datapulse-login-reset-backup-20260830.db`，重置合成 `admin` 密码并撤销旧会话；通过前端代理的登录返回 HTTP 204、会话返回 HTTP 200，详情见 `test-evidence/blackbox-20260829/BB-019/BB-019-login-after-reset.txt`。用户截图对应的 BB-020 观察和 ISS-013 已追加到本文及 `test-evidence/blackbox-20260829/BB-020/`、`test-evidence/automation-20260829/ISSUES.md`；凭据值、Cookie、token 和密钥均未写入记录。上述记录只表示当时已运行命令或已观察步骤，后续修复状态以本文件最新“测试执行结果”和问题索引为准。
截至 2026-08-31，用户又提出空状态只保留加号以及在数据源表单增加测试连接入口；两项分别记录为 ISS-014 和 ISS-015。用户随后明确授权修复，已完成前后端实现、测试补充和自动化回归，详见问题索引。
截至 2026-08-31，针对“无法从数据源创建数据集”的反馈执行 BB-022：正确入口的查询与创建请求分别返回 200/201，详情页跳转正常；两个临时数据集已清理，用户确认此前为入口误操作。本次未新增产品问题；证据见 `test-evidence/blackbox-20260829/BB-022/`。
截至 2026-08-31，针对用户提供的 AI 面板截图完成 ISS-017 样式修复：相关面板改为高对比度浅色主题，Chromium E2E 颜色断言 2/2 通过，AI 定向测试 3/3 通过，Web 构建退出码 0。未使用真实凭据、生产数据或外部 AI 服务。
