# 独立审查记录

2026-09-21，审查当前未提交工作区；HEAD 为 `37a11b0`，包含保留的第三阶段实现。

审查范围：API/worker 生命周期、维护租约、插件发布依赖与播放/Embed 鉴权、CSP、结构化请求日志、备份引用验证、本地容器验证工具，以及后续浏览器运行器隔离。

两项 P2 均已通过真实 fixture 复现并修复：

1. 删除未引用的音频后，完成任务仍可保留缓存资产 ID，导致后续备份失败。现在只在备份快照中清除终态任务缺失资产的缓存关联，保留源实例历史；活跃任务和真实文档的资源缺失仍拒绝备份。
2. 安装模板的依赖未纳入备份校验。现在校验模板文档合同及插件精确版本，缺少依赖时拒绝创建、校验或恢复归档。

复审没有确认新的重要问题。独立定向执行 12 项回归通过；主代理再次运行备份、含数字人资源的 CLI 恢复和运行器相关回归，47 项通过，见 `review-followup-recheck.log`。这两个计数存在重叠，不相加计为新增覆盖。

浏览器运行器改为每个项目独立准备和清理数据库，并分离输出目录；前一项目失败仍执行后续项目并保留非零退出码。该修复避免不同浏览器之间的同名资源冲突。

后续容量预检发现同一 CSV 的并发查询重复解析；新增有界缓存与共享解析后，再次独立审查缓存失效、错误响应和取消行为。审查复现重复取消可能中断子进程启动/回收，导致泄漏；引入受保护的清理等待，并对扫描器同类路径复现和修复。独立真实子进程探针确认取消返回前进程已回收、暂存目录清除、处理槽可以再次使用。没有剩余已确认的缓存或运行时阻塞缺陷。最终后端全量 848 passed / 15 deselected，取消与缓存定向 27 passed。
