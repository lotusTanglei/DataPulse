# 产品第四阶段目标环境与生产成熟度门禁

日期：2026-09-12

这些用例属于产品第四阶段“产品成熟度与生态”的目标部署兼容性、数字人生产成熟度和长期运维验收。2026-09-09 经用户明确调整，四项目标环境门禁从第二阶段移交至第四阶段；2026-09-12 再将数字人真实供应商、播放矩阵、长期性能和生产运维门禁纳入本阶段，不再作为第三阶段功能完成条件。第二、三阶段的完成依据分别见 `docs/PHASE_TWO_CLOSEOUT.md` 和 `docs/PHASE_THREE_PROGRESS.md`。对话数字人暂不纳入当前阶段交付，设计文档仅作为独立产品候选。自动化仿真、开发代理和短时预检可以提前发现缺陷，但不能替代本文件明确要求的真实目标环境或生产证据。

## 待验收矩阵

| 编号 | 第四阶段待办 | 状态 | 阻塞条件 |
| --- | --- | --- | --- |
| P4-ENV-001 | 真实密码管理器兼容性 | 阻塞（待完成） | 缺组织实际使用的浏览器/密码管理器组合及专用配置 |
| P4-ENV-002 | 目标生产代理链、CSP 与客户端 IP | 阻塞（待完成） | 缺将要上线的完整 LB/WAF/CDN/代理链 |
| P4-ENV-003 | 物理 4K/高 DPI 显示验收 | 阻塞（待完成） | 缺物理 3840×2160 或以上显示设备；Retina 高 DPI 抽样已通过 |
| P4-ENV-004 | 连续 8 小时播放稳定性 | 阻塞（待完成） | 缺目标浏览器/代理/kiosk 条件下的完整执行窗口和资源监控证据 |
| P4-DH-001 | 真实 TTS 供应商与故障/语言降级 | 阻塞（待完成） | 缺专用真实 Azure/custom 账号、供应商网络矩阵和脱敏端到端证据 |
| P4-DH-002 | 真实媒体、无障碍、高 DPI 与播放矩阵 | 阻塞（待完成） | 缺真实跨域媒体、屏幕阅读器、物理高 DPI 和目标浏览器播放矩阵证据 |
| P4-DH-003 | 数字人性能基线与 8 小时长播 | 阻塞（待完成） | 缺目标环境首帧/字幕基线、连续 8 小时资源趋势和跨主机调度证据 |
| P4-DH-004 | 数字人生产运维与安全部署 | 阻塞（待完成） | 缺生产指标告警、结构化日志、重启演练、实际扫描器和处理隔离证据 |

第四阶段门禁统计：通过 0，失败 0，未执行 0，阻塞 8。八项均未实际完成，因前置条件缺失统一列入阻塞，不重复计入未执行。跟踪记录为 `docs/known-issues.md` 的 DP-005 和 `docs/TESTING_SESSION.md` 的 ISS-022。

## 通用规则

- 使用专用测试账号、随机密钥、fixture 数据和隔离数据库；禁止生产凭据和客户数据。真实 AI/TTS provider 只能使用专用测试账号，并按 P4-DH-001 保存脱敏证据。
- 每项记录提交、操作系统、浏览器及版本、部署拓扑、显示设备、开始/结束时间、命令退出码和脱敏证据路径。
- 失败、skip、deselect、仿真结果或未完成的人工步骤不能计为通过。
- URL、Cookie、Local/Session Storage、请求/响应头、代理日志和应用日志都要检查；证据中不得保留 display key、API key、ticket、Session、数据库密码或初始化代码。

## 真实密码管理器

前置条件：目标 Chrome/Safari/Firefox 与组织实际使用的密码管理器，建立全新浏览器配置，仅保存专用 DataPulse 管理员测试凭据。

1. 证明登录页能够填入并提交管理员凭据。
2. 登录后分别打开 PostgreSQL 和 MySQL/MariaDB 的新建、编辑表单。
3. 确认数据库用户名和密码没有被管理员凭据填入；编辑密码保持空白。
4. 检查页面、Network、URL 与 Storage，不得出现管理员密码。
5. 至少对组织支持矩阵中的每种密码管理器执行一次；浏览器原生密码存储和扩展式密码管理器分别计结果。

关闭条件：所有目标组合通过并有截图、浏览器版本、表单值摘要和脱敏 Network 证据。当前自动化只固定 `autocomplete` 契约，不能关闭此门禁。

## 生产反向代理与 CSP

DataPulse 容器只信任 `DATAPULSE_FORWARDED_ALLOW_IPS` 指定的代理地址。保持默认 `127.0.0.1`，除非前方确有可信代理；禁止配置为 `*`。代理必须覆盖客户端传入的 `X-Forwarded-For`、`X-Real-IP` 和 `X-Forwarded-Proto`，不能追加不可信链；参考 `deploy/nginx/datapulse.conf`。

本地生产等价预检：

```bash
uv run --package datapulse-server python tools/verify_proxy_boundary.py \
  --evidence-dir test-evidence/blackbox-YYYYMMDD/proxy-equivalent
```

目标环境还必须复核：

1. HTTPS Studio 的写请求不出现 `AUTH_ORIGIN_INVALID`，Session/CSRF Cookie 为 Secure。
2. 伪造转发头不能绕过 IP 策略；允许/拒绝的真实客户端 IP 符合负载均衡器和代理链定义。
3. `/embed/{id}` 与嵌入 API 保留精确 `frame-ancestors`、`no-store`、`no-referrer` 和 `nosniff`。
4. WAF/CDN/代理不缓存播放或嵌入响应，不改宽 CSP，不把 query string 写入访问日志。
5. 应用日志中的 ticket 只能显示 `[REDACTED]`；display key、API key、Session 和数据库凭据均不出现。

关闭条件：在将要上线的完整代理链执行，而不是只在本机 Nginx 预检通过。

## 物理 4K 与高 DPI

目标设备必须报告至少 3840×2160 物理分辨率和实际 DPR；远程桌面缩放、无头浏览器或 Playwright `deviceScaleFactor` 仿真不能替代。

```bash
pnpm --filter @datapulse/web exec playwright install chromium
pnpm test:e2e:display
```

人工核对深色/浅色的编辑器、草稿预览、独立播放和嵌入：标题、坐标、图例、表格、图片和地图清晰；16:9 contain 留白正确；无重叠、裁切、横向滚动或空白 Canvas；系统“减少动效”开启时图表首帧完整且不播放入场动画。

关闭条件：物理 4K 设备截图/照片、系统显示信息、DPR 指标和人工核对记录齐全。3024×1964 Retina 可以关闭高 DPI 抽样，但不能关闭 4K 子项。

## 长期播放

第四阶段门禁时长固定为连续 8 小时。使用目标浏览器、目标代理和接近上线的刷新策略；浏览器窗口保持前台或使用部署时实际的 kiosk 模式。

```bash
DATAPULSE_E2E_SOAK_SECONDS=28800 \
DATAPULSE_TARGET_EVIDENCE_DIR=test-evidence/blackbox-YYYYMMDD/soak \
pnpm test:e2e:soak
```

通过标准：进程不中断；所有查询完成且无失败；最大并发不超过单轮 7 个绑定组件；无残留 loading/错误；标题和数据组件持续可见；每分钟 JS 堆采样无持续失控增长且总增长低于 128 MiB。另用目标系统监控记录浏览器 RSS、CPU、页面崩溃和网络重连。

关闭条件：完整 8 小时退出码为 0，并保存指标 JSON、结束截图、浏览器资源趋势和代理/应用脱敏日志。10 分钟预检不能计为长期播放通过。

## 数字人生产成熟度门禁

### P4-DH-001 真实 TTS 供应商

使用专用 Azure/custom 测试账号和脱敏 fixture，验证供应商连接、语音列表、合成音频、超时、连续失败熔断、明确语言降级、缓存版本隔离和用量记录。不得使用客户数据或生产凭据；fake TTS 和 HTTP mock 只作为第三阶段本地回归证据。

关闭条件：目标网络中每个已启用供应商至少完成一次端到端合成和一次故障/语言降级演练，保存供应商、语言、错误码、延迟、音频校验和脱敏日志；供应商凭据不出现在日志、URL、任务或证据中。

### P4-DH-002 真实媒体、无障碍、高 DPI 与播放矩阵

在目标浏览器、独立播放、Embed、跨域宿主和真实媒体资源中验证音频/视频时钟、字幕 cue、用户手势、静音/暂停、访问撤销和错误降级。覆盖物理 4K/DPR、完整屏幕阅读器、键盘、深浅主题、窄屏和 `prefers-reduced-motion`；物理设备证据与 P4-ENV-003 共享设备信息，但需单独记录数字人媒体和字幕结果。

关闭条件：组织支持矩阵中的浏览器、播放模式和媒体类型均有通过记录，包含物理显示信息、屏幕阅读器结果、字幕截图、媒体时钟和跨域 Network/CSP 脱敏证据。

### P4-DH-003 性能与 8 小时长播

在目标部署执行数字人首帧、数据到字幕和数据到音频基线，并完成连续 8 小时长播。记录浏览器 RSS/CPU、JS 堆、音频上下文、网络重连、队列长度、任务并发和跨主机 worker 调度；P4-ENV-004 的 8 小时窗口可复用，但必须保留数字人维度指标。

关闭条件：目标环境基线达到发布阈值，8 小时退出码为 0，无任务泄漏、音频重叠、持续内存增长或错误风暴，并保存指标 JSON、结束截图和脱敏应用/代理日志。

### P4-DH-004 生产运维与安全部署

在上线拓扑执行指标导出与告警、结构化日志采集、进程重启和未完成任务恢复演练、跨主机容量治理、数据出站提示、实际恶意文件扫描器和处理隔离。核对数字人话术、媒体、供应商凭据、ticket、Session 和 API key 的最小暴露。

关闭条件：演练记录显示告警可达、日志字段和保留策略符合要求，重启不会重复播报或丢失可诊断状态，扫描器和隔离策略在目标环境实际生效，并完成安全审查签字。

## 2026-09-09 本地预检记录

- 生产等价 HTTPS Nginx：通过；结果见 `test-evidence/blackbox-20260909/proxy-equivalent/result.json`。
- 3024×1964 Retina、DPR 2 和 3840×2160、DPR 2 仿真：最终自动化 2/2，通过人工视觉复核；首张过早截取的 Canvas 证据保留但不计通过。
- 60 秒浸泡：49/49 次查询完成，无错误；10 分钟浸泡：427/427 次完成、最大并发 7、JS 堆最终增长 1.27 MB，无请求、页面或 console 错误。
- 上述三组仅为预检。真实密码管理器、目标生产代理链、物理 4K 和连续 8 小时播放仍为第四阶段阻塞，不计为目标环境验收通过；第二阶段完成状态依据已调整的阶段范围及已通过的功能门禁。

## 2026-09-21 条件确认

用户已明确回复“尚未准备，先完成开发和本地验证”。因此本轮不运行假定目标条件的物理显示、真实供应商和 8 小时门禁。开发、fixture 自动化、数据库集成及隔离恢复可以继续；新增本地证据见 `PHASE_FOUR_PROGRESS.md` 和最终交付记录。八項仍为通过 0、失败 0、未执行 0、阻塞 8。
