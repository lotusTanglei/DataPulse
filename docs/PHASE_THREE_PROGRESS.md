# 第三阶段数字人实现与验收进度

更新时间：2026-09-12。

唯一范围依据：`docs/superpowers/specs/2026-09-09-phase-three-digital-human-broadcast-design.md`。
本记录不替代、不缩减原规格。第三阶段负责数字人功能实现、可用降级和本地自动化回归；真实供应商、目标部署和生产成熟度门禁属于第四阶段。
当前结论：**第三阶段功能开发与本地回归已完成；第四阶段目标环境与生产成熟度门禁另行验收。**

## 2026-09-12 继续推进

- 补齐 Web 端 `SpeechPlanRequest`/`SpeechPlan` 类型中的 `rate`、`pitch`、`volume` 字段，使编辑器试听请求与服务端合同、数据库迁移和供应商适配器保持一致。
- 持久 speech worker 增加独立调度查询：服务端按任务优先级降序、同优先级创建时间 FIFO 领取排队任务，管理端任务列表排序保持不变；新增仓储顺序回归，原子 `queued -> running` 领取仍由数据库保证。
- 收紧话术变量敏感字段 allowlist：前后端统一规范化驼峰、下划线和短横线命名，阻止 `phoneNumber`、`emailAddress`、`apiKey` 等变体进入话术或 AI 上下文，并补充同步回归。
- 本轮新增定向验证通过：数字人合同 82 项、Web 数字人模板 23 项、speech repository 19 项；Ruff 通过。
- 本轮完整 `pnpm verify` 通过：合同同步、Embed SDK 20 项、Web 227 项、后端 672 项；15 项集成测试按配置跳过；类型检查和生产构建通过。构建仍仅有既有第三方注释和 chunk 体积警告。

## 2026-09-11 继续推进

- 冻结并实现 TTS 供应商适配器协议：`openai_compatible` 保持 `/models`、`/voices`、`/audio/speech`；`azure` 使用 Azure Speech REST 的 SSML、订阅密钥和标准语音列表；`custom` 使用直接 POST Base URL 的受限 JSON/音频约定并通过 `X-API-Key` 鉴权。服务层统一保留重试、明确语言 422 降级、熔断和用量记录；新增 Azure/custom HTTP mock 回归。真实 Azure 账号、供应商网络故障和目标环境验收仍未完成。
- 增加显式 `integration` 标记的真实供应商 smoke 测试：配置专用 `DATAPULSE_LIVE_TTS_*` 环境变量后可验证 Azure/custom 语音列表与合成音频；默认缺少变量时跳过，不输出密钥或响应体。当前环境未提供真实凭据，因此该门禁仍未验收。
- 增加启动时媒体存储对账：在资产互斥锁内读取数据库引用，按一小时宽限期清理崩溃遗留的孤立文件；数据库引用资源和近期文件保留，清理失败不阻塞服务启动；新增资产与 lifespan 回归。
- 本轮完整 `pnpm verify` 通过：合同同步、Embed SDK 20 项、Web 226 项、后端 666 项（13 项连接器和 2 项真实供应商集成测试按配置跳过），类型检查与生产构建通过；构建仍仅有既有第三方注释和 chunk 体积警告。
- 状态机补齐加载/媒体准备阶段的合法静音路径，避免用户在 `disabled`、`loading_data` 或 `preparing_media` 时操作静音触发非法跳转；新增状态转移回归。
- 修复设置页多行治理规则解析：禁播词和敏感正则按实际换行拆分后分别提交，新增 PATCH 请求体回归；该修复避免规则被错误合并为单条表达式。
- 重新执行数字人三浏览器完整 E2E：Chromium、Firefox、WebKit 共 24 项全部通过，覆盖结构化话术、fake TTS 试听/发布、独立播放、Embed、跨源宿主、访问撤销、真实视频字幕时钟和资源库媒体路径；fake 供应商证据仍不替代真实供应商验收。
- 新增 `ScreenRuntime` 切屏/刷新代际回归：替换文档时立即中止上一代查询，迟到的 `AbortError` 不冒泡为组件错误，最新一代查询结果保持可见；该证据覆盖本地刷新竞态，不替代目标环境切屏、发布切换和长播矩阵。
- 修复服务端复制大屏时数字人组件绑定引用未随组件 ID 一起重映射的问题；复制后的 `source=components` 话术绑定现在指向复制出的源组件，原屏引用保持不变，并增加服务层回归。
- 修复 Compose observability profile 未将 `DATAPULSE_METRICS_TOKEN` 传入 DataPulse 容器的问题，并新增可选 `DATAPULSE_HTTP_PORT` 主机端口配置（默认仍为 8000）。使用项目自身 `docker compose --profile observability up -d --wait` 在临时端口完成整套启动验证：健康检查 200、无 token 返回 401、正确 token 返回 200，Prometheus target 最终为 `up` 且无抓取错误；测试项目及 volume 已清理。
- 启动本机 OrbStack 后完成生产镜像与观测链路 smoke test：`docker compose build` 构建 `datapulse-datapulse` 成功；临时容器执行全部迁移并返回 `/api/health` 200；Prometheus `v3.5.0` 通过临时 Docker 网络抓取受 token 保护的内部指标端点，target 状态为 `up`，3 条告警规则成功加载。临时容器、网络和 volume 已清理；该证据仍不等同于目标环境部署、告警接收器或长期运行验收。
- 重新执行 60 秒 Chromium soak 并扩展证据采样：49 次查询全部完成，最大在途查询 7，`failedRequests/pageErrors/consoleErrors` 均为空；JS 堆从 25.11 MB 增至 26.72 MB（增长 1.61 MB），资源计时累计传输约 8.35 MB，采样点记录查询计数、在途数、堆和网络字节。该结果仍是短时门禁，不能替代 8 小时长播及 CPU/网络/音频上下文/队列趋势验收。
- 新增并通过播放基线门禁：Chromium 1920x1080 下首个运行时状态 476 ms、首个可读字幕 590 ms，查询响应到字幕 49 ms（HTTP 200）；指标随 Playwright 证据附件保存。该本地基线不替代目标环境在不同资源规格下的正式测量。
- 修复数字人卸载时动作预设 Blob URL 未释放的问题，并增加组件回归；现在头像、播报媒体、映射录音和动作资源均在卸载/切换时释放，减少长播和切屏后的内存累积风险。
- 新增跨源 Embed 浏览器回归：宿主使用 `http://localhost`、iframe 使用 `http://127.0.0.1`，Chromium/Firefox/WebKit 共 3 项通过；验证 Ticket、`frame-ancestors` CSP、消息桥、数字人字幕和 URL 清理。该测试证明父页面跨源嵌入路径，不替代外部媒体 CDN 和真实代理链矩阵。
- Embed API 错误响应现在也保留受限 CORS 头；访问策略撤销时，跨源 iframe 可读取稳定的 `403/EMBED_ORIGIN_DENIED`，不会被浏览器折叠为 `Failed to fetch`。新增服务端 HTTP 回归覆盖该行为。
- 新增访问撤销浏览器回归：已签发 Ticket 在屏幕 Origin 策略收紧后，从 iframe 跨源请求受保护文档返回 403；策略恢复后同一 Ticket 返回 200，Chromium/Firefox/WebKit 共 3 项通过。
- 增加 Embed 访问撤销回归：已签发票据在屏幕 Origin 访问策略收紧后，后续文档请求立即返回 `EMBED_ORIGIN_DENIED`；策略恢复后同一票据可继续使用，证明访问策略按请求实时生效而非只约束新票据。Embed API 专项现为 6 项通过。
- 增加可选的内部 Prometheus 指标端点 `/api/internal/digital-human/metrics/prometheus`。端点仅在配置 `DATAPULSE_METRICS_TOKEN` 时开放，使用恒时 bearer token 比较；未配置、缺失或错误 token 均返回 `METRICS_AUTH_REQUIRED`，不依赖管理员浏览器 Session。
- Prometheus 导出复用当天持久指标快照，累计任务/缓存/合成次数声明为 `counter`，耗时声明为 `gauge`，标签固定为 `service` 与 `feature`，不包含屏幕、组件、用户或话术内容。
- 增加可选 Compose `observability` profile、Prometheus 抓取配置和数字人失败率/失败次数/延迟告警规则；token 文件默认位于被忽略的 `.secrets/` 目录。配置语法、端点鉴权回归、Ruff 和完整 `pnpm verify` 已通过（Embed SDK 20、Web 221、后端 658）。
- `docker compose --profile observability config --quiet` 配置校验通过；本轮已在本机 OrbStack 使用项目自身 Compose profile 完成镜像构建、迁移、健康检查、指标鉴权和 Prometheus 抓取 smoke test。
- 该实现推进了部署准备，但尚未在目标 Prometheus、告警接收器或生产多进程环境实际部署验证，不能将生产运维门禁标为完成；本机 smoke test 仅证明配置和单机容器链路可用。
- 编辑器话术预览已接入现有 TTS 计划/任务 API：可选择已配置供应商和声音，创建受治理的预览任务、轮询 queued/running/succeeded/failed、试听生成音频，并通过明确的“采用此音频”写入草稿；失败或超时仅显示稳定错误并保留字幕/浏览器语音路径。新增编辑器回归覆盖生成成功、音频预览和草稿写入隔离。
- 新增可选 fake TTS E2E fixture（`tools/run_e2e.py`）：使用本地 deterministic OpenAI-compatible HTTP 服务生成标准 WAV，验证“创建供应商 -> 编辑器生成/试听 -> 明确采用 -> 确认录音元数据 -> 发布 -> 独立播放 -> Embed”的闭环；Chromium、Firefox、WebKit 三浏览器专项均通过。该 fixture 仅证明 DataPulse 自身链路，不替代真实供应商、供应商语言降级或生产环境验收。
- TTS 采用动作现在同时写入资产 SHA、媒体时长、确认文案和 `recording` 元数据，避免运行时因缺少录音确认而错误回退到浏览器语音；手动替换音频仍会清除旧确认信息。
- 话术编辑器新增受合同约束的 `speech_segments`：支持段落、停顿时长和强调段，所有文本段仍复用模板变量/敏感字段校验；空数组继续兼容旧 `speech_template` 文档，应用模板或 AI 预览会明确切回模板模式。
- 本轮 TTS 编辑器与计划预览错误边界接入后的 `pnpm verify` 通过：合同同步、Embed SDK 20 项、Web 217 项、后端 649 项、类型检查和生产构建。
- 新增 fake TTS 后完整数字人 E2E 三浏览器回归通过：Chromium、Firefox、WebKit 共 24 项，包含结构化话术持久化、发布隔离、TTS 试听/确认/发布、独立播放、Embed、跨源宿主、访问撤销、真实视频字幕时钟和资源库媒体路径。
- 结构化脚本与触发器增强后的最终 `pnpm verify` 通过：Embed SDK 20 项、Web 221 项、后端 657 项、合同同步、类型检查和生产构建；构建仍只有既有第三方注释及大包体积警告。
- 话术计划预览接口现在将供应商不存在和内容治理拒绝转换为稳定错误码/HTTP 状态；新增 HTTP 回归覆盖禁播词拒绝，不再向编辑器泄漏 500。
- 新增结构化话术浏览器回归：段落、停顿和强调可在编辑器修改，保存后刷新仍保留，发布快照与独立播放字幕一致；Chromium、Firefox、WebKit 三浏览器各 1 项通过。该证据仍不替代真实供应商和生产环境验收。
- 触发器新增显式 `ranking_change` 与 `status_change` 类型：分别仅接受数值排名变化或选定状态值变化，编辑器切换类型时会清理不适用的阈值/条件/参数字段；合同、运行时和全量 Web 回归已覆盖，兼容原有 `data_change`。

## 2026-09-10 继续推进

- 补齐语音缓存的保留期清理：服务端按诊断数据保留期先解除过期成功任务的缓存引用，再仅删除没有其他语音任务引用且未被大屏引用的孤立音频资源；共享缓存和已发布资源保持不变。
- 清理流程写入独立审计事件 `speech.cache_retention_cleared`，并复用启动时的保留清理入口；新增旧缓存、共享缓存和孤立缓存回收测试。
- 预录片段和动作条件增加受限优先级；运行时按优先级降序选择，优先级相同时按稳定资源/动作 ID 决定 tie-breaker，编辑器支持配置并补充合同、运行时测试。
- 增加独立 `datapulse-speech-worker` 入口；API 可关闭内置 worker，独立进程复用持久任务表和原子 `queued -> running` 转换，worker 启动不会误恢复并终止 API 进程留下的排队任务；并新增并发领取测试，避免两个进程重复合成同一任务。
- 管理 `/metrics` 接口增加当天持久任务/用量及合成耗时聚合，计数更新使用数据库原子累加并覆盖并发 worker；独立 worker 和多 API 进程可共享队列、成功/失败/取消、缓存命中和合成耗时计数；生产环境仍需验证指标导出、告警和长期趋势。
- 增加播放诊断查询闭环：管理端可按屏幕/组件筛选最近任务，返回状态、来源、资产、错误码和时间字段，不返回话术、查询数据或供应商密钥；查询支持稳定排序和数量上限，并覆盖成功/失败任务持久化测试。
- 本轮专项测试（缓存、条件优先级、无障碍、worker、持久指标、播放诊断和供应商降级边界）通过，Ruff 通过；最新完整 `pnpm verify` 通过：Embed SDK 20 项、Web 211 项、后端 641 项；13 项连接器集成测试按配置未运行。
- 三浏览器数字人 E2E 12 项通过（Chromium、Firefox、WebKit），覆盖发布隔离、Embed 控制、真实视频音频/字幕时钟、暂停/静音和资源库媒体路径。
- Chromium 数字人 E2E 额外验证区域标签、字幕键盘焦点和可访问字幕属性。
- 本轮新增持久指标证据：`0018_speech_metrics_timing` 迁移持久化合成尝试、失败和耗时；两个独立数据库连接并发累加不丢计数，服务实例重建后仍可读取当天聚合结果；供应商 5xx/网络故障重试不会错误触发语言降级。
- 播放诊断管理接口新增 FastAPI HTTP 回归：管理员认证后按屏幕/组件筛选、数量上限和无话术/查询数据字段返回均已验证；`limit=0` 等非法查询参数由接口合同拒绝。该证据仍不替代生产鉴权、跨域和部署验收。
- 编辑器数据与话术面板增加三种本地预览模式（演示数据、当前查询结果、阈值模拟），显示渲染后的文本/稳定错误码，并提供试听、停止和清理未引用缓存操作；预览不修改草稿或发布内容。新增编辑器回归覆盖模式切换和草稿不变性。
- 编辑器复制数字人时增加话术/数据绑定、触发规则、音频与录音引用三项明确策略；命令层支持安全复制并保持原组件不变，关闭触发或音频复制时会进入手动/浏览器语音安全默认。新增命令回归覆盖引用隔离。
- 参数触发路径现在会在新参数代际入队前取消旧播放、重置冷却并保留新触发；新增运行时回归验证旧语音不会覆盖参数变更后的播报。
- 本轮改动后的完整 `pnpm verify` 已通过：合同同步、Embed SDK 20 项、Web 214 项、后端 644 项（13 项连接器集成测试按配置跳过）、类型检查和生产构建；仅保留既有第三方注释和 chunk 体积警告。
- 新增受管理员保护的 `GET /api/admin/digital-human/metrics/prometheus` 文本导出端点，导出当天持久化任务、缓存、合成次数、失败数和耗时等低基数指标；HTTP 回归覆盖内容类型、固定标签和不包含屏幕/用户数据。
- 预录音频/视频明确媒体失败或卡顿时现在会只回退一次浏览器语音，保留同一任务、字幕、request ID 和错误码；自动播放拒绝仍进入用户手势状态。补充媒体失败回退与 `fallback -> speaking` 状态机回归。
- 数字人运行区域现在可通过键盘聚焦并操作：空格/Enter 播放或暂停、`m` 静音、`c` 切换字幕、Escape 停止；区域关联字幕描述并保留原生控件键盘行为。新增无障碍回归测试通过（15 项）。
- 键盘无障碍与跨域媒体改动后的完整 `pnpm verify` 通过：合同同步、Embed SDK 20 项、Web 216 项、后端 647 项，类型检查与生产构建通过；三浏览器数字人 E2E 12 项亦已重新通过。
- TTS 语言降级进一步收紧：仅当供应商 HTTP 422 响应体明确包含 language/locale 不支持语义时才尝试下一语言；无关的 422（如无效音色）立即返回稳定请求拒绝错误，不误降级。新增回归覆盖单次请求行为，语音服务专项 16 项通过。
- 语言降级兼容非 JSON 供应商错误：对纯文本 `language/locale not supported` 同样按降级链处理，普通纯文本 422 仍不降级；语音服务专项现为 17 项通过。
- Embed 跨域媒体访问补齐受限 CORS：`OPTIONS` 预检仅允许播放器所需方法/请求头，Origin 经过格式校验；已发布媒体、查询和文档响应回传票据声明的精确 Origin，并暴露必要的媒体响应头。Embed HTTP 回归 5 项通过。
- 数字人发布 E2E 增加 4K 视口检查：3840x2160 下画布、字幕和控件无横向/纵向溢出，字幕计算字号保持合同下限；Chromium、Firefox、WebKit 全套数字人 E2E 12 项通过（含该 4K 路径）。
- Embed 页面与媒体/查询响应补齐严格 CSP：限制脚本、连接、对象和基础 URL，仅允许同源资源及数字人所需的 `blob:` 图片/媒体；Embed 与静态页面回归 11 项通过。

## 2026-09-09 继续推进

- 队列编排补齐独立语义：`queue` 达到上限拒绝新任务，`merge` 合并同组件待播任务，`drop_old` 仅在达到上限时淘汰最旧待播任务，`interrupt` 释放全局音频焦点；优先级降序且同优先级 FIFO。新增策略、优先级和跨组件打断测试。
- 数字人增加安静时段行为合同与编辑器配置：`mute` 静默、`delay` 延迟、`subtitle` 仅更新字幕；开始/结束时间按组件时区校验，延迟模式在安静结束后可取消地重试入队，并在运行时显示稳定原因码。
- 话术格式化扩展为受限的枚举、排名、趋势和中文数字格式，前后端解析规则同步并增加运行时渲染测试。
- 服务端 TTS 增加供应商语言降级链：区域语言 -> 基础语言 -> 供应商默认语言（仅对明确的 422 不支持语言响应降级），新增真实 HTTP mock 测试。
- 服务端持久语音任务补齐来源、优先级、过期时间字段及 `0016_speech_task_metadata` 迁移；worker 启动任务前拒绝已过期任务并返回稳定错误码，默认任务 TTL 为 5 分钟。
- 数字人 AI 话术预览增加原文/预览差异视图与显式撤销应用，仍通过编辑器历史写入草稿，不会自动发布。
- 触发器增加按组件时区的独立有效时段（跨午夜区间也可配置），运行时自动触发在窗口外被抑制，手动触发仍可用。

- 补齐 `digital-human-cache-clear` 导出合同 fixture，合同导出测试不再因新增 Schema 漂移失败。
- 设置页新增清空未引用语音缓存的交互测试，覆盖二次确认、POST 请求和清理后的刷新路径。
- 数字人编辑器新增条件组与预录片段条件映射测试；修复条件组/片段映射更新把 Vue 响应式数组传入历史快照导致的 `DataCloneError`。
- 当前最新 `pnpm verify` 通过：Embed SDK 20 项、Web 208 项、后端 622 项；13 项连接器集成测试按配置未运行；合同同步、类型检查和生产构建通过。构建仍只有既有第三方注释和主包体积警告。
- 资源库到期提醒及指标设置页变更后的三浏览器 E2E 再次通过：Chromium、Firefox、WebKit 共 12 项（包含独立播放、Embed、真实视频字幕时钟、资源预览/版本替换和窄屏路径）。

本轮验证覆盖的是实现回归，不等同于真实 TTS 供应商、跨域媒体、8 小时长播、4K/高 DPI、CSP/代理和生产运维验收。

本轮还完成部署级资产格式门禁：`DATAPULSE_ASSET_ALLOWED_MIME_TYPES` 支持 `*` 或逗号分隔的 MIME 白名单，上传会在解码前拒绝禁用格式；默认值保持现有内置安全格式兼容。新增配置解析及上传拒绝测试通过。

资源授权到期提醒已补齐：管理 API 提供受保护的临近到期查询，资源库打开时主动加载 30 天窗口并以无障碍警告提示；已过期资源也会返回。后端 API 和前端资源库测试覆盖该路径。

资源上传增加可注入的异步病毒扫描接入点；扫描拒绝返回 `ASSET_SCAN_REJECTED`，扫描器不可用返回稳定的资源错误，默认未配置扫描器时保持现有行为。专项测试覆盖拒绝后不落盘。

浏览器 Speech Synthesis 增加语言降级链：完整区域语言不可用时依次尝试基础语言、配置的 fallback 和浏览器默认语言；自动播放拒绝仍直接进入用户手势状态，不会被语言降级吞掉。新增播放单测覆盖。

新增 `GET /api/admin/digital-human/metrics` 管理指标接口及设置页“运行指标”区域；任务、用量和 TTS 合成尝试/失败/耗时按日持久聚合，播报任务同时写入不含话术、查询结果或供应商密钥的结构化日志。生产指标导出、告警和长期趋势仍需部署验收。

## 已形成的实现基础

- `DigitalHumanSpec`、绑定、触发和受限模板合同，以及生成的 JSON Schema/TypeScript 类型。
- 数字人组件注册、六组编辑配置、同屏组件字段选择、静态/演示/已有 QueryResult 数据。
- 发布时检查同屏引用、变量绑定和已发布资源引用保护。
- 静态形象/视频展示、文字降级、基础播放控制、浏览器语音和上传音频。
- 页面内音频队列、取消、播放超时、资源清理及初始/变化/间隔/阈值/参数触发基础。
- Embed SDK 命令、状态响应、具名生命周期事件、能力协商、错误相关性及请求取消。
- 票据过期时停止数字人、卸载运行时，拒绝后续控制并清理 SDK 待完成请求。
- 图片真实解码、音视频探测与完整解码、WebVTT 校验、缩略图、不可变资源替换、引用保护、去重和项目配额。
- 编辑器媒体资源库：搜索/类型筛选/分页、图片与真实音频/视频预览、元数据和授权信息编辑、版本选用、引用检查与删除确认。
- 供应商健康历史、连续失败计数和 60 秒熔断；供应商版本参与语音缓存键。
- TTS 成本估算、按供应商成本报表、AI 治理设置、播报/健康/任务/资源/屏幕发布审计，以及按保留天数清理健康、用量、任务、话术计划和审计数据。
- 服务端播报 worker 的启动/停止和中断任务恢复标记；重启不会自动重播旧的 `queued/running` 任务。
- 临时低码率媒体预览，支持响度归一化和静音段裁剪，不写入发布资产。

这些是已存在的实现，不等于下面各功能域已经完整验收。

## 规格覆盖核对

| 原规格 | 当前证据 | 仍需完成或验证 |
| --- | --- | --- |
| 1 目标与能力分层 | 同屏 QueryResult 复用、无语音引擎字幕降级、预录视频原声和 TTS/AI deterministic fixture 回归 | 真实供应商/AI 路径及生产环境统一验证已移交第四阶段 P4-DH-001 |
| 3 DH-COMP | 组件注册、编辑器入口、静态/视频渲染、`cover/contain/原始比例`、镜像、外观与控制代码；发布/编辑/播放 E2E；组件异常局部兜底；复制大屏时数字人组件引用随 ID 重映射 | 更完整的组件异常矩阵 |
| 3 DH-APPEAR | 状态显示、独立静音/音量、说话动画、减少动态效果 CSS；动作预设按条件在播报开始时触发并自动恢复；身份/状态/字幕显示开关与语言标签 | 所有尺寸可读性 |
| 4 DH-ASSET 资源库 | 真实解码/探测、元数据与缩略图、搜索/筛选/分页、改名/版本替换、删除保护、去重；启动时孤立存储文件对账与崩溃遗留清理；含缩略图的容量、时长、记录数与跨进程处理配额；授权信息编辑、主动到期查询与提醒；三浏览器资产操作 E2E；部署级 MIME 白名单；动作资源类型、验证和发布白名单 | 损坏磁盘处理与版本历史策略完整验收 |
| 4 格式与处理 | PNG/JPEG/WebP 与 MP3/OGG/WAV/MP4/WebM 实际解码测试；WebVTT 毫秒/顺序/重叠/坏片段检查；FFmpeg 输出限长/超时/取消回收；Docker 添加 FFmpeg；临时预览已验证 MP3、低码率 MP4、`loudnorm` 与 `silenceremove` 参数 | 新 Docker 镜像构建与部署验证；透明视频跨浏览器兼容矩阵 |
| 5 DH-SCRIPT | 静态与模板话术、受限语法；结构化 `speech_segments` 段落/停顿/强调编辑；预录片段条件映射及发布一致性校验 | 生产媒体与数据快照、真实供应商音频停顿口径移交第四阶段 P4-DH-001/P4-DH-002 |
| 5 DH-TEMPLATE | 数字/百分比/货币/日期/文本；枚举/排名/趋势/中文数字；缺值、错误和过期基础；敏感字段大小写、驼峰、下划线和短横线变体拦截 | 更完整的本地化数字和趋势质量验收；复杂字段语义和数据源命名质量验收 |
| 5 DH-CONDITION | 单规则阈值、变化与参数；显式排名变化/状态变化；`all/any` 条件组、优先级、稳定 tie-breaker、进入/恢复边沿和防抖；按组件时区的有效时段窗口与编辑器配置；刷新代际旧数据隔离回归 | 更完整的条件与本地化质量验收 |
| 5 DH-AI | 复用 AI Gateway 的生成/改写/缩短/翻译预览 API；编辑器支持显式生成、变量范围展示、数据出站确认、警告、“应用预览”确认、差异视图和撤销；服务端校验变量、模板结构、敏感字段和禁用内容；AI 草稿成功/失败审计记录模型、操作、阶段与耗时且不写入完整话术 | 完整诊断和多语言质量验收 |
| 6 DH-VOICE | 上传音频、浏览器语音、预录视频原声；超时、拒绝自动播放、暂停计量和时长测试；供应商 CRUD/测试、OpenAI-compatible/Azure/custom TTS 适配器、受控音频资产写入、内容哈希缓存、供应商版本参与缓存、有限重试、持久任务状态、用量扣减、成本估算和语音列表；编辑器 TTS 试听/明确采用；健康历史与 60 秒熔断；手动清理及按保留期回收未引用缓存、设置页入口；浏览器和服务端语言降级链 | 真实供应商端到端和供应商侧语言降级移交第四阶段 P4-DH-001 |
| 6 DH-SUBTITLE | 文字、估算分句、高亮、开关；字幕资产导入与受校验 cue 元数据；预录音视频真实媒体时钟、手工时间轴和 Embed SDK `subtitleCue` 事件；服务端 4.5:1 对比度与 12px 字号下限；受限字幕前景/背景色、换行/滚动模式、语言标注和隐藏字幕状态通知 | 完整屏幕阅读器、真实跨域媒体播放矩阵移交第四阶段 P4-DH-002 |
| 7 DH-TRIGGER | 初始/手动/间隔/变化/阈值/参数与宿主入口；切屏刷新代际回归 | 更完整触发组合、首次/恢复边界回归和编辑器模拟 |
| 7 DH-QUEUE | 页面音频焦点、限长、过期、取消；`queue/merge/drop_old/interrupt` 独立语义、优先级 FIFO；服务端任务来源/优先级/过期时间持久化；持久 worker 按优先级/FIFO 调度并以原子领取避免重复执行 | 跨主机调度、持久配额竞争和安静时段目标环境验收移交第四阶段 P4-DH-003 |
| 8 DH-STATE | 状态变化、任务 ID、开始/结束/错误、取消后迟到回调测试 | 所有非法跳转防护；静音与播放正交状态；更新/切屏/发布切换的全面矩阵 |
| 8 DH-FALLBACK | 无引擎降级、加载失败和自动播放状态 | AI/TTS/缓存/预录音频的完整回退路径；同一话术版本约束；生产诊断信息 |
| 9 DH-EDITOR | 六分组、字段插入、上传、基本预览；媒体资源库和明确选用新版本；关闭/切换预览释放媒体；原生模态焦点和 Safari 返回焦点 E2E；AI 原文/预览差异与撤销应用；安静时段与队列策略配置；复制组件时按批次重映射数字人数据引用 | 格式化控件、空值预览、阈值模拟、完整资源诊断 |
| 9 DH-SETTINGS | 服务端持久化总开关、默认静音/字幕、单条时长、冷却时间、每日任务数和音频秒数限额、AI 模型覆盖、上下文变量上限、数据保留天数、禁播词、敏感信息正则和人工审核开关；管理 API GET/PATCH；前端设置页支持策略保存、供应商添加/编辑/测试/语音列表/删除、健康历史、今日用量、成本报表、刷新状态和近 30 天资源授权到期提醒 | 生产保留策略验收 |
| 10 DH-PLAYER | 三浏览器独立播放/嵌入基础 E2E（当前数字人套件 24 项）；草稿隔离；过期停止单测；跨源访问撤销回归 | 真实媒体用户手势、所有播放模式一致性、切屏与访问撤销全矩阵移交第四阶段 P4-DH-002 |
| 10 DH-SDK | 命令/事件、能力协商、Origin/窗口/实例/请求/组件校验；字幕 cue 事件及排名/状态变化来源；SDK README；非法 cue 拒绝测试 | 真实跨域媒体播放及高级已发布规则选择/限额联动移交第四阶段 P4-DH-002 |
| 11 管理/播放接口 | 资源元数据/修改、缩略图、版本、引用、用量 API；统一查询复用；供应商 CRUD/测试、健康历史、语音列表、话术计划、任务入队/查询/取消/列表、按屏幕/组件筛选的播放诊断、AI 草稿预览、持久使用记录、按供应商成本报表、全局设置、审计和诊断页 API；播放器/Embed 损坏路径稳定错误与无路径泄漏测试 | 生产环境完整接口验收 |
| 11 数据模型与版本 | 资产/修改/引用/用量及 SpeechPlan/SpeechTask/Provider/Usage 合同生成与同步检查；0009-0018 迁移、任务状态/缓存/用量事务测试；持久任务元数据、并发用量累加和过期拒绝测试 | 后续配额和兼容迁移的生产升级验收 |
| 12 安全与治理 | 受限变量、无任意脚本、受保护资源、Embed 基础边界；解码限制与请求取消回收；上传者及授权字段；供应商凭据 AES-GCM 信封、治理审计和数据保留清理；前后端同步的敏感字段命名变体拦截；禁播词、敏感信息正则、人工审核在计划/草稿/执行关口强制校验；可注入病毒扫描接入点 | 数据出站提示、实际扫描器部署、资源处理硬隔离和生产审计移交第四阶段 P4-DH-004 |
| 13 运维 | 前端任务状态/错误事件及 request ID；服务端 worker、独立 worker 入口、播放诊断接口、健康历史、熔断、成本报表和保留清理；API 启动时将中断的 `running/queued` 记录标记为失败，不自动重播；独立 worker 不恢复 API 留下的排队任务；管理指标接口提供跨进程持久任务/用量及合成耗时计数；诊断页支持失败任务一键重试并继续服从治理策略 | 生产指标导出/告警、结构化日志采集落地、跨主机容量/调度和完整重启演练移交第四阶段 P4-DH-004 |
| 14 功能门禁 | 当前基础功能与资产单元测试；三浏览器数字人 24 项 E2E；字幕 cue 合同、SDK、Web、后端全量回归 | 所有启用能力逐条对应验收，不能以现有 E2E 代替 |
| 14 性能稳定性 | Chromium 1920x1080 首个运行时状态 476 ms、首个可读字幕 590 ms、查询响应到字幕 49 ms；60 秒 soak 记录堆/网络/队列采样 | 目标环境首帧/字幕基线、8 小时长播、真实媒体故障、大资源、内存/CPU/音频上下文/队列趋势移交第四阶段 P4-DH-003 |
| 14 安全兼容性 | Chromium/Firefox/WebKit 页面与真实视频音频回归；4K 仿真、CSP、Origin/Ticket、键盘和无音频降级已有自动化证据 | 物理高 DPI、完整屏幕阅读器、跨域媒体和专用供应商测试移交第四阶段 P4-DH-002 |
| 15 S0-S6 | S0-S5 已形成实现基础；S6 的目标环境与生产成熟度门禁移交第四阶段 | 按第四阶段 P4-DH-001～004 执行，不再作为第三阶段完成条件 |
| 16 后置范围 | 不实现原规格明确排除的能力 | 维持边界；不扩展数据库直连、开放聊天、3D 或跨屏设备管理 |
| 17 最小可演示闭环 | 无 AI/无供应商静态字幕、发布与嵌入已运行；编辑器已接入 TTS 计划/任务，支持试听并明确采用生成音频；deterministic fake TTS 三浏览器 E2E 已覆盖生成、确认、发布、独立播放和 Embed | 真实供应商端到端、供应商故障/语言降级和目标环境证据移交第四阶段 P4-DH-001～003；fake fixture 不作为生产验收证据 |

## 本轮新增证据

2026-09-09，本轮补齐媒体资产管理及编辑器资源库。

- `pnpm verify` 通过：SDK 19 项、Web 182 项、后端 545 项；13 项连接器集成测试未运行。合同同步、类型检查、生产构建通过，仍有第三方注释和主包大小警告。
- 后端新增证据覆盖异常标准输出/错误输出实时限长、进程取消和超时回收、图片线程结束前保持处理槽、坏字幕不静默丢失、时钟回拨下版本排序、共享文件保留、缩略图计入容量、时长/记录数限额以及其他工作进程占用处理槽。
- 迁移验证在独立临时数据库执行 `0008 -> 0009 -> 0008 -> head`，旧资源 ID、路径、哈希和创建时间均保留，版本约束与 SHA 索引存在。
- 资源库新增 9 项前端测试，验证筛选分页、明确选用新版本、引用删除保护、删除二次确认、元数据保存、类型限制、失败状态、请求取消、焦点恢复和媒体卸载。
- 三浏览器最终 E2E 9 项通过：新增实际 PNG/WAV 上传、预览解码和播放进度、版本替换、线上旧资源原始字节保留、桌面/390px 布局及媒体停止验证。录制音频来自测试生成器，不含真实客户音频。
- 初次 E2E 暴露了经管道生成的 WAV 缺少完整文件头、跨项目复用夹具造成版本号递增的测试假设错误；修正夹具后验证通过，没有放宽生产解码校验。
- 截图检查推动工具栏宽度约束、固定底部操作区和新资源选用后的滚动复位；Safari 鼠标点击不自动聚焦的问题通过显式记录触发控件解决。
- 本轮新增/修改的媒体、资产、发布、播放 API、合同、迁移与测试 Python 文件 Ruff 通过。
- 最后一次滚动/焦点调整后，资源库 9 项单测、完整类型检查、生产构建及三浏览器 9 项 E2E 再次通过。

本轮主要命令：

```sh
pnpm verify
pnpm test:e2e digital-human.spec.ts --project=chromium --project=firefox --project=webkit --output=test-results/digital-human-assets-20260909-final
pnpm --filter @datapulse/web test src/features/screens/assets/AssetLibraryDialog.test.ts
pnpm typecheck
```

最终截图与追踪输出位于 `apps/web/test-results/digital-human-assets-20260909-final/`。
E2E 运行器在退出时清理自己的测试数据库；本地预览的独立数据没有被清理。

### 此前 SDK 验证记录

2026-09-09，前一轮重点补齐 SDK 与授权生命周期。

- SDK：19 项测试通过，覆盖非法命令、请求取消/超时/销毁、错误组件响应、过期拒绝和旧播放器能力协商。
- Web 全量：173 项测试通过；包含数字人任务生命周期、暂停恢复不重复开始、迟到结束回调、票据计时器/焦点/命令路径过期。
- 后端全量：499 项通过，13 项连接器集成测试未运行；合同与发布专项共 67 项通过。
- `pnpm verify` 通过合同同步、上述全量测试、类型检查和生产构建。构建仍有第三方注释及主包大小警告。
- Ruff 对新增数字人合同、模板、Embed 合同和数字人后端测试检查通过。
- 三浏览器基础 E2E：6 项通过，覆盖独立播放、查询结果复用、已发布草稿隔离、编辑器配置入口、嵌入控制和不存在组件错误。
- 初次截图发现浅色测试主题没有显式 Token、字幕复选框继承通用输入框尺寸；已补充正确的浅色 Token、局部文字色覆盖和固定尺寸控制样式。修正后的三浏览器 6 项测试通过，桌面/窄屏/编辑页截图已复查；窄屏实际字号与编辑器长面板仍未完成可用性验收。

前一轮命令：

```sh
pnpm verify
pnpm test:e2e digital-human.spec.ts --project=chromium --project=firefox --project=webkit --output=test-results/digital-human-20260909-v2
uv run --package datapulse-server pytest apps/server/tests/contracts/test_digital_human.py apps/server/tests/contracts/test_embed.py apps/server/tests/screen/test_digital_human.py
```

E2E 使用独立临时数据库和测试凭据，运行器在结束后清理对应临时数据。
测试结果保留于 `apps/web/test-results/digital-human-20260909-v2/`，不含真实供应商或客户数据。
本地测试目录属于可再生证据，不能替代正式交付时归档的验收报告。

## 本地预览

- 前端：`http://127.0.0.1:5179/studio`；后端：`http://127.0.0.1:8000`。
- 独立测试数据目录：`/tmp/datapulse-phase-three-dev.qui9tM`，没有使用用户原有数据库。
- 首次设置码为仅供此本地测试实例使用的 `phase-three-local-test`。
- 本轮已将该测试数据库从 `0008` 升级到 `0009`，重启后端并验证前端 HTTP 200 和后端认证状态接口；当前测试实例尚未初始化管理员。
- 重启时更换了仅用于该实例的临时签名密钥，旧测试播放会话/Embed 票据需要重新签发。
- 这两个进程仅监听回环地址，未连接真实 AI/TTS 供应商；临时目录和服务不是生产部署。

## 本轮新增证据

2026-09-09，本轮补齐预录媒体一致性与真实播放时钟。

- `SpeechRecording` 合同现在固定媒体 ID、SHA-256、确认文案、真实时长、可选字幕 ID/哈希和经过校验的 cue；字幕必须有序、不重叠、落在媒体时长内，并与确认文案一致。
- 发布校验会验证预录音频/视频的资源类型、磁盘哈希、解码元数据、音轨、时长和字幕快照；更换媒体或字幕后必须重新确认，未确认内容不能进入发布快照。
- 播放驱动支持受控的既有 `<video>` 元素，报告真实媒体时间、媒体时长、Speech Synthesis boundary 和实际活跃播放秒数；暂停、缓冲、静音和取消不会继续消耗时长预算或触发旧回调。
- 播报来源可选择自动、浏览器语音、预录音频和视频原声。当前渲染话术与确认录音不一致时自动回退到当前文本，并保留稳定错误码。
- 编辑器新增录音确认与字幕时间轴编辑器，可试听、导入已验证 WebVTT、手动调整 cue、校验边界并明确确认；媒体替换后确认状态失效。
- 新增单元测试覆盖 9 个播放驱动场景、录音合同、发布资源一致性、编辑器确认和话术不匹配回退。新增真实 MP4 E2E 在 Chromium、Firefox、WebKit 全部通过：播放画面非空、原声未静音、真实字幕时间轴、暂停恢复、静音恢复、移动窄屏和编辑器确认。

2026-09-09，本轮补齐服务端播报协议的持久化边界。

- 新增 `DigitalHumanProvider`、`SpeechPlan`、`SpeechTask`、`DigitalHumanUsage` 合同、SQLAlchemy 模型和 `0010_digital_human_speech` 迁移。
- 管理端新增供应商列表、创建、修改、删除、配置测试、话术计划预览、任务入队和用量查询接口；供应商密钥通过现有 AES-GCM 信封保存，不进入响应或屏幕文档。
- 新增仓储、合同、迁移和导出 Schema 测试；完整后端回归覆盖 572 项非连接器测试。
- OpenAI-compatible provider 已增加受控 `/models` 连接测试，凭据使用 AES-GCM 信封解密后只放入请求头；HTTP 失败、未配置、禁用和不支持类型均返回稳定错误码，并由生命周期统一关闭 HTTP 客户端。
- OpenAI-compatible provider 已接入 `/audio/speech` 合成；任务在后台执行，使用内容哈希查找已成功任务对应的资产，生成结果通过现有 `AssetService` 做格式/解码/配额校验后入库。
- 任务状态会从 `queued` 进入 `running`、`succeeded` 或 `failed`，支持任务查询；成功、缓存命中和失败会按供应商写入每日持久用量，合成失败会使用稳定错误码并执行有限退避重试。
- 新增真实执行单测覆盖请求体、加密凭据、音频资产写入、任务状态和生成时长，并覆盖语音列表去重和任务取消；后端非连接器回归为 578 项通过。
- 新增 `digital_human_settings` 单例迁移和管理合同/API；服务端在任务入队及生成完成时执行总开关、组件冷却、每日任务数、每日音频秒数和单条时长限制，排队任务也计入当天任务限额。
- 合同导出、迁移表断言、设置持久化和配额拒绝测试已覆盖；后端非连接器回归更新为 582 项通过。
- 系统设置页已替换占位工作区，接入数字人策略、供应商和用量 API；新增 2 项页面测试，全量 Web 测试 194 项通过，Web 类型检查和生产构建通过。
- 生命周期启动时会将中断的 `queued/running` 任务标记为 `failed/SPEECH_TASK_INTERRUPTED`，不自动重复播报；启动清理和重复播放防护已加入仓储测试。
- AI 草稿服务端现在拒绝未允许变量、敏感字段、非法模板和脚本/危险 SQL 内容；编辑器预览不会自动写回草稿，只有明确点击应用才更新话术。
- 设置页增加最近 50 条播报任务诊断表，展示状态、资产、错误码和时间；服务端任务列表支持状态筛选与数量上限。
- 设置页供应商表支持编辑已保存配置、拉取语音列表并展示可选声音；API 密钥编辑时仍不会回显旧密钥。
- 字幕事件协议已贯通服务端合同、导出 JSON Schema、SDK 和 Web 运行时：`subtitleCue` 必须携带完整 cue 索引/文本/起止时间，时间必须严格递增；普通生命周期事件不携带不完整 cue。合同专项 58 项、SDK 20 项、Web 194 项和后端非集成测试 588 项通过，13 项连接器集成测试未运行。

### 2026-09-09：治理、成本、健康与临时媒体预览

- 新增供应商健康历史和连续失败计数；连续 3 次失败打开 60 秒熔断，成功检查会清零并关闭熔断。健康检查失败会写入审计记录。
- 供应商 `provider_version` 纳入话术内容哈希和缓存查询；修改供应商版本后不会复用旧版本音频。
- 用量记录增加按供应商的预计成本，新增成本报表接口和设置页展示；成本按生成秒数与供应商每分钟单价估算，缓存命中不重复计费。
- 设置页新增 AI 开关、模型覆盖、上下文变量上限和诊断数据保留天数；审计覆盖供应商、设置、播报失败、资源变更、健康失败和屏幕发布操作，敏感话术、查询结果和密钥不写入审计详情。
- 保留清理覆盖健康历史、用量、任务、话术计划和审计记录；服务端 worker 启动/停止由应用生命周期管理。启动时 `queued/running` 任务统一标记为 `failed/SPEECH_TASK_INTERRUPTED`，不会自动重播旧任务。
- 新增 `GET /api/admin/assets/{asset_id}/preview` 临时预览入口：音频转为 64 kbps MP3，视频缩放到 640 宽并编码为低码率 MP4；可选 `loudnorm` 和 `silenceremove`，输出不写入资源库。
- 新增预览单元测试验证 MP3/MP4 实际输出、滤镜参数、尺寸/码率和 20 MiB 输出上限；媒体专项测试当前 28 项通过，媒体/资产管理专项合计 44 项通过，Ruff 检查通过。
- 新增内容治理设置与 `0017_digital_human_content_governance` 迁移：禁播词、敏感信息正则和人工审核开关持久化；服务端在计划创建、AI 草稿、任务入队和合成前重复校验，人工审核任务必须通过显式 `approved=true` 放行；新增合同、服务和执行后复核测试。
- 新增 `speech.ai_draft_succeeded/failed` 审计事件：仅记录操作、阶段、模型标识、稳定错误码和耗时，不记录完整输入/输出话术或数据样例。
- 设置页增加近 30 天资源授权到期提醒和失败任务一键重试；重试复用服务端任务入队接口，不绕过内容治理、冷却、限额、人工审核或供应商熔断。

### 2026-09-11：长期播放门禁纳入数字人

- 长期播放目标屏现在包含绑定 KPI 查询结果的数字人组件，使用真实组件数据计算话术并渲染字幕；组件设置为静音且不自动播放，避免 soak 门禁产生音频副作用。
- Chromium 4K 高 DPI 显示 E2E 通过，验证发布播放中的数字人字幕存在、画布无水平/垂直溢出；物理 Retina 测试在当前无 Retina 的运行环境中 `devicePixelRatio=1`，该环境门禁未通过，不作为数字人功能失败证据。
- Chromium 60 秒长期播放 soak 通过：49 次查询刷新、`failedRequests=[]`、`pageErrors=[]`、`consoleErrors=[]`，数字人字幕断言随每次刷新执行；JS 堆增长 1.66 MB（24.86 MB -> 26.52 MB）。该结果是短时门禁证据，不能替代规格要求的 8 小时长播及 CPU/网络/音频上下文/队列趋势验收。
- 本次 `pnpm verify` 通过：合同同步、Embed SDK 20 项、Web 216 项、后端 647 项、类型检查和生产构建；构建保留第三方注释及大包大小警告。

## 第四阶段移交

以下门禁不再阻塞第三阶段完成，统一按第四阶段目标环境与生产成熟度计划执行：

1. 真实 Azure/custom TTS、供应商侧语言降级、限流和故障恢复（P4-DH-001）。
2. 真实媒体跨域、完整屏幕阅读器、物理高 DPI 和三种播放模式的目标环境矩阵（P4-DH-002）。
3. 目标环境首帧/字幕基线、8 小时长播、真实媒体故障和资源趋势（P4-DH-003）。
4. 生产指标/告警、结构化日志、重启演练、跨主机调度、实际扫描器和处理隔离（P4-DH-004）。

## 已知重要限制

- 预录视频已经支持原声和真实字幕时间轴；无确认媒体或当前话术不匹配时会回退到浏览器语音/字幕，并保留诊断码。
- 无时间轴的浏览器语音仍使用句子估算和 Speech Synthesis boundary；真实音频/视频使用媒体时钟。
- 页面内数字人限额仍在浏览器运行时；服务端已接入全局开关、任务数/音频秒数/单条时长限额及真实合成、缓存命中、失败任务计量和成本报表，但生产成本口径仍需财务/供应商账单校准。
- 媒体上传已进行真实解码，但不代表全部编码变体、透明视频或所有浏览器都可播放；生产镜像和媒体兼容矩阵仍需验收。
- 图片解码使用受像素/输入大小限制的工作线程，取消时等待线程完成；尚无独立进程硬超时/沙箱。跨进程处理配额依赖共享资产目录的操作系统文件锁，不等于跨主机集群调度。
- 字幕 cue 已绑定预录音视频的真实媒体时钟，并可通过 Embed SDK `subtitleCue` 事件提供给宿主；独立字幕样式/字号下限和完整无障碍矩阵仍需验收，资源到期主动查询与提醒已完成。
- 窄屏大屏画布整体缩放后，文字与按钮仍偏小；“没有溢出”不等于可读性验收通过。
- 现有通过的测试证明第三阶段本地功能闭环和回归通过，不替代第四阶段目标环境与生产成熟度验收。
- OpenAI-compatible、Azure Speech 和 custom 受限协议均已有服务端适配器与本地 HTTP mock 回归；真实供应商端到端、生产指标导出/告警和长期趋势属于第四阶段。独立持久 worker 已有入口、优先级/FIFO 调度和并发领取保护，跨主机部署演练归入 P4-DH-004。
- AI 话术已增加预览接口，支持生成、改写、缩短和翻译操作的结构化返回；上下文只传递显式允许的变量名，结果标记为 `preview_only`，不会自动修改草稿或发布。
