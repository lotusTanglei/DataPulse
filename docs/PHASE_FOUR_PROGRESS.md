# 第四阶段实现与验收进度

开始日期：2026-09-21。状态：开发与本地验证完成，目标验收未完成；八项目标门禁阻塞。

## 基线与范围

工作基线为 `37a11b0` 加当前全部第三阶段未提交/未跟踪实现；开发分支为 `codex/phase-four-maturity`，原工作区内容保留。基线 `pnpm verify` 已通过：Embed SDK 20、Web 227、后端 672；15 项 integration 未在该命令执行。该基线记录不替代变更后的验证。

已确认产品共四阶段；旧文档的五阶段标题及早期排除条目已按本阶段设计修正。对话数字人仍属独立候选。

| 需求 | 现有能力 | 实际缺口 | 本轮交付与验证 | 状态 |
| --- | --- | --- | --- | --- |
| 用户与权限 | 管理员 Session/CSRF、哈希密码 | 用户生命周期、角色、资源授权、撤销审计 | 追加迁移、统一服务端策略、管理界面与跨用户负向回归 | 已实现，本地回归通过 |
| 编辑协作 | revision 原子保存、409 停止重试 | 资源共享、跨账号场景 | 复用现有冲突机制并验证不同账号 | 已实现，三浏览器双账号协作回归通过 |
| 市场 | 5 个内置模板 | 安装包、目录、版本、引用保护 | 离线包市场及模板应用闭环 | 已实现，本地回归通过 |
| 插件 | manifest 合同 | SDK、加载器、配置、发布与播放依赖 | 示例插件与端到端回归 | 已实现，本地回归通过 |
| 备份恢复 | Docker 数据目录、迁移 | 一致归档、校验、恢复、演练 | CLI、隔离恢复、故障用例及手册 | 已实现，本地回归通过 |
| 运维安全 | worker、持久指标、Prometheus、媒体处理 | 缺口核查、扫描接入、生产演练 | 实现已证实缺口并提供部署验证材料 | 已实现；Linux、最终容器及本地恢复验证通过 |
| 目标环境 | 八项关闭标准与部分本地预检 | 真实设备、账号、网络和完整窗口 | 逐项独立记录证据 | 8 项阻塞，0 项通过 |

设计见 `docs/superpowers/specs/2026-09-21-phase-four-identity-design.md`、`2026-09-21-phase-four-ecosystem-design.md` 与 `2026-09-21-phase-four-operations-design.md`。

执行顺序：基线 → 身份权限基础 → 资源授权与管理界面 → 市场/插件完整集成 → 备份恢复与运维 → 全量回归及可用环境演练。可独立实现的模块限定文件范围并行，公共注册、生命周期和生成合同统一集成。

## 2026-09-21 本地集成记录

用户明确表示目标环境尚未准备，先完成开发和本地验证；八项门禁保留阻塞。

- 数据库连接器：PostgreSQL/MariaDB 容器集成 **13 passed、12 deselected**，退出 0。
- 新生态流程：Chromium/Firefox/WebKit **3 passed**，涵盖插件安装、数据绑定、发布、独立播放、Embed 和升级保留旧版；后续最终三浏览器各 26 项完整 E2E 已通过。
- 双账号协作：首次发现选择框标签不精确，修复界面 accessible name 后 Chromium **1 passed**。旧失败日志保留。
- 文件/媒体扫描、结构化请求日志、条件 CSP 与 FFmpeg 资源边界已接入，局部回归通过；本地 fixture 扫描不等于实际 ClamAV 或生产隔离验收。
- 整体 `pnpm verify`、容器全新启动、干净恢复与最终审查已通过。

## 收尾回归

- 完整 `pnpm verify` 已通过：后端 844 passed / 15 deselected、Web 281 passed、Embed SDK 20 passed，合同、类型和生产构建通过。
- 最终 Chromium 26、Firefox 26、WebKit 26 全部通过；三个浏览器均实际执行数字人 fixture 及真实后端 Embed CSP 下的插件测试。
- 运行器修复了不同浏览器共享数据库造成的名称冲突；截图基线已人工核对新增导航和数字人组件后更新。
- 独立审查发现的两项备份引用缺陷已修复并复审；备份、数字人 CLI 恢复和运行器定向复验 47 项通过。
- Docker 镜像已构建，146 个后端源码/迁移文件与工作区哈希一致。使用缓存 Playwright 构建镜像 Node 24，默认 Node 22 镜像下载未完成，不计为通过。
- 50 客户端容量预检发现的重复解析 500 已修复，保持 2 CPU/4 GiB/256 PID 和 3 秒 p95 门槛。最终镜像 250/250 查询成功，p95 590 ms，备份恢复与再次重启通过。
- 重复取消的进程清理缺陷修复后，最终后端全量 **848 passed / 15 deselected**；处理器/缓存/扫描器定向 **27 passed**；三浏览器文件流程再次 **6 passed**。

证据目录：`test-evidence/phase-four/local-20260921/`。最终准确计数与剩余限制见 [交付记录](PHASE_FOUR_CLOSEOUT.md)。
