# DataPulse 大屏组件系统与演示数据第 02 次测试计划

> 产品阶段归属：第二阶段（数据理解与融合创作）。本计划覆盖 5 个模板、当前 21 类组件、演示/静态/mock 数据、统一运行时和编辑器内 AI 局部修改；数字人播报属于产品第三阶段。

## 本次目标

验证提交 `f76699d` 引入的大屏组件体系和演示数据能力：组件注册表扩展、模板演示数据、静态/模拟数据到 `QueryResult` 的适配、编辑器组件库和属性面板、组件级运行时错误隔离，以及含演示数据大屏的发布、播放和嵌入兼容性。优先覆盖 P0/P1 的创建、编辑、预览、发布和运行时数据一致性流程。

本轮在用户授权下修复测试中发现的播放器兼容问题和 E2E 定位问题，并在隔离环境完成回归；未修改无关的业务代码、项目配置或真实数据。

## 当前代码和版本依据

- 项目：DataPulse；代码路径：`/Users/dundundebaba/sync/code/open/DataPulse`。
- Git：分支 `main`，HEAD `f76699d`（`feat: expand screen components and demo data`）；当前为该提交加工作树修复，工作区保留未跟踪 `.codegraph/`、`.idea/` 和测试证据。
- 维护文档：`/Users/dundundebaba/sync/code/open/DataPulse/docs/TESTING_SESSION.md`、`/Users/dundundebaba/sync/code/open/DataPulse/docs/known-issues.md`；设计依据为 `docs/superpowers/specs/2026-08-31-screen-component-system-and-demo-data-design.md`。
- 未发现仓库内 `AGENTS.md`、`CONTRIBUTING*` 文件；已阅读根 `README.md`、`package.json`、`pnpm-workspace.yaml`、`pyproject.toml`、`Dockerfile`、`compose.yaml`、`compose.test.yaml`、Web Vite/Vitest/Playwright 配置和上一份测试计划。
- 变更覆盖：`apps/web/src/features/runtime/registry.ts`、`mockData.ts`、`dataRuntime.ts`、`ComponentHost.vue`、`ScreenRuntime.vue`；`apps/web/src/features/runtime/builtins/*`；`apps/web/src/features/screens/editor/ComponentLibrary.vue`、`InspectorPanel.vue`、`ScreenCanvas.vue`、`templates.ts`；服务端 `screen/publishing.py`、`screen/runtime.py`、`screen/chart_query.py`、`ai/edit.py`、`ai/screen_generator.py`；对应 screen、AI、schema 测试。
- 当前注册表实际包含 21 类内置组件（文本、图片、面板、分割线、数字翻牌、KPI、表格、进度、仪表盘、状态矩阵、折线、柱状、饼、雷达、热力、散点、漏斗、排行榜、告警列表、时间线、地图）；模板为 5 个且数据组件带确定性 mock binding。

自动化覆盖矩阵：正常（AUTO-UNIT-001/002、AUTO-INT-001/002/003）、边界（mock/static 行列和配置上限、参数/字段校验、容器重启）、异常（AI 非法输出、发布校验、组件查询失败）、权限/安全（管理员/CSRF、只读连接器、发布与运行时授权）、失败恢复（取消旧查询、组件级错误隔离、发布快照和容器重启）、数据一致性（seed 确定性、模板副本 ID、发布文档与草稿）。均有对应自动化用例；不适用及原因：无。

## 测试环境与前置条件

- 主机：macOS Darwin arm64；Node.js `v22.23.1`；pnpm `10.33.0`；uv `0.11.6`；Docker `29.4.0`（OrbStack，服务可用）。
- 依赖已安装：根 `node_modules`、`.venv`、Playwright Chromium headless shell；E2E runner 会临时生成 SQLite 数据目录和本地 fake AI 服务。
- 本次实际使用的临时端口：Playwright 自动分配后端端口（日志为 `52590`）和前端端口；容器烟囱检查使用 `18082`；Compose 数据库使用 PostgreSQL `55432`、MariaDB `53306`。
- 本轮黑盒实际启动端口：后端 `127.0.0.1:18080`，Vite 前端 `127.0.0.1:5180`；健康检查和浏览器操作均通过该端口执行。
- 账号：只使用 E2E/临时管理员和 Compose 测试账号；不使用生产账号、生产数据库、真实密码、真实 API Key、真实 Embed Ticket 或真实业务文件。密钥只在进程环境变量中生成，不写入日志或计划。
- AI：E2E 使用 `tools/run_e2e.py` 启动的本地 fake provider，不访问外部模型；人工 AI 用例必须使用同类隔离服务或标记阻塞。
- 数据：使用 `apps/web/e2e/fixtures/` 生成的 `sales.db`、中文 CSV/Excel/JSON/Parquet fixture 和内存静态/mock 数据；不得把真实敏感数据写入截图、日志或 Network 证据。
- 清理：E2E runner 自动清理 `.e2e` 临时目录；Compose 测试后执行 `docker compose -f compose.test.yaml down -v`；容器烟囱测试删除专用容器/volume；人工测试删除临时屏、数据源、文件和浏览器上下文。

## 自动化测试

### 单元测试

- AUTO-UNIT-001：Web 运行时、组件注册表、演示/静态数据和编辑器元数据
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web/src/features/runtime/{registry,mockData,dataRuntime,runtime}.ts`、内置组件、`ComponentLibrary.vue`、`InspectorPanel.vue`、`templates.ts` 及其测试
  - 执行命令：`pnpm --filter @datapulse/web exec vitest run src/features/runtime/builtins/basic-components.test.ts src/features/runtime/builtins/chart-components.test.ts src/features/runtime/mock-data.test.ts src/features/runtime/runtime.test.ts src/features/runtime/data-runtime.test.ts src/features/screens/editor/component-library.test.ts src/features/screens/editor/inspector.test.ts src/features/screens/templates.test.ts`
  - 前置条件：已执行 `pnpm install`；使用仓库锁定依赖和 happy-dom。
  - 预期结果：组件定义、21 类注册、主题/状态、确定性 seed、静态数据校验、数据请求去重/取消、属性面板和模板副本测试通过。
  - 实际结果：8 个测试文件、45 个测试全部通过，退出码 0；日志见 `test-evidence/automation-20260831/AUTO-UNIT-003-web-runtime.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-UNIT-003-web-runtime.log`

- AUTO-UNIT-002：完整非集成单元回归
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：Embed SDK、全部 Web Vitest、服务端非 `integration` pytest（含 screen/AI/contract/auth/query）
  - 执行命令：`pnpm test`
  - 前置条件：Node/pnpm/uv 依赖可用；不要求外部 PostgreSQL/MariaDB。
  - 预期结果：Embed SDK、Web 和后端非集成测试均通过；集成测试只因 marker 被排除，不得误计为通过。
  - 实际结果：原始 `pnpm test` 为 Embed SDK 5/5、Web 30 文件 138/138、后端 447/447 通过；修复后追加 `pnpm --filter @datapulse/web test`，Web 30 文件 139/139 通过，退出码 0。后端 13 个 integration 项按命令被 deselect；Embed SDK 有 happy-dom iframe abort stderr，但无失败测试。日志见 `test-evidence/automation-20260831/AUTO-UNIT-002-test.log`、`AUTO-UNIT-004-web-rerun.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-UNIT-002-test.log`

### 集成/API 测试

- AUTO-INT-001：大屏发布、运行时查询、运行时 API 和图表查询编译
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/src/datapulse/screen/{publishing,runtime,chart_query}.py`、`apps/server/tests/screen/` API/service 测试
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/screen/test_publishing.py apps/server/tests/screen/test_runtime.py apps/server/tests/screen/test_runtime_api.py apps/server/tests/screen/test_chart_query.py -q`
  - 前置条件：临时 SQLite/内存 repository fixture；不连接生产数据源。
  - 预期结果：旧 chart binding 兼容；mock/static binding 可发布但非法配置被拒绝；草稿/未保存文档/已发布文档查询授权、参数和错误映射正确。
  - 实际结果：38 个测试通过，退出码 0；日志见 `test-evidence/automation-20260831/AUTO-INT-001-screen-api.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-INT-001-screen-api.log`

- AUTO-INT-002：AI 屏幕生成、局部编辑和 schema/API 错误契约
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/src/datapulse/ai/{screen_generator,edit,api,service}.py`、chart/dashboard contracts
  - 执行命令：`uv run --package datapulse-server pytest apps/server/tests/ai/test_screen_generator.py apps/server/tests/ai/test_edit.py apps/server/tests/ai/test_api.py apps/server/tests/contracts/test_chart.py apps/server/tests/contracts/test_dashboard.py -q`
  - 前置条件：使用 fake gateway fixture；不发送外部 AI 请求。
  - 预期结果：合法屏幕/编辑命令通过；未知组件、越界/锁定组件、非法字段/绑定和服务错误被稳定拒绝；契约与生成类型一致。
  - 实际结果：31 个测试通过，退出码 0；日志见 `test-evidence/automation-20260831/AUTO-INT-002-ai-contracts.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-INT-002-ai-contracts.log`

- AUTO-INT-003：PostgreSQL/MariaDB 连接器集成和只读/超时边界
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`apps/server/tests/connectors/test_postgresql.py`、`test_mysql.py`、`compose.test.yaml`
  - 执行命令：`docker compose -f compose.test.yaml up -d --wait`；`DATAPULSE_TEST_POSTGRES_URL='postgresql+asyncpg://datapulse:datapulse@127.0.0.1:55432/datapulse' DATAPULSE_TEST_MYSQL_URL='mysql+asyncmy://datapulse:datapulse@127.0.0.1:53306/datapulse' pnpm test:integration`；清理：`docker compose -f compose.test.yaml down -v`
  - 前置条件：Docker daemon、Compose 镜像和 55432/53306 端口可用；只使用 compose.test.yaml 的测试账号和临时 schema/table。
  - 预期结果：连接、catalog、类型映射、参数查询、空结果、超时和绕过 validator 的写入保护通过；测试 schema/table 清理完成。
  - 实际结果：两个服务均 Healthy；13 个 integration 测试通过，12 个非 integration 项被 deselect；退出码 0；已执行 `down -v`，未留下服务。证据：`test-evidence/automation-20260831/AUTO-INT-003-compose-up.log`、`AUTO-INT-003-connectors.log`、`AUTO-INT-003-compose-down.log`。
  - 问题编号：无
  - 失败后证据：上述三个日志路径

### E2E、构建、静态检查和视觉回归

- AUTO-E2E-001：生成代码与 schema 一致性检查
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`packages/schema/schemas/`、生成的 TypeScript 声明、`tools/check_generated.py`
  - 执行命令：`pnpm check:contracts`
  - 前置条件：uv 环境和 Node 依赖可用。
  - 预期结果：导出的 schema、生成声明和仓库文件一致，退出码 0。
  - 实际结果：退出码 0，无不一致输出。证据：`test-evidence/automation-20260831/AUTO-UNIT-001-contracts.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-UNIT-001-contracts.log`

- AUTO-E2E-002：TypeScript/Vue 类型检查
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：Web、schema、Embed SDK 的 `tsc/vue-tsc` 配置和新增组件类型
  - 执行命令：`pnpm typecheck`
  - 前置条件：Node/pnpm workspace 依赖已安装。
  - 预期结果：Web（含 e2e tsconfig）、schema 和 SDK 类型检查退出码 0。
  - 实际结果：全部通过，退出码 0。证据：`test-evidence/automation-20260831/AUTO-E2E-001-typecheck.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-001-typecheck.log`

- AUTO-E2E-003：Web 生产构建
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web` Vite production build、组件动态导入/样式资源
  - 执行命令：`pnpm build`
  - 前置条件：类型检查依赖和 workspace 包可解析。
  - 预期结果：构建退出码 0，生成 `apps/web/dist`；不得有模块解析或类型错误。
  - 实际结果：构建退出码 0；Vite 报告两个大 chunk（约 438 kB、1,335 kB）提示，但不是失败。证据：`test-evidence/automation-20260831/AUTO-E2E-002-build.log`。
  - 问题编号：无（chunk 提示列入风险）
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-002-build.log`

- AUTO-E2E-004：完整 Chromium E2E、发布/播放和视觉快照
  - 状态：失败
  - 优先级：P0
  - 覆盖代码：`apps/web/e2e/` 的 AI、模板、编辑器、发布、播放和嵌入流程；Playwright 临时服务与 fake AI
  - 执行命令：`pnpm test:e2e -- --project=chromium`（首次基线运行使用 `pnpm test:e2e`）
  - 前置条件：Playwright Chromium 可用；runner 自动创建隔离 SQLite、端口和 fake AI，并在结束清理。
  - 预期结果：16 个 Chromium E2E 全部通过，模板/组件库选择器、发布播放、错误隔离、直连嵌入和快照稳定。
  - 实际结果：修复前 9/15 通过；修复选择器/文案后最终全量 14/16 通过，退出码 1。修复提交 `a97d62a` 增加交互折线的透明数据点命中区域、调整点击坐标并更新 Linux 视觉基线；在全新 Linux 后端数据目录中定向执行受影响的编辑器和播放用例，2/2 通过（28.1 秒，无 `--update-snapshots`）。完整 16 项全量命令和 GitHub CI 尚未在本轮回归，不能据此将本用例改为通过。原始失败日志见 `test-evidence/automation-20260831/AUTO-E2E-010-e2e-final.log`；修复后证据为 `apps/web/e2e/snapshots/linux/editor-shell.png`、`apps/web/e2e/snapshots/linux/letterboxed-player.png`、`apps/web/e2e/snapshots/linux/light-player.png`、`apps/web/e2e/snapshots/linux/component-error-isolation.png`。
  - 问题编号：ISSUE-021
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-010-e2e-final.log`、`apps/web/test-results/screen-editor-administrato-b7823-and-plays-a-complete-screen-chromium/editor-shell-actual.png`、`apps/web/test-results/screen-editor-administrato-b7823-and-plays-a-complete-screen-chromium/editor-shell-diff.png`、`apps/web/test-results/screen-playback-published--4d130-error-and-letterbox-visuals-chromium/dark-player-actual.png`、`apps/web/test-results/screen-playback-published--4d130-error-and-letterbox-visuals-chromium/dark-player-diff.png`

- AUTO-E2E-005：变更相关的可执行 E2E 定向回归
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：AI 非法 JSON/字段/超时、非法屏幕不创建草稿、Embed SDK 参数/安全拒绝
  - 执行命令：`pnpm test:e2e -- --grep 'AI analysis maps malformed|invalid AI screens never create a draft|host SDK controls an embedded screen'`
  - 前置条件：同 AUTO-E2E-004；使用本地 fake AI 和临时 SQLite。
  - 预期结果：错误码/状态码映射正确；非法文档不落库；宿主 SDK ready、参数和 unsafe access 行为稳定。
  - 实际结果：3 个测试通过，耗时约 14.1 秒，退出码 0。证据：`test-evidence/automation-20260831/AUTO-E2E-005-targeted-e2e.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-005-targeted-e2e.log`

- AUTO-E2E-006：Docker 多阶段镜像构建
  - 状态：通过
  - 优先级：P1
  - 覆盖代码：`Dockerfile`、Web build stage、Python runtime stage、workspace 包复制
  - 执行命令：`docker build --tag datapulse-test:20260831 .`
  - 前置条件：Docker daemon 和基础镜像/依赖网络可用；不得使用生产 secrets。
  - 预期结果：frozen lockfile 安装、Web 构建、服务端依赖安装和非 root runtime 镜像均完成，退出码 0。
  - 实际结果：构建成功，镜像标签 `datapulse-test:20260831`；仅出现既有 chunk 大小提示。证据：`test-evidence/automation-20260831/AUTO-E2E-004-docker-build.log`。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-004-docker-build.log`

- AUTO-E2E-007：容器启动、健康检查、SPA 入口和重启保留
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/server/docker-entrypoint.sh`、Alembic migration、`/api/health`、`/studio`、runtime 用户和 `/data` volume
  - 执行命令：
    ```bash
    set -euo pipefail
    container_name=datapulse-plan-20260831-smoke
    volume_name=datapulse-plan-20260831-smoke-data
    port=18082
    trap 'docker container rm --force "$container_name" >/dev/null 2>&1 || true; docker volume rm "$volume_name" >/dev/null 2>&1 || true' EXIT
    master_key=$(python3 -c 'import base64; print(base64.urlsafe_b64encode(b"0123456789abcdef0123456789abcdef").decode())')
    signing_key=$(python3 -c 'import base64; print(base64.urlsafe_b64encode(b"fedcba9876543210fedcba9876543210").decode())')
    docker run --name "$container_name" -d -p "$port:8000" -e DATAPULSE_MASTER_KEY="$master_key" -e DATAPULSE_SIGNING_KEY="$signing_key" -v "$volume_name:/data" datapulse-test:20260831 >/dev/null
    wait_health() { for attempt in $(seq 1 90); do curl --fail --silent "http://127.0.0.1:$port/api/health" && return 0; sleep 1; done; return 1; }
    wait_health
    curl --fail --silent -o /dev/null "http://127.0.0.1:$port/studio"
    docker restart "$container_name" >/dev/null
    wait_health
    docker inspect --format 'user={{.Config.User}}' "$container_name"
    docker container rm --force "$container_name" >/dev/null
    docker volume rm "$volume_name" >/dev/null
    ```
  - 前置条件：AUTO-E2E-006 镜像已生成；18082 未占用；使用可解码为 32 字节的随机测试密钥和专用 volume。
  - 预期结果：迁移后服务以 `datapulse` 非 root 用户启动；health/studio 返回 200；重启后 health 仍 200；日志不含密钥。
  - 实际结果：首次使用错误长度密钥和一次 `python` 别名缺失导致前置启动失败，均已清理并未计入通过；按上述 `python3`/32 字节密钥重跑通过：health 两次 `{"status":"ok","version":"0.1.0"}`、`/studio` 200、runtime 用户 `datapulse`、volume 为专用 `/data`，无 key material 泄露。证据：`test-evidence/automation-20260831/AUTO-E2E-007-container-smoke.log`；前置失败记录：`test-evidence/automation-20260831/AUTO-E2E-007-container-prereq-failures.log`。
  - 问题编号：ISSUE-023、ISSUE-024（前置命令问题，重跑已通过）
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-007-container-smoke.log`

- AUTO-E2E-008：无 `instance_id` 的直连 iframe 嵌入回归
  - 状态：通过
  - 优先级：P0
  - 覆盖代码：`apps/web/src/features/player/PlayerView.vue`、`apps/web/e2e/screen-playback.spec.ts`、`apps/web/src/features/player/embed-player.test.ts`
  - 执行命令：`pnpm test:e2e -- --project=chromium --grep 'direct iframe embeds load'`；单元回归：`pnpm --filter @datapulse/web exec vitest run src/features/player/embed-player.test.ts`
  - 前置条件：E2E runner 的隔离 SQLite、fake AI 和 Chromium；有效临时 Embed ticket；URL 只包含 `ticket`，不包含 `instance_id`。
  - 预期结果：播放器为缺省 URL 生成非空实例 ID，使用 Bearer ticket 请求文档/数据，直连 iframe 显示大屏且不显示播放错误；ticket 仍被地址栏脱敏。
  - 实际结果：浏览器 E2E 1/1 通过；播放器显示 `DataPulse 运营态势总览`，无“无法播放此大屏”，退出码 0。播放器 Vitest 4/4 通过，退出码 0。
  - 问题编号：无
  - 失败后证据：`test-evidence/automation-20260831/AUTO-E2E-008-embed-rerun.log`、`test-evidence/blackbox-20260901/BB-002/08-direct-embed-no-instance.png`

自动化执行汇总：通过 12 项（AUTO-UNIT-001/002、AUTO-INT-001/002/003、AUTO-E2E-001/002/003/005/006/007/008）；失败 1 项（AUTO-E2E-004，包含 2 个视觉快照失败）；未执行 0 项；阻塞 0 项。阻塞或未执行不计入通过。

## 人工黑盒测试

人工覆盖矩阵：正常（BB-001/002）、边界（BB-001/003/006）、异常（BB-003/007）、权限/安全（BB-004）、失败恢复（BB-005）、数据一致性（BB-002/005/008）。不适用及原因：无。以下结果来自 2026-09-01 在隔离服务上的 Chromium 浏览器操作；Firefox/WebKit 需要额外浏览器安装，当前 Playwright 配置仅声明 Chromium。

### BB-001：组件库 21 类组件和无数据演示

- 状态：通过
- 优先级：P0
- 前置条件：隔离服务和临时管理员已初始化；打开大屏编辑器；准备空白大屏且无数据源/数据集。
- 操作步骤：
  1. 打开组件库，按“基础与装饰、指标与状态、图表、列表与分析、地图”逐组查看 21 类组件和缩略预览。
  2. 依次添加每类组件，检查默认位置、尺寸、标题/空态和演示数据能力；至少添加一个单值、表格、序列、雷达和地图组件。
  3. 在编辑器、草稿预览中确认演示数据可见，再刷新编辑器。
- 预期结果：每类组件均可添加且不全部重叠；支持数据的组件显示“演示数据”或明确空态；未知类型只在组件级降级，不导致整页崩溃；刷新后配置保持。
- 需要记录的实际结果：已添加 21/21 类组件；保存前后图层数均为 21，刷新后仍为 21。所有组件 frame 均在 1920 × 1080 画布内，数据组件在无绑定时显示明确空态；图表类默认 frame 存在重叠但未全部重叠。初始化页面未见应用 console 错误。
- 证据要求：编辑器/预览截图、脱敏 Network 和文档 JSON 快照保存到 `test-evidence/blackbox-20260901/BB-001/`。
- 问题编号：无
- 清理动作：已删除临时大屏；证据截图按计划保留，浏览器上下文已关闭。

### BB-002：静态、模拟和真实数据的跨运行时一致性

- 状态：通过
- 优先级：P0
- 前置条件：准备同一大屏中的真实 SQLite 数据绑定、`source=static` 合法 JSON 和 `source=mock` 固定 seed；已发布版本和嵌入 ticket 使用临时密钥。
- 操作步骤：
  1. 在编辑器加载三种数据模式，记录列、行、数值、标题和主题。
  2. 打开草稿预览、独立播放和嵌入播放，逐项比对同一组件结果。
  3. 改变 mock seed、静态数据行数和一个全局参数，分别刷新并再次比对；观察 Network 是否只为真实 binding 发 query。
- 预期结果：同一 binding 在四个运行时显示一致；mock seed 改变产生可区分结果；静态/mock 不发送数据库查询；参数只影响声明的真实查询。
- 需要记录的实际结果：已创建临时 SQLite 数据源和数据集；编辑器、草稿预览和独立播放均显示同一真实查询结果，mock 折线图显示确定性演示数据；独立播放仅对真实数据 binding 发送 1 次 query 请求，mock 未发送数据库 query。修复播放器后，生成的 ticket 在不带 `instance_id` 的直连嵌入 URL 也能加载并显示大屏，地址栏已脱敏。
- 证据要求：脱敏 Network、截图和对比记录保存到 `test-evidence/blackbox-20260901/BB-002/`。
- 问题编号：无
- 清理动作：已撤销/失效临时播放凭据并删除临时屏幕、数据集；数据源删除被历史 `query_run` 外键保护拒绝（HTTP 409），隔离数据源暂保留，未包含敏感数据。

### BB-003：静态数据边界、组件错误隔离和恢复

- 状态：通过
- 优先级：P1
- 前置条件：临时大屏包含表格、图表、地图和一个可故意设置坏字段的组件；准备空列、行长度不符、超过列/行上限、非法 mock preset、缺失 GeoJSON 资源。
- 操作步骤：
  1. 分别提交空 columns、行列长度不符、21 列或 501 行静态数据、未知 preset 和非法 trend。
  2. 将一个图表字段改为不存在的字段并播放，观察其他组件、标题和页面操作。
  3. 修复字段或点击重试，检查坏组件是否恢复且其他组件结果没有被覆盖。
- 预期结果：输入在保存/发布前稳定拒绝并带 request ID；错误只隔离到受影响组件；修复/重试后恢复最新代次结果，无旧参数回写。
- 需要记录的实际结果：`{}` 被拒绝并提示“固定数据需要是一层对象数组，或包含 rows 的数据结构”；501 行数据被拒绝并提示“固定数据需要包含 1-20 列和不超过 500 行”。将表格 binding 改为不存在的数据集后，预览仅该组件显示“数据加载失败”，演示折线图和文本组件仍可用；恢复文档后正常。
- 证据要求：脱敏响应、console、截图和文档前后快照保存到 `test-evidence/blackbox-20260901/BB-003/`。
- 问题编号：无
- 清理动作：已恢复原始文档并删除临时屏幕、坏 binding 和临时资源。

### BB-004：发布、独立播放和嵌入权限/安全

- 状态：通过
- 优先级：P0
- 前置条件：临时管理员、已发布和未发布两张屏；一组正确/错误/过期 display key，Embed API Key、ticket 和允许/拒绝 Origin；不得使用真实密钥。
- 操作步骤：
  1. 以未发布屏、无 key、错误 key、过期 ticket 和跨 Origin 访问独立播放/嵌入接口。
  2. 以正确 key/ticket 打开播放，检查地址栏移除 ticket/key、请求头 Bearer、Cookie/Storage、CSP、`Cache-Control` 和 `nosniff`。
  3. 轮换 API/display key 后重放旧凭据，检查旧会话和新凭据；尝试直接调用管理 API 和缺 CSRF 的变更请求。
- 预期结果：未授权统一拒绝且不泄露文档/凭据；正确凭据只访问被授权屏和参数；轮换立即使旧凭据失效；播放不带管理 Session Cookie；管理变更需要管理员和 CSRF。
- 需要记录的实际结果：无 key 的播放 API 返回 401，错误 key 返回 401；管理员 embed API 缺 CSRF 返回 403，带 CSRF 可签发 ticket。正确 display key 播放成功并从地址栏移除 key。修复后，未包含 `instance_id` 的 iframe 示例直接打开即可加载，`/api/embed/screens` 和 query 均成功；播放器不泄露 ticket。未配置访问限制时，Origin 限制分支不适用。
- 证据要求：脱敏 Network、响应头、截图保存到 `test-evidence/blackbox-20260901/BB-004/`；直连回归截图为 `08-direct-embed-no-instance.png`；不得保存 key/token 原文。
- 问题编号：无
- 清理动作：已轮换临时 embed API key、删除测试屏幕并关闭浏览器上下文；未保存任何凭据原文。

### BB-005：编辑、自动保存、撤销重做和失败恢复

- 状态：通过
- 优先级：P1
- 前置条件：两浏览器上下文打开同一编辑屏；准备可断开的网络代理或 DevTools offline、临时 revision 和演示数据。
- 操作步骤：
  1. 添加组件、改样式/数据绑定，等待“已保存”，刷新并重新进入。
  2. 在上下文 A/B 先后编辑制造 revision 冲突；在保存请求中断网，再恢复网络并手动重试。
  3. 对编辑、AI 局部修改和数据绑定执行撤销/重做，检查发布前后草稿/已发布快照。
- 预期结果：保存状态和 revision 清晰；冲突停止自动覆盖/重试并可重新加载；断网恢复不重复创建；撤销/重做是原子操作；演示数据配置不丢失。
- 需要记录的实际结果：添加文本后先显示“未保存”，约 1.4 秒后显示“已保存”；刷新后图层数保持 3。删除组件后点击撤销恢复图层。使用同一旧 `draft_revision` 保存返回 409 `SCREEN_REVISION_CONFLICT`，缺 CSRF 返回 403。断网恢复和双上下文 UI 操作未执行。
- 2026-09-08 补充结果：通过本地路由可控阻断 6 个数据组件查询，失败终态均含稳定 `QUERY_EXECUTION_FAILED` 和 request ID；恢复路由并重新加载后全部恢复，loading 为 0，无旧错误残留。证据：`test-evidence/blackbox-20260908/BB-005/`。
- 证据要求：脱敏 Network/console、revision 响应和截图保存到 `test-evidence/blackbox-20260901/BB-005/`。
- 问题编号：无
- 清理动作：未改变网络设置；已删除临时屏并关闭浏览器上下文，保留脱敏证据截图。

### BB-006：跨视口、主题和播放视觉回归

- 状态：失败（2026-09-08 跨浏览器视觉门禁）
- 优先级：P1
- 前置条件：已发布包含全部组件和演示数据的大屏；需要 Chromium、Firefox、WebKit；准备 1920x1080、1366x768、1200x900、320px 以上编辑器容器，并设置 `prefers-reduced-motion`。
- 操作步骤：
  1. 在深色、浅色和自定义主题下打开编辑器、预览、独立播放和嵌入播放。
  2. 逐个视口检查 contain 留边、文字/图例/状态、面板边界、滚动和空白；比较模板缩略图与正式预览。
  3. 在减少动效模式下刷新和切换参数，记录动画、4xx/5xx 单组件错误和截图差异。
- 预期结果：无重叠、溢出或空白；播放保持 16:9 和可读性；其他组件在单组件错误时继续可用；减少动效生效。
- 需要记录的实际结果：2026-09-08 Chromium 完整 E2E 16/16，另已核对 1920/1280 编辑器及 1280 深浅播放。浅色 fixture 旧 token 缺陷经 actual/diff 评审后修复并只更新一张基线。Firefox 与 WebKit 已安装并完整执行，各为 15/16；均在 `editor-shell.png` 视觉断言失败，不能判定跨浏览器通过。4K/高 DPI 和真实减少动效人工设备检查仍阻塞。
- 证据要求：Chromium 视口证据位于 `test-evidence/blackbox-20260908/BB-002/`；Firefox/WebKit actual/diff/expected 与脱敏错误上下文位于 `test-evidence/blackbox-20260908/cross-browser/`。原始 trace 可能含临时会话值，未纳入证据。
- 问题编号：ISSUE-031（跨浏览器视觉失败）、ISSUE-032（浅色 fixture，已关闭）。
- 清理动作：关闭浏览器上下文，删除视觉产物和临时屏。

### BB-007：AI 生成/编辑的确认边界和异常降级

- 状态：通过（2026-09-08 deterministic fake AI 等价验收）
- 优先级：P1
- 前置条件：隔离 fake AI 服务；临时数据集和空白屏；准备合法、坏 JSON、非法字段、超时、未授权数据集、超过数据集/问题长度上限的输入。
- 操作步骤：
  1. 生成整屏和局部编辑，先取消预览，再确认应用；观察是否提前改画布或触发发布。
  2. 逐个提交坏 JSON、非法字段、超时和未授权数据集，检查错误码、request ID、草稿和现有组件。
  3. 对成功应用执行撤销/重做、刷新和重新打开。
- 预期结果：预览取消零副作用；确认只写草稿且一次性变更，不自动发布；异常显示可恢复错误，不污染现有文档；撤销/重做和刷新一致。
- 需要记录的实际结果：原 2026-09-01 会话只完成未配置分支。2026-09-08 使用本地 deterministic fake AI 补齐：Web 8/8、Server 26/26、Chromium AI E2E 5/5，覆盖分析成功、整屏/局部预览、取消、确认、一次性撤销/重做、未配置、非法 JSON、非法字段、超时和越权数据集。请求监听确认 AI 结果只写草稿，发布仍需单独确认。
- 证据要求：历史未配置截图保留在 `test-evidence/blackbox-20260901/BB-007/`；补充命令日志为 `test-evidence/automation-20260908/AUTO-022-ai-web-targeted.log`、`AUTO-023-ai-server-targeted.log`、`AUTO-024-ai-e2e-targeted.log`。
- 问题编号：无；未调用真实模型，真实模型主观质量不在本任务授权范围。
- 清理动作：已清理临时 AI 输入和屏幕；本轮未启动 fake AI 服务。

### BB-008：模板复制、发布快照和删除一致性

- 状态：通过
- 优先级：P1
- 前置条件：已初始化管理员；准备一个含 5 模板之一和 mock binding 的临时大屏。
- 操作步骤：
  1. 从每个模板创建副本，检查文档 ID、组件 ID、mock seed 和发布前默认值是否独立。
  2. 发布一次后修改草稿中的标题、主题和演示数据，分别查看草稿、已发布播放和嵌入结果。
  3. 删除操作先取消再确认，刷新列表并检查源数据集/文件未被误删。
- 预期结果：模板副本互不串联；发布快照冻结，草稿修改不影响线上；取消删除无副作用，确认后只删除目标屏；引用数据资源保持一致。
- 需要记录的实际结果：从“总览网格”模板创建成功，生成 9 个图层；复制后屏幕 ID 和组件 ID 独立，副本名称为“BB008 Template Screen 副本”。删除先触发原生确认框，取消后目标仍在，确认后目标消失。已发布屏幕修改草稿标题后，独立播放仍不显示草稿标题，证明 published snapshot 与 draft 隔离。
- 证据要求：API 脱敏 JSON、列表/播放截图和 ID 对照表保存到 `test-evidence/blackbox-20260901/BB-008/`。
- 问题编号：无
- 清理动作：已删除模板源屏、副本和发布测试屏；临时数据集已删除，数据源因历史 query_run 外键保护暂保留。

人工/等价黑盒执行汇总（截至 2026-09-08）：通过 7 项（BB-001/002/003/004/005/007/008）；失败 1 项（BB-006 的跨浏览器视觉门禁）；未执行 0 项；阻塞 0 项。BB-006 内的 4K/高 DPI 子范围另按环境阻塞记录，未计入通过；真实密码管理器、生产代理和长期播放见第二阶段总差距矩阵。

## 测试数据与清理

- 只使用仓库 E2E fixture、`compose.test.yaml` 的临时数据库、随机 mock seed 和人工构造的静态 JSON；不使用生产数据、真实凭据、真实 API 地址、真实客户名称或可识别个人信息。
- mock 数据边界：`row_count` 最大 100、`series_count` 最大 3、`category_count` 最大 10；静态数据最多 20 列/500 行，行长度必须与列一致；这些限制由 `mockData.ts` 和发布校验共同覆盖。
- 外部服务：正常自动化不需要外部网络；E2E 的 fake AI 仅监听本机随机端口；PostgreSQL/MariaDB 仅由 `compose.test.yaml` 提供。
- 删除动作只针对本计划创建的 `.e2e` 临时目录、`datapulse-test` Compose 项目、`datapulse-plan-20260831-smoke*` 容器/volume、临时屏和浏览器数据，不执行宽范围删除。
- 本轮隔离目录为 `/tmp/datapulse-blackbox-20260901`；临时屏幕和数据集已删除，SQLite 数据源因 37 条历史 `query_run` 记录被服务端以 `DATASOURCE_IN_USE`（409）保护，待停止服务后连同隔离目录清理。
- 证据脱敏：密码、master/signing key、API Key、ticket、Session Cookie、绝对文件路径和外部数据库 URL 不写入日志、截图或 JSON；只记录状态码、摘要和哈希/计数。

## 通过标准

1. P0 单元/API、类型检查、构建、容器启动/health 和关键安全契约必须通过；失败或阻塞不得计入通过。
2. 自动化测试命令退出码为 0，且日志中通过数量与汇总一致；被 deselect 的 integration 项必须单独列出。
3. 组件库 21 类、模板 5 个、mock/static/真实 binding、组件错误隔离和发布快照均有可审计证据。
4. E2E 全量和视觉快照在修复/更新测试基线前不能作为发布通过；本轮 AUTO-E2E-004 仍因两个视觉基线失败，不作最终发布结论。
5. 人工黑盒必须在目标浏览器/视口完成并保存截图、Network、console 和数据快照后才能改为通过；BB-007 使用隔离 deterministic fake AI 完成等价审计。BB-006 因 Firefox/WebKit 视觉断言失败，仍不能通过。

## 风险、阻塞和文档不一致

- 全量 Chromium E2E 的旧选择器和旧模板文本问题（ISSUE-018～ISSUE-020）已修复并回归通过；视觉基线 ISSUE-021 仍需产品/设计评审后重新生成。
- README、维护会话和测试计划的组件数量已统一为 21 类；ISSUE-022 已关闭。
- README 密钥示例已改为 `python3`；ISSUE-023 已关闭。容器 smoke 的 32 字节测试 key 前置校验也已固定，ISSUE-024 已关闭。
- 构建产物存在约 438 kB 和 1,335 kB 的 minified chunk 警告；当前不阻断构建，但可能影响首屏性能，后续应评估拆包。
- Firefox/WebKit 已实际执行且各为 15/16，按失败而非阻塞记录，不用 Chromium 结果替代；真实密码管理器 autofill、生产静态 `/embed`/代理 CSP、高 DPI/4K 和长期播放仍因环境条件阻塞。
- 2026-09-01 黑盒发现的 iframe 缺省 `instance_id` 问题（ISSUE-025）已修复：播放器会在缺省时生成内存实例 ID，直连 E2E 和人工复测均通过。
- 临时数据源删除返回 `DATASOURCE_IN_USE`（历史 `query_run` 外键保护），不影响隔离测试服务；服务停止后可连同临时 SQLite 数据目录一并清理。
- 无新增已确认的数据内容缺陷；外部 ERP 中文字段质量问题沿用上一份计划的待确认记录，不在本提交范围内。

## 测试执行结果

- 执行时间、分支/提交、实际环境和执行者：2026-08-31 17:28–17:41 CST（初始自动化）、2026-09-01 10:55–11:25 CST（黑盒）、15:09–15:14 CST（修复前回归）及 17:57–18:06 CST（修复后 Linux 定向回归）；`main` / `a97d62a`；macOS Darwin arm64、Node 22.23.1、pnpm 10.33.0、uv 0.11.6、Docker 29.4.0 OrbStack、`datapulse-e2e-linux:local` Chromium；隔离后端 `http://127.0.0.1:18080`、Vite `http://127.0.0.1:59833`；执行者：Codex。
- 自动化用例汇总：原计划通过 12 项（AUTO-UNIT-001/002、AUTO-INT-001/002/003、AUTO-E2E-001/002/003/005/006/007/008）；失败 1 项（AUTO-E2E-004 的原始全量执行，包含 2 个视觉快照失败）；未执行 0 项；阻塞 0 项。修复后仅定向复跑 AUTO-E2E-004 的 2 个受影响规格并通过，不能计入完整用例通过数；失败或未执行范围不计入通过。
- 2026-09-08 补充汇总：人工/等价黑盒通过 7 项，失败 1 项（BB-006），未执行 0，阻塞 0；BB-006 内的 4K/高 DPI 子范围仍按目标环境阻塞。完整 Chromium 已 16/16，AI 成功/异常路径已用 deterministic fake AI 补齐，Firefox/WebKit 各 15/16。
- 总体结论：第二阶段 8 组功能已有实现，确认未开发功能点为 0；但跨浏览器视觉失败以及真实密码管理器、生产代理/CSP、4K/高 DPI、长期播放环境门禁尚未关闭，不能给出第二阶段完成结论。详见 `docs/PHASE_TWO_CLOSEOUT.md`。

## 发现的问题

- 问题编号：ISSUE-018
- 类型：测试基础设施
- 优先级：P1
- 现象：`ComponentLibrary.vue` 已将图标设为 `aria-hidden`、按钮可访问名称改为组件标签/“数据”，但 3 个 E2E 仍等待旧名称 `＋ 文本`，超时。
- 复现步骤：执行 `pnpm test:e2e`；在 `first-stage-creation.spec.ts`、`screen-editor.spec.ts` 的 `getByRole("button", { name: "＋ 文本" })` 处失败。
- 影响范围：空白创建、完整组件添加、多选拖拽三个 E2E；不等同于手工点击功能已失败。
- 证据：`test-evidence/automation-20260831/AUTO-E2E-003-e2e.log`。
- 状态：关闭
- 后续动作：已按当前可访问名称更新组件库选择器，并在最终 Chromium E2E 中验证相关创建/拖拽流程通过；证据见 `AUTO-E2E-010-e2e-final.log`。

- 问题编号：ISSUE-019
- 类型：测试基础设施
- 优先级：P1
- 现象：AI 分析首项对 `getByLabel("图层").getByText("柱状图", exact)` 使用 strict locator，当前同时匹配组件库和图层项，解析为 2 个元素。
- 复现步骤：执行 `pnpm test:e2e`，`ai-analysis.spec.ts:65` 报 strict mode violation。
- 影响范围：AI 分析成功应用流程未被该 E2E 覆盖；定向错误响应 E2E 仍通过。
- 证据：`test-evidence/automation-20260831/AUTO-E2E-003-e2e.log` 中的 locator 错误。
- 状态：关闭
- 后续动作：已限定 `section.layers-panel[aria-label="图层"]`、`[data-layer-id]` 和名称 span，AI 分析成功应用/持久化 E2E 已通过；证据见 `AUTO-E2E-010-e2e-final.log`。

- 问题编号：ISSUE-020
- 类型：测试基础设施
- 优先级：P1
- 现象：模板 E2E 仍断言旧文案 `经营分析总览`；当前五模板中的首个标题为 `数据总览`，模板名称/内容已随提交更新。
- 复现步骤：执行 `pnpm test:e2e`，`first-stage-creation.spec.ts:26` 等待旧文本超时。
- 影响范围：模板创建 E2E 失败；组件层数断言已到达并确认 9 个图层。
- 证据：`AUTO-E2E-003-e2e.log`；当前模板实现见 `apps/web/src/features/screens/templates.ts`。
- 状态：关闭
- 后续动作：已将模板断言更新为当前契约文案 `数据总览`，模板创建 E2E 已通过；证据见 `AUTO-E2E-010-e2e-final.log`。

- 问题编号：ISSUE-021
- 类型：测试基础设施
- 优先级：P1
- 现象：`editor-shell.png` 差异 30,193 pixels（约 2%），`dark-player.png` 差异 91,580 pixels（约 5%）；当前提交新增组件、面板和主题样式，旧基线未同步评审。
- 复现步骤：执行 `pnpm test:e2e -- --project=chromium`，`screen-editor.spec.ts:131` 和 `screen-playback.spec.ts:58` 的视觉断言失败。
- 影响范围：编辑器壳层和深色播放视觉回归不能判定通过；其他功能断言和 light、letterbox、component-error、嵌入功能用例均通过。
- 证据：`test-evidence/automation-20260831/AUTO-E2E-010-e2e-final.log` 及其记录的 Playwright actual/diff 文件。
- 状态：关闭
- 后续动作：后续 2026-09-07 与 2026-09-08 完整 Chromium 均为 16/16。2026-09-08 的浅色 fixture 修复按独立 actual/diff 评审记录处理，不追溯改变本问题的历史证据。

- 问题编号：ISSUE-022
- 类型：文档不一致
- 优先级：P1
- 现象：README 仍描述九类内置组件；代码注册表和 2026-08-31 设计 spec 均为 21 类。
- 复现步骤：对照 README 第 3、19 行、`apps/web/src/features/runtime/registry.ts` 和设计 spec 第 6 节。
- 影响范围：维护者会低估组件范围，测试计划/验收口径可能错误；不直接阻断运行时。
- 证据：本计划“当前代码和版本依据”列出的文件和行；AUTO-UNIT-001 45/45 通过。
- 状态：关闭
- 后续动作：已将 README 和 `docs/TESTING_SESSION.md` 的组件数量更新为 21 类，并由 AUTO-UNIT-001 与 BB-001 复核。

- 问题编号：ISSUE-023
- 类型：环境阻塞
- 优先级：P2
- 现象：README 示例和首次烟囱命令使用 `python -c`，当前宿主无 `python` 命令，仅有 `python3`。
- 复现步骤：执行包含 `python -c` 的密钥生成命令，shell 返回 `command not found: python`，容器随后因空 master key 启动失败。
- 影响范围：依赖 README 复制命令的本机容器测试；不影响 uv/pytest 或产品运行时。
- 证据：`test-evidence/automation-20260831/AUTO-E2E-007-container-prereq-failures.log`；修正为 `python3` 后 AUTO-E2E-007 通过。
- 状态：关闭
- 后续动作：README 和测试计划已改用 `python3` 生成密钥；容器 smoke 按该命令复测通过。

- 问题编号：ISSUE-024
- 类型：测试基础设施
- 优先级：P2
- 现象：首次容器 smoke 使用的测试 master key 不是可解码的 32 字节值，服务启动抛出 `SecretKeyInvalid`。
- 复现步骤：使用错误长度 key 执行 `docker run ... datapulse-test:20260831`，Alembic 完成后 lifespan 拒绝启动。
- 影响范围：仅首次测试尝试；修正生成逻辑后容器 health、SPA、重启和非 root 检查均通过。
- 证据：`test-evidence/automation-20260831/AUTO-E2E-007-container-prereq-failures.log`；成功证据 `test-evidence/automation-20260831/AUTO-E2E-007-container-smoke.log`。
- 状态：关闭
- 后续动作：已固定 32 字节测试 key 生成方式并完成容器 health、重启和非 root 回归；失败前置记录保留作历史证据。

- 问题编号：ISSUE-025
- 类型：产品缺陷
- 优先级：P0
- 现象（修复前）：发布面板生成的 iframe 示例为 `/embed/{screen_id}?ticket=<短期 ticket>`，但播放器 `PlayerView` 要求同时提供 `instance_id`；按面板示例直接打开时页面显示“无法播放此大屏”。
- 复现步骤：创建并发布临时大屏；在编辑器点击“生成播放链接”并复制嵌入示例；用签发的有效 ticket 打开示例 URL；页面未发起 `/api/embed/screens` 请求并显示失效。将 URL 增加 `&instance_id=bb-test` 后可加载并返回 200。
- 影响范围：所有依赖管理界面 iframe 示例的嵌入集成，首次接入会失败；独立播放和带 `instance_id` 的 SDK/自定义宿主流程不受影响。
- 证据：修复前 `test-evidence/blackbox-20260901/BB-002/06-preview.png`、`test-evidence/blackbox-20260901/BB-004/05-valid-embed.png`（缺参数失败）和 `06-valid-embed-instance.png`（补参数成功）；修复后 `test-evidence/blackbox-20260901/BB-002/08-direct-embed-no-instance.png`、`AUTO-E2E-008-embed-rerun.log`。
- 状态：关闭
- 后续动作：播放器在缺省 URL 参数时生成内存实例 ID，同时保留 ticket 校验、URL 脱敏和 postMessage 实例校验；播放器 Vitest 4/4、直连 E2E 1/1 以及 BB-002/BB-004 复测通过。

- 问题编号：ISSUE-026
- 类型：环境阻塞
- 优先级：P1
- 现象：修复提交在本机无法推送到 GitHub；HTTPS 连接 `github.com:443` 超时，SSH 连接也被远端重置。
- 复现步骤：在 `main` 执行 `git push origin main`；随后使用短连接超时参数连续重试 3 次，并执行 `curl -v --connect-timeout 10 --max-time 15 https://github.com`。
- 影响范围：历史执行时远端 `origin/main` 停留在 `dbf2c5f`，当时无法启动新的 GitHub CI；截至 2026-09-08 该阻塞已解除，本地历史提交和测试结果不受影响。
- 证据：`test-evidence/automation-20260901/github-push-retry.log`。
- 状态：已解除（2026-09-08）
- 后续动作：历史网络失败证据保留；当前 `HEAD` 与 `origin/main` 均为 `34a704ee2cee3ac0d11e80e261681b29ba5d3ac4`，分支无领先/落后。本次任务未经授权未推送远端。

## 不在本次范围

- 本轮已修改并回归图表交互、E2E 选择器和 Linux 视觉基线；完整跨浏览器视觉评审和 GitHub CI 回归仍不在本轮已完成范围内。
- 不验收多用户协作、复杂权限、模板市场、组件市场、任意 JavaScript/HTML 注入、3D/WebGL/GIS 平台、固定数据表格化编辑、批量样式复制、TSV/CSV 导入和高 DPI/4K 播放优化。
- 不使用真实 AI provider、真实生产数据库、真实用户凭据、真实客户文件、生产反向代理或生产静态 `/embed` 环境。
- 不以本轮 Chromium 结果替代 Firefox/WebKit、真实密码管理器 autofill、代理部署下客户端 IP、长期播放和人工可用性验收。

## 生成说明

本轮实际读取的关键文件包括：根 `README.md`、`package.json`、`pnpm-workspace.yaml`、`pyproject.toml`、`Dockerfile`、`compose.yaml`、`compose.test.yaml`；`docs/TESTING_SESSION.md`、`docs/known-issues.md`、上一份 `2026-08-29-第01次测试计划.md`；`docs/superpowers/specs/2026-08-31-screen-component-system-and-demo-data-design.md`；Web 的 Playwright/Vite/TypeScript 配置、runtime/registry/mockData/dataRuntime、内置组件、编辑器组件库/Inspector/模板、`PlayerView.vue` 及对应测试；服务端 screen/AI/schema 实现和 screen/AI/connector 测试；`tools/run_e2e.py`、`tools/prepare_e2e.py`、`tools/check_generated.py`。

已确认并实际执行的命令：`pnpm check:contracts`、`pnpm test`、`pnpm --filter @datapulse/web test`、播放器 Vitest 定向命令、screen/AI pytest 定向命令、`pnpm typecheck`、`pnpm build`、`pnpm test:e2e`、`pnpm test:e2e -- --project=chromium`、直连嵌入定向 E2E、定向 `pnpm test:e2e -- --grep ...`、Compose PostgreSQL/MariaDB 启停与 `pnpm test:integration`、`docker build --tag datapulse-test:20260831 .`、隔离容器 health/SPA/restart smoke。结果和证据分别位于 `test-evidence/automation-20260831/` 下各对应日志；问题记录位于本文件“发现的问题”。

本轮实际执行的黑盒命令/操作：`curl -i --max-time 5 http://127.0.0.1:18080/api/health`；多次 `node --input-type=module` Playwright Chromium 内联脚本（访问 `http://127.0.0.1:5180/studio`，执行 BB-001～BB-008 的初始化、组件添加、数据源/数据集创建、预览/发布/播放、嵌入、CSRF、revision、AI 降级和模板删除操作）；修复后再次以无 `instance_id` 的 `/embed/{id}?ticket=...` URL 完成直连加载并保存 `BB-002/08-direct-embed-no-instance.png`；浏览器内调用 `POST /api/admin/embed/api-key`、`POST /api/embed/tickets`、`GET /api/embed/screens/{id}`、`PATCH/DELETE /api/admin/screens/{id}` 进行脱敏契约验证。截图证据位于 `test-evidence/blackbox-20260901/` 对应编号目录；自动化命令和日志位于 `test-evidence/automation-20260831/`。

本轮修复后新增实际执行：`pnpm --filter @datapulse/web exec vitest run src/features/runtime/builtins/chart-components.test.ts`（6/6，通过）；`pnpm --filter @datapulse/web test`（30 个文件、140 个测试，通过）；`pnpm typecheck`（通过）；`pnpm build:web`（通过，保留既有 chunk 大小提示）；在 `datapulse-e2e-linux:local` 中启动 Vite 后执行两个受影响规格的 Playwright 命令，首次 `--update-snapshots` 生成 Linux 基线，随后无更新快照复核 2/2 通过。快照证据位于 `apps/web/e2e/snapshots/linux/`；GitHub 推送因本机无法连接 `github.com:443`，尚未生成新的 CI 证据。

仍需人工确认的范围：Firefox/WebKit 编辑器视觉差异是否可接受；真实密码管理器、4K/高 DPI、长期播放、生产 CSP/代理 IP 语义。BB-005 恢复和 BB-007 fake AI 成功/异常路径已于 2026-09-08 补齐；真实模型和生产数据按任务约束未使用。上述剩余失败/阻塞不是功能未开发，但在关闭前第二阶段不能标记完成。
